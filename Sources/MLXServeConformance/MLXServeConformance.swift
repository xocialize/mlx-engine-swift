import MLXToolKit

// MLXServeConformance — the C0–C13 self-check harness contributors run before a PR.
// SCAFFOLDING PLACEHOLDER for this phase.
//
// Will turn the checklist into something executable: given a package's tool types and
// declarations, evaluate each C-level and emit a pass/fail report a contributor can act on
// before opening a PR (and that CI can run).
public enum MLXServeConformance {
    public static let contractVersion = ContractVersion.current
}
