# MLXEngine Model Registry

The **living** registry of every model package the engine can serve — both **availability awareness**
(what exists, where, published) and **state** (validated? efficiency-adopted? engine version). It
supersedes the generated `mlxengine-model-catalog` snapshot, which went stale because nothing kept it
current.

> **This file is maintained as a step of integration, not regenerated.** When you bring a package up,
> publish a tag, validate it in-app, or run its efficiency-adoption sweep, **update its row here in the
> same change** (see the `mlx-swift-integration` skill, Stage 2 → "Register in the model registry").
> A row that drifts from reality is a bug. The capability contract source of truth stays
> `Sources/MLXToolKit/Capability.swift`; this is the *provider* index against it.

**Columns**
- **Package / Model** — the `MLX<Name>` wrapper (or core) + the underlying model.
- **Role** — `wrapper+core` (single folded repo) · `wrapper`/`core` (split) · `shared` · `engine`.
- **Home** — bucket path under `~/Development` (`PROD` = published+tagged+URL-consumed · `WIP` = active · research = WAN_DEV/LTX_DEV clusters).
- **Avail** — ✅ published (URL/tag) · 🧪 WIP/local · 🔬 research.
- **Val** — in-app live-validation: ✅ validated · 🟡 partial/CLI-only · ⬜ not yet.
- **Eff** — 1.14 efficiency adoption (split footprint + mmap + per-stage evict + BudgetAware):
  ✅ adopted · 🔵 in progress · 🟡 brief written · ⬜ not started · ➖ n/a (trivial/single-component low-peak).
- **Eng** — the engine **contract version the package was validated/adopted against** (its resolved build), blank = backfill. NOT the SPM `from:` floor in `Package.swift` — that floor is a resolve-**forward** minimum (`from: "0.3.0"` resolves to the latest 0.x, i.e. 0.17.0), so a low floor is usually fine, not drift. Only bump a floor when the package's code actually needs a newer contract API (e.g. `peakActivationBytes`/`QuantConfigured`/`BudgetAware` ⇒ needs ≥0.14) and you're touching the package anyway; a floor-only bump across the library is churn we deliberately skip (see the Kokoro note).

_Seeded 2026-06-30. Val/Eff/Eng are best-effort at seed time — **backfill per package as it's revisited.**_

---

## 🔊 Audio

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| tts | mlx-kokoro-tts-swift | Kokoro-82M | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); non-autoregressive single-chunk forward (core pre/post-forward checks + wrapper post-synthesis checkpoint), cadence honestly coarse generate/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged v0.3.0. Eff n/a: single tiny model (82M, flat bf16 ~500 MB), negligible transient; engine bump not worth the churn. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source Kokoro-82M-bf16 incl. voices/*.safetensors; probe includes the configured defaultVoice embedding; modelDirectory escape hatch added; MAT-1..5 green; load() now materializes into the ModelStore layout + fromModelDirectory (leaves the core's legacy mlx-audio/<org>_<name> cache — one-time ~300 MB re-download on updated installs). Tagged v0.2.0. -->
| tts | mlx-qwen3-tts-swift | Qwen3-TTS | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09 (fleet sweep, the gate's motivating package): entry checkpoint made the FIRST act of run() (was notLoaded-first — the CAN-1 signature failure); mid-run cadence = the core's pre-existing E3 per-frame Talker bails (all 4 loops: synthesize/stream/batch/ICL GenerationLoop); CAN-1..3 green (cadence of record: generate/frame); engine pin 0.23.0→0.27.0. Tagged v0.5.0. split: 1.7B-8bit weights 2.65 GB + measured talker transient ~4.0 GB; FootprintConfigured (variant×size×quant); P2 N/A (talker+codePredictor interleave per frame). WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): QUANT-VARIANT declaration (checkpoint variant×size×quant computes the model repo) + shared Qwen/Qwen3-TTS-Tokenizer-12Hz source + vocab/merges/tokenizer_config sidecars on the model source; MAT-1..5 green across the full 25-checkpoint published catalog; kept the core HuggingFaceDownloader (already store-layout), delta = missing-set + WeightDownloadProgress forwarding + model/tokenizerDirectory escape hatches. Tagged v0.4.0. -->
| tts | mlx-voxcpm2-tts-swift | VoxCPM2 | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-patch Task.isCancelled bail added to the core AR loop (mlx-voxcpm-swift v0.3.0 — bail AFTER append so partial decode stays well-formed) + wrapper post-call rethrow; CAN-3 generate/frame; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. split: weights floor 9.3 GB + measured transient 4.0 GB (flat 11 GB under-declared the ~11.9 GB peak); QuantConfigured; P2 N/A (TSLM/RALM/LocDiT interleave per patch). WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source VoxCPM2-bf16, explicit-dir escape hatch added (modelDirectory), MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress, prewarm store-resolved. Tagged v0.3.0. -->
| tts | mlx-indextts2-swift | IndexTTS2 | wrapper+core | audio/PROD | ✅ (v0.2.0) | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-mel-token bail in the GPT AR loop + inter-stage cancelCheck, anti-laundering fix so cancel beats emptyGeneration (a mid-loop bail can CREATE a laundering path — empty-output guards downstream); CAN-3 generate/token + decode/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. Stage 2 2026-07-09. C7 = LicenseRef-Index-Model NonCommercial (.permissiveOrAcknowledged only, gate live-verified); specialties emotionControl+durationControl (E12 first realization — metaData emotion/emoAlpha/targetDuration/speechRate/seed; targetDuration EXACT 3.00s/3.0s). Born clean: split fp16 5.0 GB + measured in-app transient 5.0 GB (phys floor 4.76, run peak 9.61, post-evict 0.37); QuantConfigured fp16|int8|int4 (in-memory GPT-backbone quant); BudgetAware fp16→int8 drop; MAT-1..5 + live first-run materialization (3 sources → container store, 321 s incl. ~6.6 GB download). PUBLISHED 2026-07-09: pkg v0.1.1 + mlx-audio-dsp v0.1.0 (dep, PROD-promoted audio/PROD same day), engine v0.23.0 (LicenseRef-Index-Model + specialties), weights consolidated mlx-community/IndexTTS-2-fp16 (all 3 WeightSource roles). Remaining debt: fp16 w2v-BERT re-publish (−1.1 GB, needs p3w2v gate re-run). -->
| tts | mlx-gepard-swift | Gepard-1.0 | core | audio/WIP | 🧪 | 🟡 | ⬜ | 0.28.0 | <!-- Stage 1 (core port) COMPLETE + parity-validated 2026-07-10, CLI gates --p2..--p8 green: CodecOps/TextRepetition bit-exact, backbone (Qwen3.5 full-attn lifted from mlx-swift-lm Qwen35) prefill rel 4-6e-4, audio-interface+32 heads+stop rel 1e-7, Q-Former ladder rel 4-6e-4, greedy rollout integer-exact mod 1 fp-tie (exact-match-ceiling), NanoCodec 21.5fps decoder max|Δ| 7e-6/SI-SNR 104dB. GPU/Metal e2e valid audible audio + REALTIME (clean reboot: 8.6 ms/frame, gen-RTF 0.187 = 5.3× realtime, TTFA 22 ms — beats 5090 ref 46 ms). P9 encoder (ref-audio→codes for the Q-Former) IN PROGRESS. C7 = two PERMISSIVE weight layers: LM Apache-2.0 + codec LicenseRef-NVIDIA-Open-Model (reviewed OMLA v2024-06-14, added to permissiveAllowlist in engine v0.28.0) ⇒ SHIPPABLE under default .permissiveOnly (NOT eval-gated); §3.1 Notice obligation on codec weights. Stage-2 engine-wrap kicked off: template IndexTTS2Package (2-module split GepardCore core + MLXGepardTTS wrapper PENDING); WeightSourcing main=nineninesix/gepard-1.0 + codec=xocialize/nanocodec-22khz-1.89kbps-21.5fps-mlx (repo publish pending). Remaining: GepardConfiguration+GepardPackage (manifest+load/run+CAN), bf16 footprint, MAT-1..5 + CAN-1..3, app validation (dBFS/RTF/short-word matrix), publish + registry Avail→✅. plan=mlxengine-audio/Docs/Gepard-Swift-Port-Plan.md. -->
| audioSeparation | mlx-demucs-swift | HTDemucs v4 | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); SwiftDemucs core bails per 30 s chunk (demucs-mlx-swift v0.1.1 — core checkCancelled() gained the Task lane; it previously only read its own cancel() flag), CancellationError rethrown unchanged + mlxInferenceFailed wrap now passes it through; CAN-1..3 green (cadence generate/chunk, long-run via 2.2 GB peak); engine pin 0.27.0. Tagged v0.3.0. split: weights floor ~0.3 GB (84 MB fp16 on disk) + 30 s-chunk transient ~2.2 GB (flat 2.5 GB baked it in); QuantConfigured (fp16); P2 N/A (single VocalSeparator); unload clearCache. activation = smoke est, phys re-baseline pending. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source htdemucs-ft-vocals-mlx (one safetensors), modelDirectory escape hatch added, MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress. Tagged v0.2.0. -->
| audioSeparation | mlx-mel-roformer-swift | Mel-Band-RoFormer | wrapper+core | audio/PROD | ✅ | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); SwiftRoFormer core bails per 8 s chunk (mel-roformer-mlx-swift v0.1.2 Task lane); CAN-1..3 green (cadence generate/chunk declared despite short-run envelope — exemption not credible for minutes of chunked compute); engine pin 0.27.0. Tagged v0.3.0. split per variant: kimVocal2 bf16 0.7 GB + 8 s-chunk transient 0.8 GB · zfturboVocalsV1 fp16 0.3 GB + 0.3 GB (flat 1.5/0.6 GB); QuantConfigured (variants distinguished by quant); P2 N/A (single RoFormerSeparator); unload clearCache. activation = smoke est, phys re-baseline pending. Val 🟡 left as-is (out of scope). WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): VARIANT-KEYED declaration (kim-vocal-2 vs zfturbo-vocals-v1 repos follow the variant), MAT-1..5 green per variant, modelDirectory escape hatch, load() via WeightMaterializer + MelRoFormer(config:)+WeightLoader (was fromPretrained/HubApi). Tagged v0.2.0. -->

| speechEmotion | mlx-emotion2vec-swift | emotion2vec+ | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); CAN-1..3 green (subSecondRuns — one classification forward per short utterance, single MLX eval); engine pin 0.27.0. Tagged v0.3.0. Eff n/a (deliberate, 2026-06-30): single-component recogniser (flat fp16 ~1.0 GB ≈ weights, one short-utterance forward → negligible transient). Split would be low-value; revisit only if it becomes a co-residency bottleneck. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source emotion2vec-plus-large-mlx (safetensors + config), modelDirectory escape hatch added, MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress. Tagged v0.2.0. -->
| audioCodec | mlx-mimi-codec-swift | Kyutai Mimi | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); CAN-1..3 green (subSecondRuns — encode is one whole-clip MLX graph eval; "streaming" describes causality, not execution); engine pin 0.27.0. Tagged v0.3.0. Eff n/a (deliberate, 2026-06-30): single MimiEncoder, flat fp32 ~0.8 GB, streaming neural codec → negligible transient. Not worth the engine-bump churn. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source mimi-encoder-mlx (encoder.safetensors), modelDirectory escape hatch added, MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress. Tagged v0.2.0. -->
| audioPolish | mlx-audio-polish-swift | AudioPolishKit (DSP) | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); CAN-1..3 green (subSecondRuns — classical Accelerate DSP, no MLX eval); engine pin 0.3.0→0.27.0 + MLXServeConformance test dep added. Tagged v0.2.0. Eff n/a (deliberate, 2026-06-30): Accelerate DSP — NO MLX, no weights, no GPU (the non-MLX capability seam). Nothing to split; the efficiency contract doesn't apply. -->

| soundEffect | mlx-moss-soundeffect-swift | MOSS SoundEffect | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-denoise-step onStep checkpoint (pre-existing, throwing) + new pre-VAE-decode seam (core moss-soundeffect-mlx-swift 0.2.0); CAN-3 denoise/step + decode/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged 0.3.0. split per quant: resident post-evict (DiT + fp32 VAE) bf16 5 GB / int4 3 GB + 100-step CFG denoise @1500-token transient ~10 GB (flat 15/13 GB); QuantConfigured (bf16/int4). P2 APPLIED: Qwen3-1.7B text encoder (~4 GB) encoded once then EVICTED before the denoise loop (encoder-evict lever, in core Pipeline.generate) — removed from the resident floor. unload clearCache. activation = smoke est, phys re-baseline pending. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single consolidated "main" source (DiT/VAE + text_encoder/ + tokenizer/ in ONE repo — Qwen3 encoder is NOT separate); repo-keyed (bf16/-4bit); modelDirectory escape hatch; MAT-1..5 green; load() via WeightMaterializer into store layout (old HubApi models/<org>/<name> snapshots re-download once). Tagged 0.2.0. DECIDED 2026-07-09: the frozen (stock-weights) Qwen3-1.7B text_encoder STAYS in the consolidated repo — no think-area split. No second 1.7B-conditioner consumer exists (z-image/klein vendor 4B with different taps), so a split buys nothing and churns the validated encoder-evict lever + MAT globs. Revisit trigger = product pull: a second port needing Qwen3-1.7B hidden-state conditioning → promote the encoder CODE to a think backbone pkg (qwen3vl-mlx-swift pattern) + add a second WeightSource role "text-encoder" on a shared weight repo. -->


## 🖼️ Image

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| textToImage | ernie-image-swift | ERNIE-Image-Turbo | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); core denoise/step checkpoint + encode/decode seams; CAN-1..3 green; engine pin 0.27.0; also fixed ErnieImageTests broken since the Lens→flux2-vae split (blocked ALL test targets). Tagged v0.2.0. split: resident floor bf16 15 GB / int4 5 GB (DiT+VAE; flat was 26/16) + ~11 GB transient; QuantConfigured (bf16/int4); P2 = Mistral-3B encoder load→encode→evict before denoise. activation = smoke est, phys re-baseline pending (image app autorun). -->
| textToImage | lens-mlx-swift | Lens 3.8B | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); GPT-OSS post-evict seam + denoise/step bails + pre-decode seam; CAN-1..3 green both tiers; engine pin 0.27.0; stale flat-footprint testManifest fixed (dead since the 1.14 split). Tagged v0.3.0. split: resident floor bf16 8 GB (DiT+VAE; flat was 62) + ~54 GB transient (GPT-OSS-20B encoder load dominates); QuantConfigured (bf16); P2 = encoder load→encode→evict before denoise, both LensT2I + LensTurboT2I wrappers via shared core. activation = smoke est, phys re-baseline pending (image app autorun). -->
| textToImage / imageEdit | z-image-swift | Z-Image 6B (S3-DiT) | wrapper+core | image/PROD | ✅ (v0.3.0) | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise loop bails per step (ZImagePipeline, shared t2i/img2img both tiers) + cancelled-task decode skip; CAN-1..3 green both tiers; engine pin 0.27.0. Tagged v0.3.0. Tongyi-MAI Z-Image, fully Apache-2.0 (DiT+Qwen3-4B encoder+FLUX.1 AE). Two tiers via shared ZImageGenerator: ZImageT2IPackage (Base, 28-step CFG, z-image-t2i) + ZImageTurboT2IPackage (distilled 8-step no-CFG, z-image-turbo-t2i). split QuantFootprint: resident bf16 20 / int8 15 / int4 6 GB (DiT+encoder+VAE co-resident — encoder small enough that P2 evict NOT warranted here) + ~14-20 GB transient (int4 decode scratch dominates). QuantConfigured (bf16/int8/int4; quant on load from bf16 snapshot — pre-quant repos later). WeightSourcing/MAT (7/7). unload()→clearCache. Parity fp32/CPU: DiT cos ≥0.9999999, VAE 118 dB, encoder ids-exact, e2e 105-108 dB; GPU int4 1024² ~13 s/25.7 GB peak. img2img SHIPPED v0.2.0 (imageEdit surface z-image-{,turbo-}img2img; encode→renoise@strength→denoise, metaData strength default 0.6, reuses full AutoencoderKL encoder). Weights mlx-community/Z-Image-{Turbo-bf16,bf16}. Val 🟡 = CLI pkg-e2e green (real load→run→decode), pending in-app generation + phys re-baseline (smoke under-reads ~2.7×). -->
| textToImage / imageEdit | flux2-klein-swift | FLUX.2-klein-4B (MMDiT) | wrapper+core | image/PROD | ✅ (v0.5.0) | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); encode seam (encodeAndEvict) + per-step bails in KleinPipeline/KleinEditPipeline + decode skip; CAN-1..3 green both tiers; engine pin 0.27.0. Tagged v0.5.0. BFL FLUX.2-klein-4B, Apache-2.0 (4B ONLY — 9B variants are FLUX Non-Commercial, intentionally NOT wrapped). Klein4BT2IPackage (surface flux2-klein-4b-t2i) via KleinGenerator. Compact rectified-flow MMDiT (5 double + 20 single blocks, 24 heads, 4-axis RoPE θ2000) + Qwen3-4B 3-layer-tap (9/18/27→7680) conditioner + FLUX.2 VAE (reused from flux2-vae-mlx-swift). Distilled 4-step, guidance 1.0, no neg prompt. split QuantFootprint: resident bf16 16 / int8 12 / int4 11 GB (DiT+encoder+VAE co-resident) + ~8 GB transient; int4 DiT 2.35 GB / peak 19.2 GB @1024². QuantConfigured (quant-at-load from bf16 snapshot). WeightSourcing/MAT (5/5). unload()→clearCache. Parity vs diffusers goldens: DiT cos ≥0.9999995 @64×64 grid, VAE 105-130 dB, encoder cos 0.9999999; GPU int4 1024²/4-step ~6 s. Weights mlx-community/FLUX.2-klein-4B-bf16. THREE pipeline bugs found via capturing diffusers' exact transformer inputs: full-512-padded-text-tokens, diffusers-linear-mu scheduler, chat-template. Multi-ref EDIT SHIPPED v0.2.0 (surface flux2-klein-4b-edit, IEditRequest multi-image): net-new FLUX.2 VAE encoder (cos 1.0/130dB) + reference token-concat + 4D-RoPE t-offset t=10*(i+1); plain path (KV-cache speed TODO); fox-ref+beach-prompt validated through real package. Weights unchanged (encoder.* keys in same snapshot). **Val ✅ (v0.4.1)** — in-app IMAGE_AUTORUN validated: t2i/edit coherent+adherent, 2-ref fuse confirmed. **Footprints RE-BASELINED to in-app phys** (int4 t2i floor 9.32/peak 20.54/act 11.22; edit 2-ref peak 20.57). **ENCODER-EVICT light tier v0.4.0** (KleinConfiguration.evictEncoder): per-request Qwen3-4B evict → **phys @768² floor 5.13/peak 10.06 = FITS 16 GB**; surfaced via FootprintConfigured so the governor admits it. Consumer ladder: 16 GB evict@768² → 24 GB evict@1024² → 32 GB+ resident → 64 GB base → 128 GB base-bf16. BASE quality tier v0.3.0 — see next row. -->
| textToImage / imageEdit | flux2-klein-swift | FLUX.2-klein-base-4B (MMDiT, CFG) | wrapper+core | image/PROD | ✅ (v0.5.0) | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: see distilled klein row — same repo/v0.5.0, CAN-1..3 green on Klein4BBaseT2IPackage too. BFL FLUX.2-klein-base-4B, Apache-2.0 (4B ONLY). Klein4BBaseT2IPackage (surfaces flux2-klein-4b-base-t2i / -base-edit) — the QUALITY tier, DISTINCT PackageID from the distilled Klein4BT2IPackage so both co-resist. Transformer architecture BYTE-IDENTICAL to distilled klein-4B (same 5 double + 20 single MMDiT, guidance_embeds=false) → drop-in through the same KleinWeights loader; text_encoder (Qwen3-4B) + FLUX.2 VAE are the SAME frozen weights as the distilled tier. The base checkpoint is NOT guidance-distilled: runs classic two-pass classifier-free guidance (do_cfg = guidance_scale>1 and not is_distilled) — noise = neg + scale·(pos−neg), guidance=nil to the DiT both passes — + negative prompts over ~28 steps (KleinConfiguration.base(): repo klein-base-4B, defaultSteps 28, guidanceScale 4.0). CFG in BOTH KleinPipeline (T2I) and KleinEditPipeline (edit; references identical in both passes). split QuantFootprint: resident bf16 16 / int8 12 / int4 11 GB (same as distilled) + ~10 GB transient (CFG runs DiT 2× per step; edit doubles the seq → base edit ≈4× base T2I). QuantConfigured (quant-at-load). WeightSourcing/MAT + base conformance 9/9 (MAT gate, .base()/.turbo() factories, distinct base surfaces, guidanceScale Codable). Validated on GPU: base T2I 28-step CFG renders dense picnic scene w/ correct object counts + legible 'PICNIC' text (distilled 4-step degrades to 'PIC NC'); base edit CFG preserves reference-fox identity while composing helmet + castle-wall-at-sunset. Weights mlx-community/FLUX.2-klein-base-4B-bf16 (transformer new; encoder/vae identical to distilled repo). Val 🟡 = CLI + oracle-validated, pending in-app + phys re-baseline. -->

| textToImage / imageEdit | boogu-image-swift | Boogu-Image-0.1 | wrapper+core | image/WIP | 🧪 | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise/step bails (t2i+edit) + decode skip, post-encode-evict checkpoints pre-existing; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. encoder-evict (P2 headline) + split. Qwen3-VL-8B conditioner (~16 GB bf16) loaded→encode pos/neg→eval→evict (nil + clearCache) BEFORE the DiT denoise, on EVERY quant — moves ~16 GB resident→transient. Post-evict resident floor = DiT(quant)+FLUX VAE ≈ int4 8 / int8 13 / bf16 20 GB (flat was 24/29/36, which baked the conditioner in) + ~20 GB transient (max(encode, denoise)). QuantConfigured (int4/int8/bf16; quant=DiT only). unload()→clearCache; per-step GPU.clearCache (Pipeline.swift:69) + absolute snapshot-path resolution NOT regressed. Parity gate green post-refactor (s0-keys exact 942/244; s1-sched 6.3e-8; s1-vae decode/encode 0.0; s2-dit t2i/edit 0.0 — bit-exact). Known PRE-evict in-app phys was floor 67.4 / peak 70.5 GB (vs 36 GB declared, ~1.85× gap); activation = smoke/derived est, in-app phys re-baseline pending (MLXEngineImage autorun, post-evict). --> |
| textToImage | anima-mlx-swift | Anima (Cosmos-Predict2-2B) | wrapper+core | image/PROD | ✅ (v0.1.1) | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise/step bails in AnimaPipeline.sample + encode/decode seams; CAN-1..3 green; engine pin 0.27.0. NOTE longRunImplied=false only because the flat pre-1.14 footprint declares no peakActivationBytes — cadence declared anyway; flip the suite's assert when the split-footprint retrofit lands. Committed on wip/rope-extrapolation-fix (no tag/push — rides the in-flight RoPE branch). NC weights (C7 two-layer gate, rejectedWeight under .permissiveOnly); parity-locked to xocialize/anima-mlx Python (e2e cos 0.999105); measured peak bf16 8.0 GB / int4 6.5 GB @512². Wired into MLXEngineImage ImageValidationView (bf16 + int4 variants, NC admitted via .permissiveOrAcknowledged) — app BUILDS; Val 🟡 = CLI --package-gate green + app-wired, pending one interactive in-app generation. -->
| imageEdit | qwen-image-edit-swift | Qwen-Image-Edit-2511 | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() in ALL THREE wrappers (base/Turbo/TeleStyle — Turbo's even sat after the LoRA-swap network path); shared-core QwenImageEditGenerator denoise/step checkpoint + encode/decode seams; LoRA-download laundering fixed (LoRACache.ensure rethrows CancellationError); CAN-1..3 green ×3; engine pin 0.3.0→0.27.0. Tagged v0.4.0. --> |
| imageEdit | qwen-image-edit-swift | FireRed-Image-Edit-1.1 | wrapper (checkpoint swap) | image/PROD | 🟡 | 🔴 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: rides the 2511 row (same QwenImageEditPackage code path, v0.4.0). FR1 (FIRERED-ENHANCEMENTS.md): architecturally stock Qwen-Image-Edit-Plus → NO port/conversion; FireRedImageEdit.registration in MLXQwenImageEdit reuses QwenImageEditPackage + QwenImageEditConfiguration(snapshotPath:<firered snapshot>), own manifest (Apache-2.0, provenance FireRedTeam/FireRed-Image-Edit-1.1, PackageID firered-image-edit). 2511-measured split footprint applies (identical graph). GATES: 57.7 GB snapshot download, in-app A/B vs 2511 (self-reported GEdit 7.943 vs 7.877), true-CFG scale validation (FireRed-1.0 "CFG offset too large"). Never stack 2511-trained TeleStyle LoRAs on these weights; FireRed Lightning LoRA (8-step) only fuses onto FireRed base. --> |
| imageColorize | mlx-ddcolor-swift | DDColor | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); re-baselined 4.54 GB peak makes it long-run implied so cadence (encode/frame + postprocess/frame) declared, NOT the sub-second exemption; first test target added; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. --> |
| imageRestore | mlx-nafnet-swift | NAFNet (v0.4.0) | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); monolithic full-frame eval — cadence encode/frame + postprocess/frame at the real seams; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. --> |
| imageUpscale | mlx-realesrgan-swift | Real-ESRGAN 4× | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-64²-tile checkpoint in the core tile driver, CancellationError exempted from the PlaybackTierError wrap (laundering fix); cadence upsample/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged v0.5.0. BUNDLED-WEIGHTS posture, first-class since 2026-07-09: all 3 SRVGGNetCompact checkpoints (~2 MB each) are vendored in the core bundle — there is NO download, ever. Engine 0.24.0 (contract 1.17) grew the bundled MAT vocabulary FOR this package (the filed gap, closed same day): BundledWeightSourcing declares the selected variant's checkpoint (role "checkpoint" → Variant.bundledWeightsURL); the gate verifies it PRESENT on a fresh machine (inverse of WeightSourcing's required-missing rule) and engine.needsDownload reads FALSE via the sanctioned bundled signal (pre-0.4.0 the store-marker heuristic read TRUE forever — the ModelSheet Studio G6 fast-upscale degradation; v0.4.0 bridged with always-present prewarmPaths). WeightSourcing stays undeclared by design (no fresh-machine network source; the HF repo also lacks the anime variant); WeightPrewarming keeps its cold-start page-in job. FULL MAT gate green per variant + e2e MLXServeEngine register→needsDownload==false. Engine pin 0.23.0→0.24.0. Tagged v0.4.1. --> |
| imageUpscale | mlx-seedvr2-swift (MLXSeedVR2) | SeedVR2-3B | wrapper+core | video/PROD | ✅ | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09 (see videoUpscale row — same package, v0.6.0): CAN-1..3 green on both surfaces. Export/diffusion tier of imageUpscale (Real-ESRGAN = fast tier; multi-package via PackageID). ONE `SeedVR2UpscalePackage` exposes imageUpscale + videoUpscale (see the videoUpscale row — SAME package, loads the 3B core once). Lives in `mlx-seedvr2-swift`, which folded `SeedVR2MLX` core in: the standalone `seedvr2-mlx-swift` core repo was ARCHIVED read-only 2026-06-15, so the EdgeTAM same-repo fold couldn't ship there — consolidation went the REVERSE of BRIDGE-VID-002's note (core folded INTO the wrapper repo, wrapper NOT retired; `mlx-seedvr2-swift` is the single live home). Born sweep-clean: QuantConfigured fp16+int8 ONLY (int4 degrades 22.7 dB); BudgetAware fp16→int8 under pressure; unload()→clearCache; mmap/lazy. Split measured via seedvr2-package-smoke @256²→512²: int8 resident 4.72 GB / MLX-peak 7.07 GB (transient ~2.35 GB); fp16 est. Activation 3.5 GB conservative (single-pass, resolution-driven); in-app phys re-baseline pending (~2.5–2.9× under). License Apache/MIT permissive. Published v0.3.0 (a45b7eb), relocated to video/PROD. Val 🟡 = CLI gate green (valid 512²); MLXEngineImage in-app run pending (owner's call — it's imageUpscale, has IMAGE_AUTORUN) for Val→✅ + phys re-baseline. Resolves BRIDGE-VID-002. --> |
| imageInpaint | mlx-lama-swift | LaMa + MI-GAN | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); LaMa/MI-GAN each one monolithic eval — cadence encode/frame + postprocess/frame, pre-forward checkpoints after both lazy tier builds; first test target added; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. LIGHT split + unload-clearCache + QuantConfigured. Split the flat fp16 4.5 GB into resident floor 0.5 GB (inpainter weights: LaMa-bf16 ~0.1 GB on disk + MI-GAN-fp16 ~15 MB; load() holds only LaMa, MI-GAN loads on first `fast` request — both nets tiny, P2 per-stage-evict skipped as negligible) + ~4.0 GB FFC inpaint transient (measured e2e erase peak 2.8 GB fp32 @880²; total envelope stays ~4.5 GB). dtype stays bf16 (LaMa fp16-FATAL; .fp16 is the quant-tier LABEL only). unload()→clearCache. 2-input surface (image+mask) → headless autorun can't read an arbitrary mask (sandbox), so activation = smoke/derived est, in-GUI phys re-baseline pending. --> |
| matting | mlx-birefnet-swift | BiRefNet | wrapper+core | image/PROD | ✅ | ✅ | ✅ P1a (P1b deferred) | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); monolithic single-eval forward — cadence encode/frame + postprocess/frame at the real seams (pre-forward after lazy best-tier build, pre-encode); CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. --> |
| promptSegment / trackObject | mlx-edgetam-swift | EdgeTAM (SAM 2) | wrapper+core | image/PROD | ✅ (v0.4.0) | ✅ | ✅ (activation = est, phys re-baseline pending) | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint was ALREADY first; pre-existing per-frame track checkpoint declared generate/frame (+ postprocess/frame for promptSegment) — honest cadence over the available exemption; CAN-1..3 green both surfaces; engine pin 0.27.0. Tagged v0.4.0. --> |
| imageQualityScore | mlx-siglip2-iqa-swift | SigLIP2 NR-IQA | wrapper+core | image/WIP | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); one small batched forward — subSecondRuns exemption; CAN-1..3 green; engine pin 0.27.0. Tagged v0.3.0. --> |
| opticalFlow | mlx-sea-raft-swift | SEA-RAFT | wrapper+core | image/WIP | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); RAFT refinement loop is LAZY graph construction (one eval — per-step checkpoints would be decorative) so subSecondRuns exemption with rationale, post-eval checkpoint added; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. --> |
| imageTo3D | mlx-trellis2-swift | TRELLIS.2 / Pixal3D | wrapper+core | mlxengine-3d/WIP | ✅ (0.5.0) | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() (was validation-first); FlowSampler per-denoise-step bail across all 4 diffusion stages + SparseVAEDecoder per-block bails + pipeline stage-seam early-outs (seams placed BEFORE shape-sensitive consumers — partial feats would crash flexiPost / trip coords precondition), wrapper rethrows unchanged; CAN-1..3 green (denoise/step + decode/layer); engine pin 0.12.0→0.27.0 (mlx-swift exact:→from: required — engine floors 0.31.5); first-ever test target added. Tagged 0.4.0. WeightSourcing retrofit 2026-07-09 (was pre-MAT, field-issue MSS-7 — fresh installs silently degraded instead of downloading): single "snapshot" source xocialize/trellis2-mlx (~15 GB consolidated, globs safetensors+json+LICENSE/NOTICE/DINOv3_LICENSE.md); missing-probe = STRICT all-10-files (loadConsolidated tolerates absent tex/cascade files, so a lenient probe would silently degrade textures/tiers on a half-materialized snapshot); weightsRootOverride escape hatch honored first (flat or org/name layout, never networks); load() via WeightMaterializer (HubClient) w/ .downloading progress; prewarm store-resolved; upstream 3-repo + HF-cache chain stays a load()-time fallback only (NOT declared/probed — keeps MAT-4 honest; store-rooted machines with cache-only weights re-download once, fleet-accepted). Repo UNGATED 2026-07-09 (same day): re-framed as a PIPELINE artifact (converted DINOv3 conditioner integral; card redirects DINOv3-seekers to Meta's repo; terms-respect = license-with-artifact + C7 gate + "Built with DINOv3" app caption) — token-less first-run materialization verified, no HF_TOKEN needed. MAT-1..5 green + layout/partial-snapshot tests. Tagged 0.5.0. --> |

> BiRefNet `Eff: ✅ P1a` (2026-06-30, engine 0.14.0) — the motivating same-quant multi-mode case (fast@1024 vs best@2048). Split declared on the **fast** envelope: `QuantFootprint(.fp16, resident 0.9 GB, peakActivation 14 GB)` (+ `QuantConfigured`), replacing the flat 6.5 GB → engine charge ~0.9 GB resident + a shared transient. best stays a runtime-guarded variant (`insufficientMemoryForBest`, guard 48 GB; peakActivation ~47.8 GB, documented not admitted). **RE-BASELINED 2026-06-30** to in-app process `phys_footprint` (fast 13.7 / best 47.8 GB peak) after the MLXEngineImage acceptance run — the birefnet-smoke MLX-peak (4.9/18.3) under-read the real admission basis ~2.7× (MLX cache + overhead); guard 19.5→48 GB stops best falsely admitting on 32 GB Macs. **NB the optimizer trio + other smoke-measured packages likely share the same MLX-peak-vs-phys under-read — re-measure in-app.** **P1b deferred** — promoting mode → PackageID (best first-class admitted) is a coordinated change; the PROD consumer (`EngineMatteProvider`) relies on per-request `req.mode` + the fallback.

> Qwen-Image-Edit-2511 `Eff: ✅` (2026-06-30, engine 0.15.0) — the heaviest image package, three wrappers (`MLXQwenImageEdit` base · `MLXQwenImageEditTurbo` bf16/int4 · `MLXTeleStyle`) over one shared core (`QwenImageEditGenerator`). **P2 (headline) = per-stage encoder eviction:** the ~16.6 GB Qwen2.5-VL encoder, used once per request then idle through denoise+decode, is no longer held resident — the core generator owns an async `encoderProvider`, loads it, encodes, `eval`s the embeddings, then drops the ref + `Memory.clearCache()` before the DiT denoise peak (Swift 6: the now-async `generate`/`loadEncoder` take `isolated (any Actor)? = #isolation` to stay on each wrapper's `@InferenceActor`; back-compat `keepEncoderResident` init for parity tests). All three wrappers inherit it; the DiT (with its resident LoRA swapper) + small fp32 VAE stay resident. **Split (measured M5 Max, 1024²/8-step CFG4, `QIE_MEMBENCH` in MLXQwenImageEditTests):** bf16 resident floor (DiT 40.9 GB + fp32 VAE 0.5 GB) **41.4 GB**, worst peak **59.2 GB** → activation (peak−floor, the transient encoder load) **17.9 GB → 21 GB** declared (+20%). base/TeleStyle `QuantFootprint(.bf16, resident 42 GB, peakActivation 21 GB)` replaces the flat 60 GB; Turbo adds `(.int4, resident 12 GB, peakActivation 17 GB)`. All three configs now conform to `QuantConfigured` (Turbo's `quant` is computed bf16/int4). P3 (mmap): verified — the measured floor equals on-disk DiT+VAE bytes exactly, so the per-key `asType` rebuild does NOT eager-copy. P4 (BudgetAware): deferred (quant is config-chosen). **FLAG:** the int4 split is DERIVED from the documented component measurements (pre-split resident 22 GB included the co-resident encoder); the bf16 split is freshly measured — re-measure int4 via `QIE_MEMBENCH` with `quantizedDiTPath`+`quantizedEncoderPath` once the image category app is stood up for app-autorun.

> EdgeTAM `Eff: ✅` (2026-06-30, engine 0.17.0) — light split + `unload()` clearCache (batch #6, non-image/video stack). Single fp16 checkpoint (54 MB on disk), two surfaces (`promptSegment` image + `trackObject` streaming video) over one encoder+decoder. Flat `QuantFootprint(.fp16, 1.5 GB)` → **split** `QuantFootprint(.fp16, resident 1.0 GB, peakActivation 0.5 GB)`; config now conforms to `QuantConfigured`. residentBytes = the RepViT encoder+FPN+decoder weights floor held resident through the per-frame loop; peakActivationBytes = one frame's encode+decode transient. **P2 N/A** — the encoder IS the per-frame core (not an evictable upfront stage); the video path already runs `MLX.GPU.clearCache()` per frame (EdgeTAMVideo.swift:411) so streaming stays flat (~1.0 GB + 0.37 MB/frame) — kept, not regressed. `unload()` now `MLX.Memory.clearCache()` after niling predictor/videoPredictor. mmap/lazy: noted — the core `fromPretrained` does `.mapValues{ asType }` (eager) but it's a 54 MB no-op cast (already fp16), left as-is. **Smoke green** (promptSegment, 960×540 synthetic, fp16): valid binary matte, score 0.945, MLX-peak **0.41 GB** (matches documented image-mode 0.42). **FLAG:** activation = smoke/derived est — in-app process `phys_footprint` (R-MEM-1/admission, ~2.5–2.9× a smoke MLX-peak) re-baseline pending (promptSegment via MLXEngineImage, trackObject via the video app).

> Optimizer image trio `Eff: ✅` (2026-06-30, engine 0.15.0) — NAFNet (`imageRestore`) · Real-ESRGAN (`imageUpscale`) · DDColor (`imageColorize`), the ForgeOptimizer chain alongside BiRefNet matting. All three are single-component (P2/P3 n/a) and activation-dominated, so the win is the **split + the shared transient reserve**: each now declares a small resident weights floor + its activation peak (measured via each package's own smoke target through `MLXServeEngine` at the documented envelope), and all conform to `QuantConfigured`. Floors/activation: NAFNet signage **64 MB / 2.0 GB** (fp16) + width64 **512 MB / 2.9 GB** (fp32) @1024² · Real-ESRGAN **32 MB / 2.2 GB** (fp32, tile-bounded — 1024² peaked below 512²) · DDColor **512 MB / 1.8 GB** (fp16, best). Finding: the old flat NAFNet-fp16 0.6 GB and Real-ESRGAN 1.0 GB **under-declared** the real activation peak; the split right-sizes the charge AND frees it into the single shared reserve, so the trio + BiRefNet co-reside on ~1.1 GB of weights (64+32+512+~900 MB) sharing ONE ~4.4 GB transient (BiRefNet fast is the max) instead of each baking activation into residency. BudgetAware deferred on all three (validated runtime dtypes, no in-variant quality/memory lever).

## 🎬 Video

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| textToVideo | **ltx-2-mlx-swift** | Lightricks LTX-2.3 | wrapper+core | video/LTX_DEV (research) | 🔬 | ✅ | ✅ **+tiers** | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint moved to first act of run() (was notLoaded-first — even the program's proving package had the CAN-1 drift); per-denoise-step + per-VAE-chunk checkpoints pre-existing and RunProgress-evidenced (DenoiseLoop.swift:75/119, VideoVAE.swift:93), encoder seams declared; CAN-1..3 green; engine pin 0.25.0→0.27.0. Commit c1c961f, no tag (research repo). --> |

> LTX `Eff +tiers`: memory-tier profiles landed (LOW-TIER-PLAN complete) — compact24 15.4 GB / balanced32 16.1 / standard64 37.5 / max128 92.2 measured peaks; 24 GB Macs supported, 16 GB declared ineligible.
| textToVideo | helios-mlx-swift | Helios (Wan) | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); FIXED `try? onChunk` cancellation swallow (documented per-chunk cancellation never worked) — Task.isCancelled bails per AR chunk/DMD step + per decode chunk, throwing post-generate rethrow; CAN-1..3 green; engine pin 0.27.0. Commit 3e21af1, no tag (research). --> |
| textToVideo | ti2v-5b-mlx-swift | Wan TI2V-5B | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise/step onStep now on ALL paths (t2i gained pass-through onStep) + vae22 decodeStreaming22 per-chunk bail (wan-core b791b62, local-path pin); CAN-1..3 green; engine pin 0.27.0. Commit 287f711, no tag (research). --> |
| textToVideo | phantom-wan-mlx-swift | Phantom-Wan | wrapper+core | video/WAN_DEV (research) | 🔬 | ⬜ | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); S2V sampler per-step onStep + decode/chunk bail with post-core rethrow; CAN-1..3 green; engine pin 0.27.0. Commit 5287b77, no tag (research). --> |
| videoEdit | bernini-r-mlx-swift | Bernini-R | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-denoise-step throwing onStep in all t2v/t2i/r2v/v2v/rv2v paths + wan-core streaming-decode per-chunk bail with post-core rethrow; CAN-1..3 green; engine pin 0.9.1→0.27.0. Local commit c28710f (branch phase-b-wan-core, no upstream), no tag. --> |
| videoEdit | vace-mlx-swift | VACE (Wan) + VACE-Fun-A14B | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() in BOTH MLXVACEPackage + MLXVACEFunPackage; shared surface runners per-denoise-step onStep + decode/chunk bail; CAN-1..3 green per PackageID (4 tests); engine pin 0.27.0. Local commits d422afe (pre-existing Fun-A14B WIP, labeled) + 15c155a (CAN) — NOT pushed (would publish the WIP); push when Fun-A14B is ready. --> |
| videoEdit | chronoedit-mlx-swift | ChronoEdit | wrapper+core | video/WAN_DEV (research) | 🧪 | ⬜ | ⬜ | |
| characterAnimation | scail-2-mlx-swift | SCAIL-2 | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); segmented DPM++ per-step onStep + decode/chunk bail with post-core rethrow; CAN-1..3 green; engine pin 0.27.0. Commit 809cc26, no tag (research). --> |
| talkingHead | musetalk-mlx-swift | MuseTalk 1.5 | wrapper+core | video/PROD | ✅ | ✅ | ✅ (activation = est, phys re-baseline pending) | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-generated-frame checkpoint pre-existing in the UNet+VAE loop + pre-MP4-encode seam; cadence generate/frame; NEW MLXTalkingHeadTests target (repo had none) with CAN-1..3 green; engine pin 0.27.0. Tagged v0.9.0. --> |
| videoUpscale | mlx-seedvr2-swift (MLXSeedVR2) | SeedVR2-3B | wrapper+core | video/PROD | ✅ | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() before two-surface dispatch; per-frame (video) + ADDED per-diffusion-tile (frame refiner) and pre-forward (single-eval image path) checkpoints; cadence upsample/frame + upsample/chunk; CAN-1..3 green on both surfaces; engine pin 0.27.0. Tagged v0.6.0. BRIDGE-VID-002 fold LANDED 2026-06-30 (a45b7eb): videoUpscale is now a 2nd SURFACE on the SAME `SeedVR2UpscalePackage` as the imageUpscale row above (one model, two surfaces; loads the 3B core once). Streamed tile-refined per-frame path on frame-stream-native/NativeFrameStream. Correction to the original decision: the consolidation folded the archived `seedvr2-mlx-swift` core INTO `mlx-seedvr2-swift` (which stays as the single live home) — the WIP wrapper repo was NOT retired; it IS the home. Born-clean levers + footprint shared with the imageUpscale row. Val 🟡 = CLI NativeFrameStream round-trip green; live GPU video run + MLXEngineVideo pending. --> |
| frameInterpolate | mlx-rife-swift (MLXRIFE) | RIFE 4.25 | wrapper+core | video/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() (was validation-first); per-source-frame checkpoint pre-existing in the NativeFrameStream transform; cadence generate/frame; CAN-1..3 green; engine pin 0.27.0. Tagged v0.8.0. Consolidated repo (RIFEMLX core folded in 2026-06-18; standalone `rife-mlx-swift` core ARCHIVED read-only, local clone quarantined to `mlxengine-video/_ARCHIVED-cores-quarantine/`). The 2026-06-18 pass moved to frame-stream-native but left engine pinned 0.8.0 → bumped to 0.17.0 (tag v0.4.0). Born-clean hygiene applied though Eff stays ➖ (fp32-only single-component low-peak; BudgetAware/per-stage-evict genuinely N/A): unload()→MLX.Memory.clearCache() (was ref-drop only); honest split footprint resident 150 MB (weights ~21 MB) + peakActivation 1.5 GB pyramid (est) — the old flat fp32 1.5 GB mislabeled activation as resident. License MIT/MIT. Validated: builds @0.17.0 + offline conformance 5/5; inference path unchanged. --> |
| contentClassify | mlx-vjepa2-swift (MLXVJEPA2) | V-JEPA2 ViT-L | wrapper+core | video/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); laundering fix — frame-sampling catch (async AVAssetImageGenerator call) now rethrows CancellationError unchanged; per-sampled-frame checkpoint added; CAN-3 subSecondRuns (two monolithic ViT-L forwards, 1.7 GB < 2 GB); CAN-1..3 green; engine pin 0.27.0. Tagged v0.5.0. Consolidated repo (VJEPA2MLX core folded in 2026-06-18; standalone `vjepa2-mlx-swift` core ARCHIVED, local clone quarantined to `mlxengine-video/_ARCHIVED-cores-quarantine/`). Same as RIFE: the 2026-06-18 pass left engine pinned 0.8.0 → bumped to 0.17.0 (tag v0.4.0). Born-clean hygiene, Eff stays ➖ (fp32-only single-component; BudgetAware/evict N/A): unload()→clearCache; honest split footprint resident 1.3 GB (ViT-L ~1.2 GB) + peakActivation 1.7 GB attention (est) — flat fp32 3.0 GB baked activation into residency. License MIT/MIT. Validated: builds @0.17.0 + offline conformance 6/6; inference path unchanged. --> |

> MuseTalk `Eff: ✅` (2026-06-30, engine 0.17.0) — split + `unload()` clearCache (batch #6, non-image/video stack). Wrapper `MLXTalkingHead` over core `MuseTalk` (VAE+UNet pipeline + shared whisper-tiny + bisenet). Flat residentBytes (fp16 8 / int8 7 / int4 6.5 GB) BAKED the bs=8 per-frame activation into residency → **split** per quant: residentBytes = weights floor (VAE fp32-loaded ~0.67 GB + UNet@quant + whisper-tiny ~75 MB + bisenet ~50 MB) = **fp16 2.5 / int8 1.7 / int4 1.3 GB** (only the UNet shrinks with quant); peakActivationBytes = the bs=8 UNet+VAE per-frame transient, dtype-INDEPENDENT (co-residency premise) = **~4.5 GB across all three**. Config now conforms to `QuantConfigured`. **P2 whisper-evict skipped** (noted N/A-low-value): whisper-tiny encodes once upfront then idles, but at ~75 MB the win is marginal and evicting mid-`runTalkingHead` would complicate the per-frame loop — the headline is the split; bisenet runs per-frame so stays. `unload()` now `MLX.Memory.clearCache()`. mmap/lazy: noted — `MuseTalkWeights.load` casts per-tensor lazily (not a full eager dict copy), `eval(module)` realizes; left as-is. **No full-pipeline smoke** (video GPU-watchdog risk on this beta OS — the LTX lesson; build green, talkingHead surface stable). **FLAG:** residentBytes derived from on-disk weight sizes + peakActivationBytes from the ~7 GB Python-port bs=8 peak — BOTH smoke/derived est; in-app `phys_footprint` (R-MEM-1/admission, ~2.5–2.9× a smoke MLX-peak) re-baseline pending (MLXEngineVideo/LTXVideoTesting).

> Wan family `Eff: ⬜` deferred behind a dedicated deep-dive (more complex). **LTX is the sweep's first target + process-validation.**

## 🧠 Language & Vision-Language

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| llm | mlx-qwen-llm-swift | Qwen3.5 | wrapper | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-token cadence on both paths (mlx-swift-lm 3.31.4 tokenLoop Task.isCancelled + wrapper post-respond checkpoint — ChatSession.respond surfaces a cancel as a SILENT PARTIAL RETURN, and the miss also re-held a truncated turn into the KV-reuse session: the post-call checkpoint now guarantees a cancelled run drops the held session; structured TokenIterator checks per token); CAN-1..3 green; engine pin 0.27.0. Tagged v0.5.0. --> |
| llm | mlx-gemma-llm-swift | Gemma-3-12B-it + Gemma-4-12B-it | wrapper | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-token cadence on both paths (upstream tokenLoop + post-respond checkpoint fixing silent-partial-return absorption; structured TokenIterator per token); static manifest under the 2 GB threshold but gemma-4 int4 (2.16 GB measured) crosses it → cadence declared, not exemption; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. PUBLISHED xocialize/mlx-gemma-llm-swift @v0.2.0, engine URL from:0.19.0 (v0.1.0 first publish @0.18.0 = gemmaTerms allowlisted). MLXLLM-only (never MLXVLM — gemma3 registry-shadowing guard; guard's FIRST LIVE CATCH: the pe-e2e single-binary mix with MLXQwenLLM, 2026-07-02 → harness split into per-backbone executables). Born sweep-clean: split MEASURED int4 floor 10 GB + act ~1 GB @2k envelope (dense Gemma has NO Qwen GDN prefill-scratch blowup); QuantConfigured+FootprintConfigured+WeightPrewarming; modelDirectory reuses LTX's gemmaDirectory (zero extra download). Val ✅ 2026-07-02: RunGemmaLLM smoke/mem-bench + prompt-enhance-e2e-gemma GATE PASS 4/4 + IN-APP (LTXVideoTesting, BRIDGE-LTX-006): compact24 enhance→generate 15.02 GB (zero regression vs the ungoverned 15.04) AND enhance-with-LTX-resident via governor LRU-evict-to-admit (exceedsMemoryBudget = alone-too-big only; no app-side evict-retry needed). Purpose: govern the LTX enhancer's previously out-of-engine Gemma load. FAMILY AXIS (2026-07-04, in v0.2.0): GemmaFamily gemma3|gemma4 — gemma-4-12b-it (model_type=gemma4_unified, mlx-swift-lm ≥3.31.4 text factory Gemma4Model; sanitize strips vision_embedder). Gemma-4 weights Apache-2.0 (manifest keeps stricter gemmaTerms = conservative-correct); catalog += gemma-4 12B {4bit,8bit,bf16}; identical 12B hybrid KV geometry (verified both configs). Val ✅ 2026-07-04: RunGemmaLLM --smoke/--mem-bench --gemma4 — int4 floor MEASURED 7.11 GB (declared 8), act MEASURED 2.16 GB @2k envelope (≈4.6× analytic KV — gemma4_unified prefill scratch; declared 2.7, family-specific NOT analytic). Gemma-3 regression smoke PASS same day. Codable back-compat: legacy {size,quant} payloads decode as gemma3. MAT (2026-07-04, v0.2.0): born materialization-clean — WeightSourcing single "checkpoint" source (quant tiered by REPO ⇒ whole-snapshot matching, nothing to exclude; unpublished quants declare nothing = explicit-dir-only); missingWeightSources honors explicit modelDirectory first then store <root>/<org>/<name> (config.json probe); load() auto-materializes dir-less configs via HubClient.downloadSnapshot → WeightDownloadProgress (replaces the v0.1.0 HubClient(cache:) hub-cache path — weights materialized under the OLD hub-cache layout re-download ONCE into the store layout); prewarm resolves the store (second-cold-launch payoff). Offline MAT-1..5 green per published checkpoint (22 tests, Xcode-beta xcrun). --> |
| llm (prompt enhance) | ernie-pe-swift | ERNIE-PE (Ministral-3B) | wrapper+core | image/PROD | ✅ | ✅ | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); pre-existing per-token isCancelled seam in the vendored Mistral3 core declared generate/token; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. --> |
| embed | mlx-qwen-embedding-swift | Qwen3-Embedding-0.6B | wrapper | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); pre-existing per-text checkpoint in the batch embed loop declared as encode/chunk cadence (not long-run-implied, ~1 GB); CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. PUBLISHED xocialize/mlx-qwen-embedding-swift @v0.1.0, engine from:0.20.0 (contract 1.15.0 — `embed` introduced FOR this package; unblocks MLXCompanion semantic memory, ENGINE-NEEDS N4; vectors ride the response per the imageQualityScore non-Artifact precedent, no CanonicalOutput.vector). PATH-A REUSE, zero layer translation: checkpoint model_type=qwen3 → stock MLXLLM Qwen3Model (quantized Linears included); Qwen3ModelInner.callAsFunction already returns norm(h) = last_hidden_state — the package adds ONLY the head semantics: last-token pooling at the trailing <|endoftext|> (151643; tokenizer post-processor appends it, encode paths that don't are repaired defensively — the #1 silent-failure risk), query-side "Instruct: {task}\nQuery: {text}" (documents verbatim — asymmetric retrieval), Matryoshka truncate-BEFORE-normalize (dims 1..1024, native 1024 = hidden_size), always L2-normalized. MLXLLM-only (registry-shadowing guard: load() verifies Qwen3Model). Texts run as SEQUENTIAL single-seq forwards, cache:nil (no left-pad/mask bookkeeping; pooling exact by construction; batched-GEMM win is noise at 0.6B/consumer scale). Born sweep-clean: split MEASURED int8 floor 0.87 GB (declared 1.0) + act 1.02 GB @4k-token envelope (declared 1.3; no KV cache — full-seq forward scratch, shared across quants); QuantConfigured+FootprintConfigured+WeightPrewarming. Born materialization-clean: WeightSourcing single "checkpoint" source, quant tiered by REPO (8bit default 633 MB / 4bit-DWQ 335 MB; -mxfp8 out-of-grammar, uncataloged; no published bf16 ⇒ explicit-dir-only); offline MAT-1..5 green per checkpoint (15 tests). Val ✅ 2026-07-04: PARITY vs sentence-transformers fp32 reference (embed-oracle goldens) — token ids EXACT on every role, per-vector cosine ≈0.9996 (gate ≥0.99, int8-vs-fp32), Matryoshka-256 ≈0.9996, retrieval ranking match (max |Δscore| 0.0055), instruct-vs-plain asymmetry matches oracle ±0.003; RunQwenEmbedding --smoke/--dump/--mem-bench + --engine-e2e through the REAL MLXServeEngine (register→prewarm 0.6 GB→prepare 0.3s store-resolved dir-less→run→evict): order-preserving batch cos 1.000000 vs single-text, asym cos 0.7876, m256 via engine, modelID provenance stamped. int4-DWQ published but not yet live-validated (floor est 0.7 GB — re-measure at first consumer). Consumer contract reminder: persist modelID+dimension beside stored vectors; an index must never mix models/dims. -->
| imageAnalysis | qwen25vl-mlx-swift | Qwen2.5-VL-3B | wrapper+core | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); NEW per-token checkpoint in the greedy decode loop + post-encode/pre-decode stage seam in Qwen25VLPipeline.generate; CAN-1..3 green (bf16 3.0 GB implies long runs); engine pin 0.27.0. Tagged v0.3.0. WeightSourcing retrofit sweep 2026-07-09 (pre-MAT fleet): single self-contained "main" snapshot per quant tier (defaultRepo(for:) → mlx-community/Qwen2.5-VL-3B-Instruct-{bf16,4bit}; explicit repo wins); missingWeightSources honors explicit snapshotDirectory first, then the store layout (probe = config/preprocessor/tokenizer/index + ≥1 safetensors shard — bare dir is NOT enough, the materializer pre-creates it); load() materializes via WeightMaterializer (HubClient snapshot) w/ .downloading progress; prewarm store-resolved (index + dir). BONUS FIX: the tokenizer now loads from the snapshot's own files (AutoTokenizer modelFolder) instead of fetching the stock Qwen repo into HubApi's PRIVATE cache at load() — a materialized snapshot is now offline-sufficient; gated TokenizerLocalLoadTests pins the vision special-token ids (151655/151652/151645/151643) on the local path. snapshotDirectory stays the dev escape hatch (never touches the network). MAT-1..5 green per quant tier (MLXQwen25VLTests). Engine pin 0.15.0→0.23.0, + swift-huggingface. Tagged v0.2.0. --> |
| imageAnalysis | qwen3vl-mlx-swift | Qwen3-VL | core (wrapper pending) | think/WIP | 🧪 | ⬜ | ⬜ | |

> mlx-qwen-llm-swift `Eff: ✅` (2026-06-30, engine 0.15.0) — first **autoregressive** sweep target: the transient is the **context-scaled** activation, not a fixed peak. P1 split: flat `residentBytes = onDisk + 600 MB` (KV-cache-blind) → `FootprintConfigured` split `residentBytesHint = onDiskBytes` (weights floor) + `peakActivationBytesHint = peakActivationBytes` declared at a **documented 2048-token context envelope**. **Measurement surprise (the interesting bit):** Qwen3.5 is a HYBRID linear/full-attention model — only 1-in-4 layers is softmax attention with a context-growing `KVCacheSimple`; the rest are GatedDeltaNet (linear) with a fixed `MambaCache`. So the analytic KV-cache (`QwenModel.kvCacheBytes`, verified **bit-exact** at 12 288 B/token for 0.8B) is TINY (96 MB @8192). But the measured transient is **prefill-scratch-dominated** (the GatedDeltaNet chunked-scan over the prompt), ~20× the KV-cache and ~linear in sequence length: 0.8B-8bit measured 427 MB @~340 tok · 2.1 GB @~2k · 4.2 GB @~4k · 7.0 GB @~8k. Declared peak is the **measured** prefill-scratch at the 2048 envelope (~2.2 GB for 0.8B), width-scaled by `hidden_size` for 4B/9B (only 0.8B measured). Classic "flat footprints UNDER-declare." P2 (per-stage evict) N/A (single model). P3 (mmap) verified — floor ≈ on-disk weight bytes (loads via mlx-swift-lm safetensors). P4 (BudgetAware) the LLM-specific lever = cap context to fit KV-cache into budget; **documented + deferred** (KV-cache is small; silently shrinking context is a correctness surprise). Engine pin `from: 0.3.0 → 0.15.0`, no API drift.

> mlx-qwen-llm-swift **KV-cache reuse** (2026-07-05, **v0.3.0** — the package-level tail of MLXCompanion ENGINE-NEEDS N5; engine 0.21.0 shipped the pool-policy side separately). `run()` now holds ONE `ChatSession` across calls: exact-transcript fingerprint (`SessionReuse.decision`, pure + unit-tested) — hit ⇔ incoming messages == held transcript + exactly one new user turn AND same system prompt; on hit only the new turn is prefilled (`TokenIterator` does no prefix dedupe), on ANY mismatch it rebuilds fresh (byte-identical pre-reuse behavior). System prompt seeds `history[0]` (NOT `instructions:`, which re-templates every respond); `generateParameters`/`additionalContext` refresh per call so sampling/mode changes don't rebuild; held session drops on `unload()`, mismatch, or mid-generation throw. **Triage note:** the retained KV/GDN state is *intentional* active-memory retention (+143 MB measured after a 4-turn chat) — "active climbing across turns" while chatting is not a leak. Observability: `kvReuseHits/kvReuseMisses` + `Logger(subsystem: MLXQwenLLM, category: kv-reuse)`; `kvCacheReuseEnabled` escape hatch. Gates (`RunQwenLLM --kv-reuse/--mem-bench`, 0.8B-8bit): counters exact, held-leg recall through the reused cache ✅, post-first-turn latency **0.29s → 0.06s (5.3×, flat)** under a ~5k-token persona, mismatch fallback byte-matches a fresh run. **Known + accepted:** held-vs-fresh temp-0 byte equality is NOT guaranteed *by upstream design* (the cached generation prompt carries the empty `<think>` block a re-template omits) — near-tie argmax flips are possible (1-of-4 turns observed, both outputs valid); the consumer must echo the package's replies **verbatim** into subsequent requests or every turn is a (correct, slower) rebuild. Floor/envelope declarations unchanged (re-measured on the ~1.03 MB/token line).

> mlx-qwen-llm-swift + mlx-gemma-llm-swift **structured output** (2026-07-05, qwen **v0.4.0** / gemma **v0.3.0** — MLXCompanion ENGINE-NEEDS **N6**; contract **1.16.0** `LLMRequest.responseFormat`, engine v0.22.0). Grammar-constrained decoding: `.json(container: .any/.object/.array)` masks every token that can't extend a valid JSON prefix and force-stops after one complete top-level value; `.jsonSchema` = V1 best-effort (`.json` syntax + container inferred from the schema root). Runtime = shared **`mlx-constrained-decoding-swift` v0.1.0** (think/PROD): byte-level JSON FSM + once-per-residency vocab classification (Qwen byte-level-BPE ~151k / Gemma SentencePiece ~262k — string-safe bitset fast path + first-byte buckets; per-step full-vocab scans don't scale) + MLXLMCommon `LogitProcessor`. **Wiring:** structured requests BYPASS `ChatSession` (mlx-swift-lm 3.31.x has no processor seam through `GenerateParameters`) — template via `UserInput`/`prepare`, drive `TokenIterator` (custom-processor init) with a FRESH cache; the v0.3.0 held-session KV-reuse path is untouched (companion structured calls are one-shot side calls). Qwen structured calls default `enable_thinking:false` (`<think>` is masked anyway; explicit `.thinking` still wins). Honest `finishReason`: `.stop` only when the grammar completed; maxTokens-truncation → `.length`. Dead ends force EOS (never silently un-constrain). Manifests advertise `supportsStructuredOutput` (C11); packages without the runtime must throw `PackageError.unsupportedRequestFeature` (contract). **Gates:** `RunQwenLLM --structured` (0.8B-8bit, 3 companion shapes × 20 @ temp 0.7): constrained strict-parse **60/60** vs baseline scrape 57/60 (parseFacts 17/20 — the motivating silent-drop), masking **1.04 ms/step** /1928 steps (decode ≈5–15 ms → negligible); `RunGemmaLLM --structured` (gemma-3-12B-4bit): **15/15**, 1.07 ms/step (proves the SentencePiece path). Vocab classification ≈0.3 s once per residency, dropped on `unload()`. Kit offline tests: 14 (FSM malformed-prefix rejection, container hints, stop detection, dead-end forcing — no MLX kernel in the test product).

> qwen25vl-mlx-swift `Eff: ✅` (2026-06-30, engine 0.15.0) — the **VLM** sweep target, which combines two levers: a vision tower + an autoregressive LM. **P2 (vision-tower evict):** the ViT encodes the image once then is idle through the LM decode loop, so the core pipeline now holds `vision` as a rebuildable `private(set) var` — `load()` captures a `visionBuilder` closure over the (mmap-backed) ViT weights; `generate()` runs `ensureVision()` → encode → `eval(imageFeatures)` → `evictVision()` (`vision = nil` + `Memory.clearCache()`) before the LM prefill+decode, rebuilding lazily on the next call. Stays synchronous (the builder closure isn't async), so no `#isolation` hop was needed. Modest (the 3B LM ≫ the ViT) but real. **P1 split (MEASURED via gated `MemoryReportTests`, M5 Max, envelope: image 800×557 ≈0.45 MP → ~580 vision tokens × maxTokens 256, post-evict):** bf16 resident floor **7.51 GB**, worst peak **9.98 GB** → transient **2.47 GB**; int4 floor **3.07 GB**, peak **4.04 GB** → transient **0.97 GB**. Declared `QuantFootprint(.bf16, resident 7.7 GB, peakActivation 3.0 GB)` and `(.int4, resident 3.2 GB, peakActivation 1.2 GB)` (+`QuantConfigured`; single size so no `FootprintConfigured`), replacing the **flat** 9.6 / 4.5 GB — the old flat number was the *peak*, baking the activation into residency. Per the Qwen-LLM lesson the transient is **measured per quant** (it does NOT width-scale across quants: int4's 0.97 GB ≠ a scale of bf16's 2.47 GB, since the quantized LM's prefill scratch is smaller); it's the image-token-inflated LM prefill/KV-cache scratch and scales with the (image-res × maxTokens) envelope. P3 (mmap): loads via `MLX.loadArrays` (lazy safetensors) — note only. P4 (BudgetAware) deferred (quant is config-chosen). Engine pin `from: 0.3.0 → 0.15.0`, no API drift (the `imageAnalysis` surface + `ModelPackage` are unchanged). Smoke (`OracleSmokeTests`, chart-read "29") still green after the evict refactor.

## 🧱 Shared foundation (not capability providers)

| Package | Provides | Role | Home | Avail |
|---|---|---|---|---|
| mlx-engine-swift | the engine (MLXToolKit/MLXServeCore/UI/retrieval) + **MLXEngineTestKit** (opt-in category testing harness) | engine | MLXEngine/ | ✅ (0.22.0, contract 1.16.0) |
| mlx-swift-lm | LLM/VLM building blocks | shared | video/LTX_DEV | ✅ |
| mlx-constrained-decoding-swift | JSON grammar-constrained decoding (`responseFormat` runtime: byte-level JSON FSM + vocab classification + `LogitProcessor`) | shared | think/PROD | ✅ (v0.1.0) |
| flux2-vae-mlx-swift | FLUX.2 VAE decoder | shared | image/PROD | ✅ |
| wan-core-mlx-swift | shared Wan-family video core | shared | video/WAN_DEV | 🔬 | <!-- CAN sweep 2026-07-09: per-chunk Task.isCancelled bails in decodeStreaming / decodeStreaming22 / encodeStreaming (bail AFTER first append — empty concat would crash); per-tile bail in the vae22 halo-tiler deliberately NOT added (mid-tile break mis-shapes the strip concat); commit b791b62 pushed; consumers inherit via local-path pin. --> |

---

## Efficiency sweep status (the 1.14 program)

The **Eff** column above IS the sweep tracker (one source of truth — don't duplicate it elsewhere).
Order: **LTX first** → most-consumed (image caps, Qwen LLM/TTS) → optimizer family (BiRefNet/upscale/
NAFNet/SigLIP2) → Wan last (dedicated deep-dive). Per-package work orders are the
`EFFICIENCY-ADOPTION.md` briefs in each package repo (template: `ltx-2-mlx-swift/EFFICIENCY-ADOPTION.md`).
Plan + rationale: `EngineeringDocs/MLXEngineDocs/ENGINE-ROADMAP.md` → "Library efficiency program".

## Cross-references
- Capability schemas: `Sources/MLXToolKit/Capability.swift` + `capability-contract.md`.
- App-side consumption of these models: the `mlxengine-implementation` skill.
- Bringing a new model in (and adding its row here): the `mlx-swift-integration` skill.
