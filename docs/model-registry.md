# MLXEngine Model Registry

The **living** registry of every model package the engine can serve — both **availability awareness**
(what exists, where, published) and **state** (validated? efficiency-adopted? engine version). It
supersedes the generated `mlxengine-model-catalog` snapshot, which went stale because nothing kept it
current.

> **This file is maintained as a step of integration, not regenerated.** When you bring a package up,
> publish a tag, validate it in-app, or run its efficiency-adoption sweep, **update its row here in the
> same change** (see the `mlx-swift-integration` skill, Stage 2 → "Register in the model registry").
> A row that drifts from reality is a bug. The capability contract source of truth stays
> `Sources/MLXToolKit/Capability.swift`; this is the *provider* index against it.

**Columns**
- **Package / Model** — the `MLX<Name>` wrapper (or core) + the underlying model.
- **Role** — `wrapper+core` (single folded repo) · `wrapper`/`core` (split) · `shared` · `engine`.
- **Home** — bucket path under `~/Development` (`PROD` = published+tagged+URL-consumed · `WIP` = active · research = WAN_DEV/LTX_DEV clusters).
- **Avail** — ✅ published (URL/tag) · 🧪 WIP/local · 🔬 research.
- **Val** — in-app live-validation: ✅ validated · 🟡 partial/CLI-only · ⬜ not yet.
- **Eff** — 1.14 efficiency adoption (split footprint + mmap + per-stage evict + BudgetAware):
  ✅ adopted · 🔵 in progress · 🟡 brief written · ⬜ not started · ➖ n/a (trivial/single-component low-peak).
- **Eng** — the engine **contract version the package was validated/adopted against** (its resolved build), blank = backfill. NOT the SPM `from:` floor in `Package.swift` — that floor is a resolve-**forward** minimum (`from: "0.3.0"` resolves to the latest 0.x, i.e. 0.17.0), so a low floor is usually fine, not drift. Only bump a floor when the package's code actually needs a newer contract API (e.g. `peakActivationBytes`/`QuantConfigured`/`BudgetAware` ⇒ needs ≥0.14) and you're touching the package anyway; a floor-only bump across the library is churn we deliberately skip (see the Kokoro note).

> **Reading the C7/C8 notes below (changed 2026-07-26, contract 1.28.0).** Row notes recording a
> license verdict — "rejectedWeight under `.permissiveOnly`", "admitted via
> `.permissiveOrAcknowledged`", "NC weights" — are accurate as **classifications** and remain the
> record of the review. They no longer mean "the engine refuses to load it": C7/C8 are declaration
> requirements, and a default engine registers such a package while recording a `LicenseAdvisory`.
> A host that must refuse passes `licenseEnforcement: .blocking`. So these notes now answer "what is
> this license and is it shippable?" — a product/legal question — not "will it run?".

_Seeded 2026-06-30. Val/Eff/Eng are best-effort at seed time — **backfill per package as it's revisited.**_

---

## 🔊 Audio

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| tts | mlx-kokoro-tts-swift | Kokoro-82M | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.36.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); non-autoregressive single-chunk forward (core pre/post-forward checks + wrapper post-synthesis checkpoint), cadence honestly coarse generate/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged v0.3.0. Eff n/a: single tiny model (82M, flat bf16 ~500 MB), negligible transient; engine bump not worth the churn. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source Kokoro-82M-bf16 incl. voices/*.safetensors; probe includes the configured defaultVoice embedding; modelDirectory escape hatch added; MAT-1..5 green; load() now materializes into the ModelStore layout + fromModelDirectory (leaves the core's legacy mlx-audio/<org>_<name> cache — one-time ~300 MB re-download on updated installs). Tagged v0.2.0. **C14 2026-07-25:** C14 INF gate 2026-07-25 (v0.4.0): posture .moduleGraph. Kokoro is the fleet's DROPOUT case, not a BatchNorm case — MLXNN.Dropout is identity ONLY at p==0 or !training, and Albert builds Dropout(p: hiddenDropoutProb/attentionProbsDropoutProb), config default 0.1. The pre-existing train(false) in KokoroModel.fromModelDirectory (the single choke point) was therefore load-bearing but undocumented; now documented + asserted (testDropoutIsOnlyIdentityInInferenceMode pins the MLXNN behavior). NO behavior change. Also de-staled the MAT store-layout fixture (pre-MS-1 <root>/org/name literal vs contract 1.22.0 models--org--name) — had been failing against the shipped engine. 10/10 green. Engine pin 0.27.0->0.36.0. -->
| tts | mlx-qwen3-tts-swift | Qwen3-TTS | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09 (fleet sweep, the gate's motivating package): entry checkpoint made the FIRST act of run() (was notLoaded-first — the CAN-1 signature failure); mid-run cadence = the core's pre-existing E3 per-frame Talker bails (all 4 loops: synthesize/stream/batch/ICL GenerationLoop); CAN-1..3 green (cadence of record: generate/frame); engine pin 0.23.0→0.27.0. Tagged v0.5.0. split: 1.7B-8bit weights 2.65 GB + measured talker transient ~4.0 GB; FootprintConfigured (variant×size×quant); P2 N/A (talker+codePredictor interleave per frame). WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): QUANT-VARIANT declaration (checkpoint variant×size×quant computes the model repo) + shared Qwen/Qwen3-TTS-Tokenizer-12Hz source + vocab/merges/tokenizer_config sidecars on the model source; MAT-1..5 green across the full 25-checkpoint published catalog; kept the core HuggingFaceDownloader (already store-layout), delta = missing-set + WeightDownloadProgress forwarding + model/tokenizerDirectory escape hatches. Tagged v0.4.0. -->
| tts | mlx-voxcpm2-tts-swift | VoxCPM2 | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-patch Task.isCancelled bail added to the core AR loop (mlx-voxcpm-swift v0.3.0 — bail AFTER append so partial decode stays well-formed) + wrapper post-call rethrow; CAN-3 generate/frame; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. split: weights floor 9.3 GB + measured transient 4.0 GB (flat 11 GB under-declared the ~11.9 GB peak); QuantConfigured; P2 N/A (TSLM/RALM/LocDiT interleave per patch). WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source VoxCPM2-bf16, explicit-dir escape hatch added (modelDirectory), MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress, prewarm store-resolved. Tagged v0.3.0. -->
| tts | mlx-indextts2-swift | IndexTTS2 | wrapper+core | audio/PROD | ✅ (v0.3.0) | ✅ | ✅ | 0.36.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-mel-token bail in the GPT AR loop + inter-stage cancelCheck, anti-laundering fix so cancel beats emptyGeneration (a mid-loop bail can CREATE a laundering path — empty-output guards downstream); CAN-3 generate/token + decode/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. Stage 2 2026-07-09. C7 = LicenseRef-Index-Model NonCommercial (.permissiveOrAcknowledged only, gate live-verified); specialties emotionControl+durationControl (E12 first realization — metaData emotion/emoAlpha/targetDuration/speechRate/seed; targetDuration EXACT 3.00s/3.0s). Born clean: split fp16 5.0 GB + measured in-app transient 5.0 GB (phys floor 4.76, run peak 9.61, post-evict 0.37); QuantConfigured fp16|int8|int4 (in-memory GPT-backbone quant); BudgetAware fp16→int8 drop; MAT-1..5 + live first-run materialization (3 sources → container store, 321 s incl. ~6.6 GB download). PUBLISHED 2026-07-09: pkg v0.1.1 + mlx-audio-dsp v0.1.0 (dep, PROD-promoted audio/PROD same day), engine v0.23.0 (LicenseRef-Index-Model + specialties), weights consolidated mlx-community/IndexTTS-2-fp16 (all 3 WeightSource roles). Remaining debt: fp16 w2v-BERT re-publish (−1.1 GB, needs p3w2v gate re-run). **C14 2026-07-25:** C14 INF gate 2026-07-25 (v0.3.0): posture .moduleGraph. CHOKE-POINT REFACTOR — train(false) moved from the CAMPPlus construction site into IndexTTS2Generator.loadComponent; it had been applied per call site across FOUR places (fix-by-repetition, where a fifth is silently wrong). All 7 components load through the one function. CAMPPlus is the BatchNorm carrier (batchnorm/bn/bn1/bn2 across TDNN/CAMDense/FSMN) and its 192-d embedding conditions the whole clone, so per-clip batch stats would have drifted the speaker identity run to run. loadComponent private->internal so the gate exercises the choke point directly (the indextts2-gate parity harness loads inline and keeps its own calls). Tests need no download — they round-trip a component's own parameters through a temp safetensors with identity sanitize, exercising the real 0-missing/0-unused key contract. Also de-staled the MAT store-layout fixture (all 3 dirs now derive from ModelStore.directory(for:) on their own repo property). 23/23 green. Engine pin 0.28.1->0.36.0. -->
| tts | mlx-gepard-swift | Gepard-1.0 | wrapper+core | audio/PROD | ✅ (v0.3.0) | ✅ | ✅ | 0.33.0 | <!-- STAGE 2 COMPLETE 2026-07-10 (engine-wrap). 2-module split GepardCore core + MLXGepardTTS wrapper (IndexTTS2 template). Stage-1 core parity green --p2..--p9 (CodecOps/TextRep bit-exact, Qwen3.5 full-attn backbone prefill rel 4-6e-4, audio-interface+heads rel 1e-7, Q-Former ladder 4-6e-4, greedy rollout integer-exact mod 1 fp-tie, NanoCodec decoder max|Δ| 7e-6/SI-SNR 104dB, encoder ≥99% int-exact). NEW P10 tokenizer gate: text→cond_ids INTEGER-EXACT across 8 texts + 3 safetensors cross-checks (Qwen3.5 tokenizer.json via swift-transformers PreTrainedTokenizer + TextRepeater SOT 248073/EOT 248074/SOS 248070, repetition 16/13/8) — closed the last run() functional gap. C7 = two PERMISSIVE weight layers: LM Apache-2.0 + codec LicenseRef-NVIDIA-Open-Model (declared the more-restrictive NVIDIA-OML as weightLicense; on permissiveAllowlist since engine v0.28.0) ⇒ SHIPPABLE under default .permissiveOnly (NOT eval-gated). C8 port Apache-2.0. Specialties voiceClone + realtimeStreaming + companion(0.8) (no E12 emotion/duration lane; voiceClone/realtimeStreaming are string-literal terms — register in engine Specialty.swift as a follow-up). WeightSourcing 2 sources: main=nineninesix/gepard-1.0 (public: model.safetensors+config+tokenizer.json+tokenizer_config.json) + codec=mlx-community/nemo-nano-codec-22khz-1.89kbps-21.5fps (PUBLISHED 2026-07-10 to mlx-community: model.safetensors + NVIDIA OMLA PDF + NOTICE §3.1 + model card). Offline MAT-1..5 + CAN-1..3 + manifest C0-C13 XCTest suite GREEN (11 tests). Born sweep-clean: MEASURED split (gepard-gates --validate headless engine run() harness, M5 Max) resident floor 1060 MB MLX-active/1157 MB phys + peak activation ~3.8 GB (whole-utterance NanoCodec decode dominates — V2 frame-streaming decode shrinks it; app sentence-chunks) → QuantFootprint(.bf16, resident 1.2 GB, peak 4.0 GB); mmap/lazy bf16 LM + fp32 codec; unload→clearCache. Val (engine-path, quantified): load 0.68 s, TTFA 17 ms, gen-RTF 0.24 (4.2× realtime), −25 dBFS valid cloned speech (12.8 s paragraph, 64% active frames), full GepardPackage load→run()→decode→WAV. Onset text-CFG knob (metaData.cfgScale) wired: short-word matrix (deterministic greedy, one run/condition) — CFG-off all clean-stop; CFG-on w2.6 rescues One./Hi. but "Okay!" collapses to 1 frame/−78 dBFS (consistent with tech-report §5.3 <50% success) ⇒ CFG default-off, companion policy app-side. IN-APP GUI VALIDATION CONFIRMED 2026-07-10 (MLXEngineAudio GepardValidationRunner, the real MLXServeEngine register→prepare→run→evict path): admitted under DEFAULT .permissiveOnly (no acknowledgement flow); cold prepare 340.5 s (first-run full weight download of both sources); engine-charged 1.20 GB / phys 1.26 GB = EXACT manifest match; post-evict phys 0.69 GB (clean teardown); all 7 cases match the headless --validate numbers (clone-paragraph valid speech, short-word matrix CFG-off clean / CFG-on One.&Hi. rescued & Okay! the documented 1-frame collapse). Follow-up = Specialty.voiceClone/.realtimeStreaming registered in engine 0.28.1 (chip), package manifest uses the constants. **v0.2.0 2026-07-24 — stopThreshold knob + 1.24 adoption + Gepard Demo consumer validation (Eng 0.28.1→0.32.0).** (1) NEW metaData knob `stopThreshold` (double, default 0.5, clamped 0.05–0.99) closes an oracle-parity gap: runner.py always exposed stop_threshold; the port hard-coded sigmoid>0.5. Motivating find: the stop head is PREFIX-CONDITIONED — 2 of 4 bundled Roxy reference clips cross 0.5 at a mid-utterance sentence pause and truncate multi-sentence text (worst case frame-2 stop ⇒ 0.1 s from an ~11 s, 3-sentence input; text-sensitive — straight-vs-curly apostrophes changed the stop frame). 0.9 rescues: 4/4 preset audit on the 3-sentence input all complete (9.8–11.0 s), endings clean (stop head still fires naturally, no maxFrames runaway). Package default STAYS 0.5 (oracle parity); the demo app defaults 0.9 app-side. (2) ENGINE-EXECUTED MATERIALIZATION ADOPTED (engine 0.32.0 / contract 1.24.0): package-local WeightMaterializer deleted, load() keeps the missing-sources guard as the offline backstop; swift-huggingface dropped; engine floor 0.28.1→0.32.0; offline MAT/CAN/manifest suite 12/12 green. (3) IN-APP VAL REFRESH via the NEW consumer **Gepard Demo** (mlxengine-audio/PROD/gepard/"Gepard Demo" — HF-Space-parity UI, bundled-reference presets, golden-path consumer: store-before-register /Volumes/Satechi/Models, needsDownload consent, ModelStateView, detached-task runs, MLXEngineTestKit harness per MFD-2): live [MAT] on a THROWAWAY engine + empty temp store — `[MAT] pkg=gepard bytes=1550640077 (main=1131411576,codec=419228501) secs=31.2 downloadPhase=yes events=107 marker=yes peak=4874309568` — with the package materializer deleted, prepare() succeeding IS the engine-executor live proof (the z-image E2E pattern, via bench instead of test). [RUN] lines (Release, M5 Max, ~9-11 s utterances): rtf 0.32–0.35 end-to-end (~3× realtime incl. whole-utterance codec decode; headline gen-RTF 0.24 unchanged) phys 4.20–4.77 GB post-run — inside the declared 1.2 GB resident + 4.0 GB peak envelope. Evidence log <modelsRoot>/gepard-demo.log. CAVEAT (GPD-2, mlxengine-implementation field-issues): Debug -Onone misreads the AR loop ~4× (RTF 1.10) — quote Release numbers only. **v0.3.0 2026-07-24 — FIRST StreamEmitting REALIZATION (engine contract 1.25.0 / v0.33.0, the N2 streaming plane).** Gepard is the motivating first provider of the new COMMON streaming surface: `TTSStreamChunk` + `StreamEmitting.runStream(_:emit:)` + `MLXServeEngine.stream()`→`TTSStreamHandle` (non-requeueable; `EngineError.streamPreempted`; abandoned stream cancels the run) + STR-1..7 gate + `StreamingBench`. Realization: `GepardDecoder.rollout` per-committed-frame `onFrameCodes` seam + `GepardModel.synthesizeStreaming` windowed incremental NanoCodec decode — EXACT by causality (left receptive field computed from the LOADED kernels = 26 frames; prefix-stability probe: equal-shape decodes bit-exact). Chunk cadence 6-then-12 frames (first chunk ≈280 ms audio). Live gate (gepard-gates --stream, M5 Max): `[STR] pkg=gepard ttfa=102ms chunks=24 cadence=239/257ms audio=12.8s wall=5.6s rtf=0.44 deltaSNR=51.1dB maxDelta=0.0133 peakStream=2350MB peakBatch=4856MB seq=yes parity=yes cancel=yes`. vs-batch parity is Metal SHAPE-DISPATCH fp noise, not causality (different sequence lengths pick different conv kernels; ~1e-3 amplified through the 40-layer stack; Δ-SNR 51 dB = inaudible) — the STR-5 documented tolerance escape. BONUS: the windowed decode BOUNDS the decode transient (2350 vs 4856 MB) — the declared V2 frame-streaming-decode item, landed on the streaming path. metaData gains streamFirstChunkFrames/streamChunkFrames/streamContextFrames (diagnostic). Offline suite 16/16 (STR-1..3 + receptive-field arithmetic). Demo-app consumer validation (Gepard Demo on the published graph engine 0.33.0 + gepard 0.3.0 + tts-orchestrator-kit 0.1.0): AVAudioEngine gapless live playback; `[RUN] mode=stream` lines — warm ttfa=143ms single-sentence; LONG-FORM via TTSOrchestratorKit TextChunker → sequential engine.stream() per sentence: `segments=6 chunks=62 ttfa=138ms secs=10.2 audio=32.6s rtf=0.31` (32.6 s take, first audio 138 ms). ENHANCEMENTS.md E2 🔴→🟢. Second-adopter candidate: Qwen3-TTS (core generateStream maps 1:1). plan=mlxengine-audio/Docs/Gepard-Swift-Port-Plan.md. -->
| tts | mlx-audio8-tts-swift | Audio8-TTS-Preview-0.6b | wrapper+core | audio/WIP | ✅ | ✅ | ✅ | 0.40.1 | <!-- Stage 1+2 2026-07-30, born clean (first fleet package with no retrofit debt: split footprint, MAT, CAN, C14 all authored at integration). Tier-2 port of an Apache-2.0 DualAR TTS (Fish-S2-Pro lineage): 24-layer slow AR emits one semantic token per 46 ms frame, 4-layer fast AR emits that frame's 10 codebooks; bundled 44.1 kHz codec (DAC-style enc/dec + split semantic/residual RVQ + windowed transformer pre/post). PARITY IS EXACT, not approximate: greedy decode reproduces the PyTorch reference 102/102 frames token-exact and codec encode is 100% code-exact (S2 gate) — the Python-MLX rung (upstreamed as mlx-audio#867) hit the same, so PyTorch->Python-MLX->Swift is exact end to end. Rope tables max_abs 0.0. C7/C8 both Apache-2.0 (single weight layer — LM+codec ship in one upstream repo), so it admits under default .permissiveOnly with no ack flow. Specialty: voiceClone ONLY — deliberately NOT realtimeStreaming (whole-utterance decode) and NOT emotion/durationControl (no native control plane). Its real differentiator, 11-language multilingual reach, has NO registered Specialty term — stated in the surface summary rather than inventing vocabulary; a `multilingual` term is the open question for the engine vocabulary. MEASURED, twice. v0.1.0 declared transient 5.00 GB from ONE 9.2 s utterance (4413 MB). v0.1.1 raised it to 7.20 GB (18-run corpus sweep, 6352 MB at 15.7 s). v0.1.2 raised it again to **9.50 GB** once the transient was fitted as LINEAR in frames across a 50x range (5 points, +-4%): `transient_MB ~= 1824 + 14.2 * frames`. The declaration must cover the longest run the package permits BY DEFAULT -- maxFrames=512 => 8.87 GB. All three passes under-declared for the SAME reason: the sample never reached the cap. LESSON: when activation scales with an input dimension, don't sample it -- MODEL it, and declare at the parameter cap the package itself allows. A caller raising maxFrames opts out (2048 => ~30 GB), which is inherent to whole-utterance decoding. Resident 2434 MB -> declared 2.60 GB; RTF: median 0.94 across 18 runs (matches the CLI gate 0.96 -- an apparent gap was warm-up + a single short sample; first-run RTF 1.80 vs steady 1.11). RTF is LENGTH-DEPENDENT: short ~0.9 s utterances run 1.05-1.64 (slower than realtime, fixed per-request cost dominates), >=5 s runs 0.85-0.94. Output level tracks the reference clip -- the iris corpus voice peaks at -0.3 dBFS (clipping) while clara sits at -12; inherited from the clip, not the port. Transient is utterance-length-driven (1623 MB short line vs 4413 MB @ 9.2 s) because the codec decodes the whole utterance in one conv-stack pass — the same pre-streaming shape Gepard had, and the obvious target for a windowed decode that would also unlock StreamEmitting. unload() returns MLX active AND cache to 0 MB (verified), residual phys is allocator retention not a package leak. C14 posture .moduleGraph: Audio8 is the fleet's NEITHER case — no BatchNorm, no Dropout (LM is Linear/Embedding + hand-rolled RMSNorm; codec is a functional port with no Module). train(false) at the Audio8TTS.load choke point is hygiene, and the suite ASSERTS the no-training-sensitive-module claim so adding one later fails a test. Swift-port lesson recorded: MLX-Swift subscripts need EVERY axis named — a short [.newAxis, ...] list silently no-ops instead of inserting, producing a downstream broadcast error that points at the consumer (use expandedDimensions). NAX split-K bug N/A here (max FFN K=4864 < 10240). LONG-FORM: TTSOrchestratorKit chunking on a 48 s passage cut peak activation 16.19 -> 5.97 GB (63%) AND ran FASTER (RTF 1.16 vs 1.33) -- the giant single decode is memory-bound, so chunking is not a memory-for-speed trade here. STREAMING **BUILT v0.2.0**: windowed decode, StreamEmitting, .realtimeStreaming. NOT at Gepard's seam -- post_module is 8 stacked layers of 128-wide causal attention so its receptive field COMPOUNDS to 1016 frames (47 s), making a whole-stack window non-viable; the memory is in the conv stack below it, which has an 11-frame field. Split: post_module whole-utterance, window only the expensive half. Bit-identical proven (--s5, max_abs exactly 0.0; measured min context 10 vs derived 11 = sufficient w/ slack; negative control drifts 1.8e-1). SEPARATE chunk floor of 48 frames that is NOT a context effect -- raising context 11->128 changes nothing because MLX picks different conv kernels for small inputs (<=1.4e-3, ~-57 dBFS). Measured: peak activation 5423 -> 3482 MB (-36%), TTFA 2.41 s (first cut was 9.51 s of 9.90 s -- decode now INTERLEAVES with the rollout, legitimate because post_module is causal over prefixes). **v0.2.1 ENDS THE SAGA: activation is now FLAT at 3482 MB -> declared 4.20 GB.** generate() routes through the windowed decode (bit-identical, --s5), so the transient no longer grows with utterance length: 3482 MB at 64, 128, 224 AND 1035 frames, where batch previously climbed 3457 -> 4993 MB and hit ~16 GB. maxFrames now affects duration only, not memory. The declaration was wrong three times (5.00/7.20/9.50 GB) for one reason -- a sample cannot bound an envelope that grows with input; the fix was removing the growth, not sampling harder. **v0.2.2 fixes a correctness regression v0.2.1 shipped**: the interleaved path windowed at the CODES level and re-ran post_module per window -- wrong twice (1016-frame receptive field; absolute RoPE re-phases a window as 0..k), and generate() had been routed through it so BATCH inherited it. Output was plausible audio of the right length. Neither gate caught it: --s5 exercises a different function (decodeStreaming, which was correct), and --stream compared the aggregate by BYTE COUNT -- an approximate decode yields exactly as many samples as an exact one. --stream now compares CONTENT and that caught it. LESSON: a gate that compares size cannot fail on a bug that preserves size. MEASURED COST of the bounded envelope, two 18-run app sweeps: median RTF 0.94 -> 1.23 for max transient 6.20 -> 3.43 GB. Repos: github.com/xocialize/mlx-audio8-tts-swift; weights mlx-community/Audio8-TTS-Preview-0.6b-bf16. WIP->PROD pending app-harness validation. -->
| audioSeparation | mlx-demucs-swift | HTDemucs v4 | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); SwiftDemucs core bails per 30 s chunk (demucs-mlx-swift v0.1.1 — core checkCancelled() gained the Task lane; it previously only read its own cancel() flag), CancellationError rethrown unchanged + mlxInferenceFailed wrap now passes it through; CAN-1..3 green (cadence generate/chunk, long-run via 2.2 GB peak); engine pin 0.27.0. Tagged v0.3.0. split: weights floor ~0.3 GB (84 MB fp16 on disk) + 30 s-chunk transient ~2.2 GB (flat 2.5 GB baked it in); QuantConfigured (fp16); P2 N/A (single VocalSeparator); unload clearCache. activation = smoke est, phys re-baseline pending. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source htdemucs-ft-vocals-mlx (one safetensors), modelDirectory escape hatch added, MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress. Tagged v0.2.0. -->
| audioSeparation | mlx-mel-roformer-swift | Mel-Band-RoFormer | wrapper+core | audio/PROD | ✅ | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); SwiftRoFormer core bails per 8 s chunk (mel-roformer-mlx-swift v0.1.2 Task lane); CAN-1..3 green (cadence generate/chunk declared despite short-run envelope — exemption not credible for minutes of chunked compute); engine pin 0.27.0. Tagged v0.3.0. split per variant: kimVocal2 bf16 0.7 GB + 8 s-chunk transient 0.8 GB · zfturboVocalsV1 fp16 0.3 GB + 0.3 GB (flat 1.5/0.6 GB); QuantConfigured (variants distinguished by quant); P2 N/A (single RoFormerSeparator); unload clearCache. activation = smoke est, phys re-baseline pending. Val 🟡 left as-is (out of scope). WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): VARIANT-KEYED declaration (kim-vocal-2 vs zfturbo-vocals-v1 repos follow the variant), MAT-1..5 green per variant, modelDirectory escape hatch, load() via WeightMaterializer + MelRoFormer(config:)+WeightLoader (was fromPretrained/HubApi). Tagged v0.2.0. -->

| speechEmotion | mlx-emotion2vec-swift | emotion2vec+ | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); CAN-1..3 green (subSecondRuns — one classification forward per short utterance, single MLX eval); engine pin 0.27.0. Tagged v0.3.0. Eff n/a (deliberate, 2026-06-30): single-component recogniser (flat fp16 ~1.0 GB ≈ weights, one short-utterance forward → negligible transient). Split would be low-value; revisit only if it becomes a co-residency bottleneck. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source emotion2vec-plus-large-mlx (safetensors + config), modelDirectory escape hatch added, MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress. Tagged v0.2.0. -->
| audioCodec | mlx-mimi-codec-swift | Kyutai Mimi | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); CAN-1..3 green (subSecondRuns — encode is one whole-clip MLX graph eval; "streaming" describes causality, not execution); engine pin 0.27.0. Tagged v0.3.0. Eff n/a (deliberate, 2026-06-30): single MimiEncoder, flat fp32 ~0.8 GB, streaming neural codec → negligible transient. Not worth the engine-bump churn. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single "main" source mimi-encoder-mlx (encoder.safetensors), modelDirectory escape hatch added, MAT-1..5 green, load() via WeightMaterializer w/ .downloading progress. Tagged v0.2.0. -->
| audioPolish | mlx-audio-polish-swift | AudioPolishKit (DSP) | wrapper+core | audio/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); CAN-1..3 green (subSecondRuns — classical Accelerate DSP, no MLX eval); engine pin 0.3.0→0.27.0 + MLXServeConformance test dep added. Tagged v0.2.0. Eff n/a (deliberate, 2026-06-30): Accelerate DSP — NO MLX, no weights, no GPU (the non-MLX capability seam). Nothing to split; the efficiency contract doesn't apply. -->

| stt | mlx-nemotron-stt-swift | Nemotron 3.5 ASR streaming 0.6B | wrapper+core | audio/PROD | ✅ (v0.3.0) | ✅ | ✅ | 0.35.0 | <!-- NEW 2026-07-10 (first stt provider; contract 1.20.0 stt capability + STTRequest/Response, OpenMDW-1.1 allowlisted, stt added to CAN longRunCapabilities). PUBLISHED: engine main+tag v0.29.0 (contract 1.20.0); core xocialize/nemotron-asr-mlx-swift v0.1.0; wrapper xocialize/mlx-nemotron-stt-swift v0.1.0; weights already on mlx-community/nemotron-3.5-asr-streaming-0.6b (bf16) + -8bit. Core nemotron-asr-mlx-swift (isomorphic MLX-Swift port of Blaizzy/mlx-audio nemotron_asr, cache-aware FastConformer-RNNT + language-ID prompt, 40 locales). Core parity vs Python oracle (CPU/fp32) BIT-IDENTICAL: subsampling / 24 stream-blocks (cold+warm caches) / prompt / decoder+joint all max|Δ|=0.0; mel 2e-4; e2e transcript+97 token ids+timestamps exact + en-US auto-detect. GPU smoke RTF x26.6 ([56,13]), int8 quant 100% word retention. Wrapper NemotronSTTPackage born clean: split footprint bf16 1.24 GB resident + 0.82 GB act / int8 0.64 GB (QuantConfigured bf16|int8); WeightSourcing single "main" source mlx-community/nemotron-3.5-asr-streaming-0.6b (bf16; -8bit variant), modelDirectory escape hatch, MAT-1..5 green; run() born cancel-clean (entry checkpoint first act, per-chunk shouldContinue + RunProgress .decode, post-decode checkpoint; CAN-3 decode/chunk; CAN-1..3 green); unload→clearCache. Val 🟡 = CLI live engine run (nemotron-stt-gate register→prepare(.stt)→run through MLXServeEngine → correct transcript+4 segments+en-US in 3.1s) + consumer MLXCompanion (branch feat/voice-input-stt, PTT + hands-free VAD) BUILD SUCCEEDED against the published graph; →✅ pending an interactive in-app speak test (mic). plan=~/.claude/plans/validated-purring-avalanche.md **v0.2.0 2026-07-24 — LIVE TRANSCRIPTION SURFACE + in-app mic validation (Val 🟡→✅, the named remaining debt).** (1) NEW package-specific **live** surface for audio that is still arriving — the engine's `stt` contract stays ONE-SHOT and the manifest is UNCHANGED (no invented specialty term; `specialties` stays empty, C6 vocabulary untouched). Reached by DOWNCASTING the instance `engine.prepare(.stt)` returns: `(try await engine.prepare(.stt) as? NemotronSTTPackage)?.startLiveTranscription(.init(language:attContextSize:))` → `NemotronLiveTranscription` (`updates: AsyncThrowingStream<Partial,Error>` cumulative, last `isFinal`; ends `CancellationError` on cancel / `NemotronSTTError.sessionInvalidated` on evict; one session at a time via `.sessionActive`). ONE residency — it reuses the model the engine already prepared, so store/progress/settings/eviction all still apply; `unload()` invalidates an open session and the session drops its model ref (else weights stay ARC-alive past eviction). THREADING is the contract: `push`/`finish`/`cancel` are `nonisolated` and only copy+enqueue (safe from an audio callback — MLX never runs on the audio thread); a driver task on `@InferenceActor` does every MLX op, serialized with `run()` like all other engine work. This is the first realization of the **specialty-surface-by-downcast** pattern (for capability-shaped work the canonical contract can't express — here push-INPUT, which N2 `stream()` cannot model since it streams OUTPUT for a request known up front). (2) Core **nemotron-asr-mlx-swift v0.2.0**: public `NemotronStreamingSession` (push any-size 16 kHz mono, cumulative partials, `finish` flushes the tail) built on a streaming mel front-end (bounded trailing sample window + ABSOLUTE frame indices) — and `streamGenerate` now DRIVES that same session, so batch and live cannot drift. NEW `stream-gate` holds them to EXACT equality: ragged pushes (37/1600/17919/160/30000/511 samples, none hop multiples) produce byte-identical tokens+timestamps+partial counts vs the whole-buffer pass at BOTH [56,13] and [56,0]. `--soak N`: 147 s of continuous audio through ONE session at [56,0] holds phys flat 2145→2193 MB (fixed-size caches + fixed trailing window; per-chunk `eval` on the encoder caches so a long stream carries state, not a growing lazy graph). Full CPU parity ladder + gpu-smoke + quant-gate UNCHANGED by the refactor. NEW zero-dependency `ASRMetrics` product (no MLX): WER + word-level Levenshtein alignment ops — for ASR gates and TTS ROUND-TRIP scoring (synthesize → transcribe → score vs input text). (3) **METRICS** (`nemotron-stt-gate --metrics [--mat]`, Release, M5 Max, bf16, published graph engine 0.35.0 + core/kit 0.2.0). `[RUN] pkg=nemotron-stt audio=12.3s cold=1.26s warm=1.24s rtfx=9.9 phys=2.24GB` (at the gate default acs [56,3]; [56,13] is ~25× — the ladder, not a regression). LATENCY LADDER, fed in 100 ms buffers like a mic, unpaced so wall = compute: `[LIVE] acs=[56,0] chunk=80ms ttfp=27ms partials=123 cadence=34ms wall=4.22s rtf=0.34 peak=2.29GB` · `[56,1] 160ms ttfp=28ms rtf=0.19` · `[56,3] 320ms ttfp=35ms rtf=0.10` · `[56,6] 560ms ttfp=32ms rtf=0.07` · `[56,13] 1120ms ttfp=43ms partials=10 rtf=0.04 peak=2.33GB`. **EVERY rung runs faster than realtime — even 80 ms at rtf 0.34 (3× headroom)** — so the whole ladder is usable live; peak is flat ~2.3 GB across rungs (inside the declared 1.24 GB resident + 0.82 GB activation envelope). `[MAT] pkg=stt bytes=1276703238 (main=1276703238) secs=0.3 downloadPhase=yes events=1 marker=yes peak=2524268968` — CAVEAT: 1.27 GB in 0.3 s is a WARM HF-cache hardlink, NOT a cold-network figure (`events=1` for the same reason); it proves the bytes/marker/progress-forwarding wiring, not download throughput. LIV-1..4 green through the REAL engine (`--live --cancel-after 2`): live transcript IDENTICAL to `engine.run` after 38 interim partials, monotonic timestamps, processed 12.4 s of 12.3 s audio, mid-stream cancel ends `updates` with CancellationError and the package still runs after. (4) **IN-APP VALIDATED 2026-07-24 (Val 🟡→✅)** via the NEW consumer **Nemotron ASR Demo** (mlxengine-audio/PROD/Nemotron/"Nemotron ASR Demo" — HF-Space-parity UI vs nvidia/nemotron-speech-streaming-en-0.6b, which is only a thin Triton client, so the native app exposes the latency ladder as a REAL control the web demo cannot). AppKit boot + SwiftUI, golden-path consumer (store-before-register /Volumes/Satechi/Models, needsDownload consent, ModelStateView, EngineSettingsView sheet with onDismiss re-register, detached-task file runs), styled from MLXEngineUI MarqueeTokens. Three modes: Live (mic, word-by-word, settled text bright vs revisable tail dimmed+italic with cursor, latency badge) · File (canonical `engine.run(STTRequest)` + segments + rtfx) · Accuracy (ASRMetrics WER + color-coded alignment). LIVE MIC CONFIRMED on GPU in the sandboxed app: real speech transcribed word-by-word at ~44 ms observed round-trip on the 320 ms rung; Stop promoted interim→final cleanly; first-run flow (folder pick → grant → prepare → Ready) and grant PERSISTENCE across relaunch both verified. Sandbox needs audio-input + network.client + user-selected READ-WRITE + app-scope bookmarks. (Both debts this entry declared are CLOSED in v0.3.0 below.) plan=~/.claude/plans/volumes-satechi-development-mlxengine-a-sorted-hamster.md **v0.3.0 2026-07-24 — ENGINE-EXECUTED MATERIALIZATION ADOPTED + the honest cold MAT figure (both v0.2.0 debts closed).** Package-local `WeightMaterializer` DELETED and `swift-huggingface` dropped; `load()` keeps only the missing-sources guard as the offline backstop (reaching it means no engine materialization ran — no store set, or a non-engine caller). Engine floor 0.29.0→**0.32.0**: with no downloader of its own the package now DEPENDS on the contract-1.24.0 executor. Offline suite 20/20 green. This also CORRECTS the v0.2.0 `[MAT]` line, which was measuring the wrong thing: the package materializer used HubClient and HARDLINKED out of a warm HF cache (1.27 GB in 0.3 s, events=1 — not a download at all). The engine executor streams from the resolve endpoint through an EPHEMERAL URLSession and never consults that cache, so an empty store is now genuinely cold — no HF_HOME juggling needed to measure it. TRUE COLD FIRST RUN: `[MAT] pkg=stt bytes=1276218390 (main=1276218390) secs=19.4 downloadPhase=yes events=72 marker=yes peak=2414217400` (~66 MB/s, 72 distinct progress fractions ⇒ no silent phase). With NO materializer left in the package, that download succeeding IS the engine-executor live proof (the gepard/z-image pattern — bench instead of test). `[RUN]`/`[LIVE]` unchanged on the new graph (rtfx 9.8 at acs [56,3]; ladder still 80 ms rtf 0.34 → 1.12 s rtf 0.04, peak flat ~2.3 GB). Consumer **Nemotron ASR Demo** bumped to kit 0.3.0 and re-verified to Ready — no app-side change was needed, which is the point of moving the executor engine-side. NO REMAINING DEBT. -->
| soundEffect | mlx-moss-soundeffect-swift | MOSS SoundEffect | wrapper+core | audio/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-denoise-step onStep checkpoint (pre-existing, throwing) + new pre-VAE-decode seam (core moss-soundeffect-mlx-swift 0.2.0); CAN-3 denoise/step + decode/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged 0.3.0. split per quant: resident post-evict (DiT + fp32 VAE) bf16 5 GB / int4 3 GB + 100-step CFG denoise @1500-token transient ~10 GB (flat 15/13 GB); QuantConfigured (bf16/int4). P2 APPLIED: Qwen3-1.7B text encoder (~4 GB) encoded once then EVICTED before the denoise loop (encoder-evict lever, in core Pipeline.generate) — removed from the resident floor. unload clearCache. activation = smoke est, phys re-baseline pending. WeightSourcing retrofit 2026-07-09 (audio-fleet MAT sweep): single consolidated "main" source (DiT/VAE + text_encoder/ + tokenizer/ in ONE repo — Qwen3 encoder is NOT separate); repo-keyed (bf16/-4bit); modelDirectory escape hatch; MAT-1..5 green; load() via WeightMaterializer into store layout (old HubApi models/<org>/<name> snapshots re-download once). Tagged 0.2.0. DECIDED 2026-07-09: the frozen (stock-weights) Qwen3-1.7B text_encoder STAYS in the consolidated repo — no think-area split. No second 1.7B-conditioner consumer exists (z-image/klein vendor 4B with different taps), so a split buys nothing and churns the validated encoder-evict lever + MAT globs. Revisit trigger = product pull: a second port needing Qwen3-1.7B hidden-state conditioning → promote the encoder CODE to a think backbone pkg (qwen3vl-mlx-swift pattern) + add a second WeightSource role "text-encoder" on a shared weight repo. -->


## 🖼️ Image

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| textToImage | ernie-image-swift | ERNIE-Image-Turbo | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); core denoise/step checkpoint + encode/decode seams; CAN-1..3 green; engine pin 0.27.0; also fixed ErnieImageTests broken since the Lens→flux2-vae split (blocked ALL test targets). Tagged v0.2.0. split: resident floor bf16 15 GB / int4 5 GB (DiT+VAE; flat was 26/16) + ~11 GB transient; QuantConfigured (bf16/int4); P2 = Mistral-3B encoder load→encode→evict before denoise. activation = smoke est, phys re-baseline pending (image app autorun). -->
| textToImage | lens-mlx-swift | Lens 3.8B | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); GPT-OSS post-evict seam + denoise/step bails + pre-decode seam; CAN-1..3 green both tiers; engine pin 0.27.0; stale flat-footprint testManifest fixed (dead since the 1.14 split). Tagged v0.3.0. split: resident floor bf16 8 GB (DiT+VAE; flat was 62) + ~54 GB transient (GPT-OSS-20B encoder load dominates); QuantConfigured (bf16); P2 = encoder load→encode→evict before denoise, both LensT2I + LensTurboT2I wrappers via shared core. activation = smoke est, phys re-baseline pending (image app autorun). -->
| textToImage / imageEdit | z-image-swift | Z-Image 6B (S3-DiT) | wrapper+core | image/PROD | ✅ (v0.3.0) | ✅ | ✅ | 0.32.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise loop bails per step (ZImagePipeline, shared t2i/img2img both tiers) + cancelled-task decode skip; CAN-1..3 green both tiers; engine pin 0.27.0. Tagged v0.3.0. Tongyi-MAI Z-Image, fully Apache-2.0 (DiT+Qwen3-4B encoder+FLUX.1 AE). Two tiers via shared ZImageGenerator: ZImageT2IPackage (Base, 28-step CFG, z-image-t2i) + ZImageTurboT2IPackage (distilled 8-step no-CFG, z-image-turbo-t2i). split QuantFootprint: resident bf16 20 / int8 15 / int4 6 GB (DiT+encoder+VAE co-resident — encoder small enough that P2 evict NOT warranted here) + ~14-20 GB transient (int4 decode scratch dominates). QuantConfigured (bf16/int8/int4; quant on load from bf16 snapshot — pre-quant repos later). WeightSourcing/MAT (7/7). unload()→clearCache. Parity fp32/CPU: DiT cos ≥0.9999999, VAE 118 dB, encoder ids-exact, e2e 105-108 dB; GPU int4 1024² ~13 s/25.7 GB peak. img2img SHIPPED v0.2.0 (imageEdit surface z-image-{,turbo-}img2img; encode→renoise@strength→denoise, metaData strength default 0.6, reuses full AutoencoderKL encoder). Weights mlx-community/Z-Image-{Turbo-bf16,bf16}. Val 🟡 = CLI pkg-e2e green (real load→run→decode), pending in-app generation + phys re-baseline (smoke under-reads ~2.7×). AUTO-MATERIALIZATION EXECUTOR wired 2026-07-23 (4eaa910), upgraded same day to the fleet-reference fast materializer (9962cbc, tagged v0.4.2; live-probed incl. the >=64MB parallel path @35MB/s — NOTE weightSources uses the quant-suffixed repo so bare family-repo probes 404; production resolves via effectiveRepo): load() now downloads missing declared sources (WeightMaterializer/HubClient + WeightDownloadProgress) — previously declaration-only (MAT offline-green but first fresh-store load threw unreadableSnapshot; the Mage Demo field issue). **ENGINE-EXECUTED MATERIALIZATION ADOPTED 2026-07-23 (engine 0.32.0 / contract 1.24.0, tagged v0.4.3):** package-local WeightMaterializer (297 lines) + the load()-side materialize call deleted — the engine now downloads missing declared sources in resident()/prepare() before load(); load() keeps only the resolvedSnapshotDirectory guard as the offline backstop. swift-huggingface dropped (the materializer was its only consumer here). **This repo carries the fleet's LIVE proof of the new path** — `EngineMaterializationE2ETests` (skipped unless ZIMAGE_MAT_E2E=1) drives a real MLXServeEngine.prepare() against an EMPTY store with a load() that refuses unless the source is already materialized, so prepare() succeeding IS the executed-before-load evidence: 99,630,640 B text-encoder shard landed, needsDownload flipped true→false, re-prepare after evict did not re-download. MAT store-layout fixture de-staled (path from ModelStore.directory(for:) — the literal predated the 1.22.0 models--org--name layout). MAT+CAN green. -->
| textToImage / imageEdit | flux2-klein-swift | FLUX.2-klein-4B (MMDiT) | wrapper+core | image/PROD | ✅ (v0.5.0) | ✅ | ✅ | 0.32.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); encode seam (encodeAndEvict) + per-step bails in KleinPipeline/KleinEditPipeline + decode skip; CAN-1..3 green both tiers; engine pin 0.27.0. Tagged v0.5.0. BFL FLUX.2-klein-4B, Apache-2.0 (4B ONLY — 9B variants are FLUX Non-Commercial, intentionally NOT wrapped). Klein4BT2IPackage (surface flux2-klein-4b-t2i) via KleinGenerator. Compact rectified-flow MMDiT (5 double + 20 single blocks, 24 heads, 4-axis RoPE θ2000) + Qwen3-4B 3-layer-tap (9/18/27→7680) conditioner + FLUX.2 VAE (reused from flux2-vae-mlx-swift). Distilled 4-step, guidance 1.0, no neg prompt. split QuantFootprint: resident bf16 16 / int8 12 / int4 11 GB (DiT+encoder+VAE co-resident) + ~8 GB transient; int4 DiT 2.35 GB / peak 19.2 GB @1024². QuantConfigured (quant-at-load from bf16 snapshot). WeightSourcing/MAT (5/5). unload()→clearCache. Parity vs diffusers goldens: DiT cos ≥0.9999995 @64×64 grid, VAE 105-130 dB, encoder cos 0.9999999; GPU int4 1024²/4-step ~6 s. Weights mlx-community/FLUX.2-klein-4B-bf16. THREE pipeline bugs found via capturing diffusers' exact transformer inputs: full-512-padded-text-tokens, diffusers-linear-mu scheduler, chat-template. Multi-ref EDIT SHIPPED v0.2.0 (surface flux2-klein-4b-edit, IEditRequest multi-image): net-new FLUX.2 VAE encoder (cos 1.0/130dB) + reference token-concat + 4D-RoPE t-offset t=10*(i+1); plain path (KV-cache speed TODO); fox-ref+beach-prompt validated through real package. Weights unchanged (encoder.* keys in same snapshot). **Val ✅ (v0.4.1)** — in-app IMAGE_AUTORUN validated: t2i/edit coherent+adherent, 2-ref fuse confirmed. **Footprints RE-BASELINED to in-app phys** (int4 t2i floor 9.32/peak 20.54/act 11.22; edit 2-ref peak 20.57). **ENCODER-EVICT light tier v0.4.0** (KleinConfiguration.evictEncoder): per-request Qwen3-4B evict → **phys @768² floor 5.13/peak 10.06 = FITS 16 GB**; surfaced via FootprintConfigured so the governor admits it. Consumer ladder: 16 GB evict@768² → 24 GB evict@1024² → 32 GB+ resident → 64 GB base → 128 GB base-bf16. BASE quality tier v0.3.0 — see next row. AUTO-MATERIALIZATION EXECUTOR wired 2026-07-23 (7b826d1), upgraded same day to the fleet-reference fast materializer (023dc6e, tagged v0.7.2: parallel ranged chunks + task-context progress bridge + no cache double-store; live-probed): load() now downloads missing declared sources (WeightMaterializer/HubClient + WeightDownloadProgress) — previously declaration-only (MAT offline-green but first fresh-store load threw unreadableSnapshot; the Mage Demo field issue). **ENGINE-EXECUTED MATERIALIZATION ADOPTED 2026-07-23 (engine 0.32.0 / contract 1.24.0, tagged v0.7.3):** package-local WeightMaterializer (297 lines) + the load()-side materialize call deleted — the engine now downloads missing declared sources in resident()/prepare() before load(); load() keeps only the resolvedSnapshotDirectory guard as the offline backstop. swift-huggingface dropped (the materializer was its only consumer here). MAT store-layout fixture de-staled (path from ModelStore.directory(for:) — the literal predated the 1.22.0 models--org--name layout). MAT+CAN green. Committed against in-flight LoRA-registry work in the same tree: only the adoption hunks were staged (filtered git apply --cached), the LoRA changes stay uncommitted. -->
| textToImage / imageEdit | flux2-klein-swift | FLUX.2-klein-base-4B (MMDiT, CFG) | wrapper+core | image/PROD | ✅ (v0.5.0) | ✅ | ✅ | 0.32.0 | <!-- CAN retrofit 2026-07-09: see distilled klein row — same repo/v0.5.0, CAN-1..3 green on Klein4BBaseT2IPackage too. BFL FLUX.2-klein-base-4B, Apache-2.0 (4B ONLY). Klein4BBaseT2IPackage (surfaces flux2-klein-4b-base-t2i / -base-edit) — the QUALITY tier, DISTINCT PackageID from the distilled Klein4BT2IPackage so both co-resist. Transformer architecture BYTE-IDENTICAL to distilled klein-4B (same 5 double + 20 single MMDiT, guidance_embeds=false) → drop-in through the same KleinWeights loader; text_encoder (Qwen3-4B) + FLUX.2 VAE are the SAME frozen weights as the distilled tier. The base checkpoint is NOT guidance-distilled: runs classic two-pass classifier-free guidance (do_cfg = guidance_scale>1 and not is_distilled) — noise = neg + scale·(pos−neg), guidance=nil to the DiT both passes — + negative prompts over ~28 steps (KleinConfiguration.base(): repo klein-base-4B, defaultSteps 28, guidanceScale 4.0). CFG in BOTH KleinPipeline (T2I) and KleinEditPipeline (edit; references identical in both passes). split QuantFootprint: resident bf16 16 / int8 12 / int4 11 GB (same as distilled) + ~10 GB transient (CFG runs DiT 2× per step; edit doubles the seq → base edit ≈4× base T2I). QuantConfigured (quant-at-load). WeightSourcing/MAT + base conformance 9/9 (MAT gate, .base()/.turbo() factories, distinct base surfaces, guidanceScale Codable). Validated on GPU: base T2I 28-step CFG renders dense picnic scene w/ correct object counts + legible 'PICNIC' text (distilled 4-step degrades to 'PIC NC'); base edit CFG preserves reference-fox identity while composing helmet + castle-wall-at-sunset. Weights mlx-community/FLUX.2-klein-base-4B-bf16 (transformer new; encoder/vae identical to distilled repo). Val 🟡 = CLI + oracle-validated, pending in-app + phys re-baseline. Engine-executed materialization adopted 2026-07-23 with the distilled row (same repo, engine 0.32.0, tagged v0.7.3). -->

| textToImage / imageEdit | mage-flow-swift (MLXMageFlow) | Mage-Flow family 4.1B (NR-MMDiT, 6 checkpoints) | wrapper+core | image/WIP | ✅ (v0.2.0) | ✅ | ✅ | 0.32.0 | <!-- Born-conformant 2026-07-23: MageFlowT2IPackage (Mage-Flow/-Base/-Turbo) + MageFlowEditPackage (Edit tiers) — two classes because t2i vs edit are DIFFERENT DiT checkpoints (same arch); tier = configuration (RL 20/5, Base 30/5, Turbo 4/1). MIT weights + MIT port. Split footprints @1024² (measured t2i peaks): bf16 16.4+3.7 (19.59), int8-g32 14.2+3.7 (17.15), int4-g64 12.9+3.7 (16.08); QuantConfigured; FootprintConfigured hints for the conditioner-evict light tier (Qwen3-VL-4B 8.3 GB transient -> int4 floor 4.7 GB) + size-scaled activation. WeightSourcing 2-source: microsoft/<variant> components (quant tiers EXCLUDE the 7.7 GB bf16 transformer) + xocialize/<variant>-mlx artifacts (folded_adaln + pre-quantized DiT int8-g32/int4-g64, self-describing metadata, no bf16 load peak). Quant recipe: attn+MLP only, mods/io/norms + trailing block 11 bf16 (final-norm amplifies its error ~1.6x); gates: flagship golden-relative int8 deficit 1.30e-4 <= 2x bf16 baseline 0.99e-4 (bf16 itself ~1e-4 from fp32 at ~1.2e5 activation range — absolute 0.9999 was miscalibrated for this arch), int4 0.9911; family siblings absolute int8 >=0.9998 / int4 >=0.99, all 12 PASS; e2e int8 43.9 dB vs bf16, int4 valid render. CAN: entry checkpoint first act of run(); post-screen seam, per-denoise-step break (MageFlowPipeline.denoise shouldStop), pre-decode seam; CancellationError never wrapped (refusal surfaced as contentRefused). MAT-1..5 + CAN-1..3 + manifest/Codable/store offline suite (13 green); live wrapper gate mage-pkg-smoke t2i+edit bf16/int4 PASS via registration factory. Trust features intact: mandatory fail-closed content filter + bit-exact Gaussian-Shading watermark. NAX row-chunk armed on the bf16 path only (inert for QMM); on mlx-swift bump run MageFlowGate --nax-probe. MATERIALIZATION EXECUTOR wired 2026-07-23 (483e23d, WeightMaterializer per MLXLTX2 pattern: HubClient snapshot into store layout + WeightDownloadProgress; found live in the Mage Demo app on family-member switch — the un-wired-executor field issue). FILTER PARITY fix (7dee62c): screen_edit sees the ORIGINAL-resolution image (upstream screens BEFORE the 384px conditioning resize); reusing the conditioning grid flipped borderline benign verdicts — upstream torch repro cleared the upstream model. Val in-app (Mage Demo, private xocialize/mage-flow-demo): live [MAT] pkg=turbo-int8 bytes=15.28 GB (components 9.23 + mlx-artifacts 6.05) secs=751 downloadPhase=yes events=5 (file-granular — fraction sits 0% during the single big file); [RUN] t2i int8 @1024^2 7.6 s phys 16.44 GB (single package); edit int8 @1024^2 15.7 s phys 30.87 GB (BOTH packages co-resident — each holds its own Qwen3-VL). ENGINE GAP FIXED 2026-07-23 (engine 021f012, MS-6, post-1.23.0 main): writeMarker now stamps one marker per declared WeightSource repo (provenance.sourceRepo = fallback for non-WeightSourcing configs; source revision pin rides the marker, nil→main), needsDownload probes the declared sources (MS-2 missing-set) instead of the provenance marker, and MaterializationBench requires markers on ALL declared source repos (caller-passed sourceRepo = fallback only) — variant configs (turbo/base/edit tiers) now credit the storage panel's correct rows and the bench's false marker=NO clears on the next [MAT] run. Stale family-primary markers (microsoft/Mage-Flow{,-Edit} marker-only dirs) are harmless residue, deletable from the storage panel. **ENGINE-EXECUTED MATERIALIZATION ADOPTED 2026-07-23 (engine 0.32.0 / contract 1.24.0, tagged v0.2.1):** package-local WeightMaterializer (340 lines) + the load()-side materialize call deleted — **this repo's copy was the fleet reference the engine executor was lifted from** (cf45682 + 6faa4cb: chunk-delegate streaming, task-context progress bridge, ≥64 MB parallel ranged chunks), so the engine now carries it for every integration. load() keeps only the resolvedSnapshotDirectory guard as the offline backstop. swift-huggingface dropped (the materializer was its only consumer here). `mage-pkg-smoke probe-dl` rewired to MLXServeCore.WeightMaterializer — the progress-granularity probe now exercises the shipping executor instead of a dead copy. MAT store-layout fixture de-staled (path from ModelStore.directory(for:) — the literal predated the 1.22.0 models--org--name layout). MAT+CAN green. -->
| textToImage / imageEdit | boogu-image-swift | Boogu-Image-0.1 | wrapper+core | image/WIP | 🧪 | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise/step bails (t2i+edit) + decode skip, post-encode-evict checkpoints pre-existing; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. encoder-evict (P2 headline) + split. Qwen3-VL-8B conditioner (~16 GB bf16) loaded→encode pos/neg→eval→evict (nil + clearCache) BEFORE the DiT denoise, on EVERY quant — moves ~16 GB resident→transient. Post-evict resident floor = DiT(quant)+FLUX VAE ≈ int4 8 / int8 13 / bf16 20 GB (flat was 24/29/36, which baked the conditioner in) + ~20 GB transient (max(encode, denoise)). QuantConfigured (int4/int8/bf16; quant=DiT only). unload()→clearCache; per-step GPU.clearCache (Pipeline.swift:69) + absolute snapshot-path resolution NOT regressed. Parity gate green post-refactor (s0-keys exact 942/244; s1-sched 6.3e-8; s1-vae decode/encode 0.0; s2-dit t2i/edit 0.0 — bit-exact). Known PRE-evict in-app phys was floor 67.4 / peak 70.5 GB (vs 36 GB declared, ~1.85× gap); activation = smoke/derived est, in-app phys re-baseline pending (MLXEngineImage autorun, post-evict). --> |
| textToImage | anima-mlx-swift | Anima (Cosmos-Predict2-2B) | wrapper+core | image/PROD | ✅ (v0.1.1) | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise/step bails in AnimaPipeline.sample + encode/decode seams; CAN-1..3 green; engine pin 0.27.0. NOTE longRunImplied=false only because the flat pre-1.14 footprint declares no peakActivationBytes — cadence declared anyway; flip the suite's assert when the split-footprint retrofit lands. Committed on wip/rope-extrapolation-fix (no tag/push — rides the in-flight RoPE branch). NC weights (C7 two-layer gate, rejectedWeight under .permissiveOnly); parity-locked to xocialize/anima-mlx Python (e2e cos 0.999105); measured peak bf16 8.0 GB / int4 6.5 GB @512². Wired into MLXEngineImage ImageValidationView (bf16 + int4 variants, NC admitted via .permissiveOrAcknowledged) — app BUILDS; Val 🟡 = CLI --package-gate green + app-wired, pending one interactive in-app generation. -->
| imageEdit | qwen-image-edit-swift | Qwen-Image-Edit-2511 | wrapper+core | image/PROD | ✅ | 🟡 | ✅ | 0.32.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() in ALL THREE wrappers (base/Turbo/TeleStyle — Turbo's even sat after the LoRA-swap network path); shared-core QwenImageEditGenerator denoise/step checkpoint + encode/decode seams; LoRA-download laundering fixed (LoRACache.ensure rethrows CancellationError); CAN-1..3 green ×3; engine pin 0.3.0→0.27.0. Tagged v0.4.0. **Val RE-CONFIRMED IN-APP 2026-07-25 (post-C14):** the pre-existing ✅ predated the inference-mode fix and was granted on exactly the eyeball validation that defect survived, so it was stale. Re-driven live through the new BiRefNet Demo consumer on the fast tier — real download phases surfaced, Ready, valid matte, 4.40 s / phys 3.08 GB / mean 0.093 / fg 9.5% (Debug -Onone; Release CLI 0.38 s). The 2048 best tier is still un-exercised in-app (its ~48 GB peak makes it a deliberate skip on anything smaller). --> |
| textToImage | qwen-image-edit-swift (MLXQwenImageFlash) | Qwen-Image-Flash (DMD2 4-step) | wrapper (T2I path on the shared core) | image/PROD | 🧪 | 🟡 | 🟡 | 0.35.0 | <!-- Born-conformant 2026-07-25: QwenImageFlashPackage, PackageID qwen-image-flash, capability textToImage (the repo's first non-imageEdit surface). nvidia/Qwen-Image-Flash is a PURE WEIGHT DELTA on Qwen-Image — transformer key set identical to 2511 (1933 tensors), text_encoder/tokenizer/vae sha256-identical — so no new core: new T2I path on QwenImageEdit (QwenImageT2IGenerator/PipelineT2I.swift) = text-only encode (template drop_idx 34 vs edit's 64), no cond latents, single imgShapes, STATIC shift-3 schedule (use_dynamic_shifting:false) → sigmas [1.0,0.9,0.75,0.5,0.0], true CFG 1.0 (guidance internalized ⇒ one DiT forward/step). C7 = LicenseRef-NVIDIA-Open-Model (allowlisted permissive; §3.1 Notice required when REDISTRIBUTING weights), C8 MIT. PARITY: prompt embeds cos 0.9999926 bf16; DiT step-0 cos 1.0000002 fp32-CPU / 0.99836 bf16 (looser bf16 gate by design — 256-token golden vs edit's ~8k); e2e 1024²/4-step bf16 coherent in 83.3 s (M5 Max). ⚠ mlx#3797 NAX split-K hits the T2I path where EDIT was immune (K≥3·max(M,N) caps the window at M≤4096; edit packs 8192): M=4096 was NaN on 0.31.6 → exact row-chunk in QwenFeedForward.downProjected, predicated on actual (rows,K,N) so edit's kernel path is untouched; QIE_NO_CHUNK hatch, NAXProbeTests is the removal signal. Offline gates green: MAT-1..5, CAN-1..3, manifest, sigma/window. footprint MEASURED (QIF_MEMBENCH, M5 Max, 1024²/4-step/cfg1): floor 41.4 GB, peak 57.4 GB, activation 16.1 GB → declared 41.4/19.3 GB (+20%), under the edit path's 17.9 as predicted; in-app phys re-baseline still owed. DEBT: weights PUBLISHED, two tiers in the mlx-community 'Qwen-Image-Flash (MLX)' collection: -bf16 (byte-identical to nvidia/Qwen-Image-Flash, verified file-by-file, plus the tokenizer.json upstream omits — swift-transformers cannot read slow-tokenizer files, so upstream alone could not materialize) and -8bit (v0.6.0; int8 DiT attn+FFN+mod g64 + int8 VL-7B LM, no vision tower, unquantized VAE; PRE-quantized so consumers never materialize bf16 — the tier is loadable on machines that cannot hold the bf16 weights at all). int8 MEASURED: DiT step-0 cos 0.9973 (bf16 0.99836), encoder prompt-embed 0.99992 (bf16 0.9999926), floor 22.3 GB / peak 30.0 GB / activation 7.8 GB→9.3 declared, e2e 1024²/4-step 19.8 s vs bf16's 83.3 s (4×), load 2.3 s vs ~60 s; render visually indistinguishable from bf16. int4 BUILT AND REJECTED on evidence: DiT cos 0.9623 (g64) / 0.9659 (g32), encoder 0.9845, render visibly soft/washed-out — finer scales did not rescue it; sweep preserved in FlashQuantSweepTests, no 4-bit repo published. BudgetAware: bf16→int8 degrade resolves BEFORE materialization so a constrained machine downloads 28 GB instead of 41 GB it could never seat. v0.7.0 adds a per-request multi-LoRA stack (metaData["loras"], up to 4 slots, rank-stacked on the resident DiT via the swapper; runtime apply, never fuse). RESOLVED→GUARDED (v0.7.2): bf16+LoRA corruption attributed to mlx#3797 reached via the adapter graph (A/B vs the #3810 backport renders clean; every isolated GEMM probes clean — composition-only). Package refuses bf16+adapters for image tokens in [1366,4096]; int8+adapters unrestricted (verified). Demo routes LoRA renders to an int8 backer. All guards removable when the mlx-swift pin vendors mlx ≥ a8c3e9c. --> |
| imageEdit | qwen-image-edit-swift | FireRed-Image-Edit-1.1 | wrapper (checkpoint swap) | image/PROD | 🟡 | 🔴 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: rides the 2511 row (same QwenImageEditPackage code path, v0.4.0). FR1 (FIRERED-ENHANCEMENTS.md): architecturally stock Qwen-Image-Edit-Plus → NO port/conversion; FireRedImageEdit.registration in MLXQwenImageEdit reuses QwenImageEditPackage + QwenImageEditConfiguration(snapshotPath:<firered snapshot>), own manifest (Apache-2.0, provenance FireRedTeam/FireRed-Image-Edit-1.1, PackageID firered-image-edit). 2511-measured split footprint applies (identical graph). GATES: 57.7 GB snapshot download, in-app A/B vs 2511 (self-reported GEdit 7.943 vs 7.877), true-CFG scale validation (FireRed-1.0 "CFG offset too large"). Never stack 2511-trained TeleStyle LoRAs on these weights; FireRed Lightning LoRA (8-step) only fuses onto FireRed base. --> |
| imageColorize | mlx-ddcolor-swift | DDColor | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); re-baselined 4.54 GB peak makes it long-run implied so cadence (encode/frame + postprocess/frame) declared, NOT the sub-second exemption; first test target added; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. --> |
| imageRestore | mlx-nafnet-swift | NAFNet (v0.4.0) | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); monolithic full-frame eval — cadence encode/frame + postprocess/frame at the real seams; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. --> |
| imageRestore | mlx-fftformer-swift | FFTformer GoPro (v0.1.0) | wrapper+core | image/WIP | 🧪 | ✅ | ✅ | 0.38.0 | <!-- P4 (Forge Gap Program), 2026-07-26/27. SECOND package on imageRestore alongside NAFNet — PackageID-selected, NO contract change. Weights mlx-community/FFTformer-GoPro-fp32 (66.3 MB, MIT code+weights committed in-repo upstream at kkkls/FFTformer; 16,560,474 params). PARITY green on the CPU stream vs the PyTorch oracle: key contract 535 tensors 0 missing/0 unused; primitives 7/7 with patchify/unpatchify/fft-gate BIT-IDENTICAL (MLX rfft2/irfft2 reproduce PyTorch exactly on 8×8 patches); blocks 5/5 worst rel 5.4e-07; full model 64/128/256 all cosine 1.00000000. Re-verified against the freshly-downloaded published artifact. Conformance MAT+CAN+manifest 8/8 green. DTYPE fp32 BY MEASUREMENT, against the "conv models ship fp16" rule: bf16 30.09 dB (disqualified — same order as the model's own 34.21 dB task signal), fp16 50.13 dB (viable but 66 MB isn't worth the margin), fp32 108.99 dB. Note fp16 BEAT bf16 by 20 dB — mantissa-bound, not range-bound, inverting the LLM default. 🔴 TILES MANDATORY: full-frame peaks 39.55 GB MLX / 109 GB phys at 1080p (level 1 at full res with 6× channel expansion to 288ch + a patchify copy; predicted within 10% by arithmetic). restoreTiled default tile 256/overlap 32 → 4.47 GB MLX / 8.07 GB phys @1080p, ~60 tiles; peak is one-tile-sized so it is FLAT in resolution, not linear. Tile geometry MUST be 32-aligned (overlaps 16/48/112 scored ~20.6 dB vs ~26 dB at 0/32/64/96 — FFT patches are measured from the tile origin and a level-3 patch spans 32 full-res px); code rounds both down to 32. Feathered blend, not crop-valid (RF is low hundreds of px); acceptable because GoPro trained on 128×128 crops, so tiles are MORE in-distribution than full frames. CAN-3 cadence names the REAL tile loop (postprocess/chunk, RunProgress-evidenced) — unlike NAFNet's monolithic forward. ✅ FOOTPRINT MEASURED 2026-07-27 via ValidationHarness through the real MLXServeEngine: floor 0.10 / peak 5.80 / act 5.70 / retain 0.33 GB @1080p; declared 150 MB / 6.5 GB. Notably the ONLY estimate in this batch that was OVER the truth — CIDNet's was under by 2x and Restormer's, DRUNet's and SCUNet's were all under too, so do not read this one as evidence that estimating is safe. Tagged v0.1.1, because v0.1.0 shipped the pre-measurement footprint and the declared footprint IS the admission basis. 🔴 C2 MEASURED 2026-07-27 and THIS PACKAGE FAILED IT — RealBlur (CC BY 4.0, beam-splitter rig, real optical blur; 300 of 980 official test pairs, the AUTHORS' ECC-alignment protocol). FFTformer GoPro scores 27.52 dB against a 27.55 dB untouched-input baseline: **-0.02 dB, it does not beat leaving the image alone**, while Restormer motionDeblur on the SAME GoPro training gains +1.29. 🔴 The mean UNDERSTATES it — the per-image distribution is the real finding: median +0.18 (it helps slightly on most images), std dev 1.96 vs Restormer's 1.14, worst case -18.70 dB vs -0.69, 41% of images HURT vs 5%, and 18% hurt by >1 dB vs 0%. Gains and losses cancel almost exactly (+180 dB vs -187 across 300 images). So it is not neutral, it is UNPREDICTABLE — and a default must be safe on the tail, not good on the median. 🔑 'Deblurring in the Wild's explanation (GoPro training fails on real blur) is the WRONG MECHANISM: Restormer is also GoPro-trained and transfers fine, so it is model-specific — and the ranking INVERTS (GoPro 34.21 vs 32.92 in FFTformer's favour; RealBlur-J -0.02 vs +1.29 against it). GoPro rank is not a weak predictor here, it is a backwards one. ⚠️ NOT comparable to FFTformer's published RealBlur-J 32.62, which is the RealBlur-TRAINED checkpoint we do not hold. On DPDD this same checkpoint scored -0.86 dB against defocus, so it is at or below the untouched input on BOTH real-blur corpora. ➡️ ACTION: Restormer motionDeblur (already shipped in mlx-restormer-swift) should be the default deblur route; if FFTformer stays, port upstream's RealBlur-TRAINED checkpoint — the architecture is not what failed. ⬜ Still open: SMARTPHONE blur (RealBlur is DSLR) — the own-device protocol in the package's REAL-BLUR-VALIDATION.md now serves that question only. Full record: mlxengine-image/corpus/realblur/RESULTS.md. Previously: the acceptance study was blocked on a tripod-paired corpus (see the package's REAL-BLUR-VALIDATION.md — "Deblurring in the Wild" found every GoPro-trained SOTA scored BELOW the blurry input on real smartphone blur, so GoPro rank is NOT evidence of product value). --> |
| **imageRelight** | mlx-cidnet-swift | HVI-CIDNet (v0.1.0) | wrapper+core | image/WIP | 🧪 | ✅ | ✅ | 0.38.0 | <!-- P3 (Forge Gap Program), 2026-07-27. FIRST PROVIDER of `imageRelight` — a NEW capability added for it in contract 1.29.0 (engine f8cf5fb, tag v0.38.0): Capability.imageRelight → CanonicalOutput.image, plus ImageRelightRequest/Response/Contract. NOT folded into imageRestore, and the reason is behavioural not taxonomic: every published checkpoint drives output toward a target mean luma REGARDLESS of input, so on an already-correctly-exposed image the model DEGRADES it — measured 23.37 / 20.99 / 16.10 dB (Generalization / LOLv1 / SICE). A planner told to "restore" must not silently re-expose. Request carries `strength` (0…1), which restoration has no notion of; response carries `appliedStrength` + `bypassed` as TYPED fields because "I declined" is something callers act on. Weights: THREE repos published — mlx-community/HVI-CIDNet-{Generalization,LOLv1,SICE}-fp32 (7.92 MB each) + a collection; upstream Fediory/HVI-CIDNet is MIT with ZERO non-commercial clauses and the authors publish first-party. PARITY green on the CPU stream vs the PyTorch oracle: key contract 191 tensors / 1,975,569 params / 0 missing / 0 unused, passing for ALL THREE checkpoints; HVI colour transform BIT-EXACT (worst 6.6e-07) incl. degenerate colours and the yellow/cyan channel TIES that decide masking priority; primitives 4/4 with PReLU exactly 0.00e+00; blocks 4/4; full model 4/4 at cosine 1.00000000 incl. a severely underexposed tile. Re-verified against the freshly-downloaded published artifact. Conformance 11/11 (MAT + CAN + manifest + contract-surface + gate-ordering + pinned defaults). SHIP-BLOCKER HANDLED IN-PACKAGE: Rec.709 luma gate evaluated BEFORE any model work, bypassing above threshold and reporting bypassed:true; strength blends linearly toward the input, and at strength 1 the path is bit-identical to the parity-gated result. UPSTREAM QUIRKS: I_LCA5 is DEAD CODE (ablation-verified — zeroing its weights changes output by exactly 0.0) so it is declared but never evaluated; RGB_HVI is stateful (HVIT writes this_k, PHVIT reads it) so the port reads the parameter directly; density_k is LEARNED and per-checkpoint (0.98/1.13/0.40 vs init 0.2). Queue corrections: padding is ReplicationPad2d and resampling is UpsamplingBilinear2d i.e. alignCorners TRUE (opposite of the sibling FFTformer port). ✅ FOOTPRINT MEASURED 2026-07-27 via ValidationHarness: floor 0.04 / peak 5.87 / act 5.84 / retain 2.66 GB @1080p; declared 80 MB / 6.5 GB. 🔴 THIS is the row that justified the whole measurement exercise — the estimated 3.0 GB activation was UNDER by ~2x, the direction that falsely ADMITS on tight Macs. The 2.66 GB retain (vs the tiled siblings' ~0.35 GB) is the other half of the story: CIDNet does not tile, so a live model holds full-frame intermediates. ⬜ Val pending: and the luma-gate THRESHOLD (default 0.35) is a documented placeholder from a synthetic linear-scaling ladder — calibrate against real underexposed captures before trusting it. --> |
| imageRestore | mlx-restormer-swift | Restormer (v0.1.0) — motion / defocus / realDenoise | wrapper+core | image/WIP | 🧪 | ✅ | ✅ | 0.38.0 | <!-- P10 (Forge Gap Program), 2026-07-27. THIRD package on imageRestore alongside NAFNet and FFTformer — same request shape, NO contract change, variant-selected. ONE ARCHITECTURE, THREE PRODUCTS: motionDeblur (GoPro 32.92), defocusDeblur (DPDD 25.98 single-image — notable because IFAN/KPAC/DRBNet/LaKDNet are ALL AGPL-3.0, so this is the only shippable defocus option), realDenoise (the only solid DND 40.03 = cross-sensor generalization). LICENCE: plain MIT verified by raw-file fetch — the SAME AUTHOR'S MIRNet/MIRNetv2/MPRNet/CycleISP carry an Academic Public License and are easy to conflate; Restormer does not. Weights from the first-party GH release v1.0, published as THREE mlx-community repos + a collection. PARITY green vs the PyTorch oracle on the CPU stream: S0 494 tensors / 26,126,644 params strict-clean (deblur) AND 406 / 26,111,668 (denoise); S1 primitives 4/4; S2 blocks 3/3 (MDTA, GDFN, TransformerBlock); S3 full model 4/4 at cosine 1.00000000, worst rel 1.0e-06. Re-verified against freshly-downloaded published artifacts, both norm variants. Conformance 11/11. 🔑 THE VARIANT CHANGES THE KEY SET, not just the weights: denoising checkpoints are LayerNorm BiasFree and carry NO bias vectors — 406 tensors vs 494, an exact 88-tensor difference; the wrong config fails the strict load with 88 missing keys (verified in both directions). BiasFree also does NOT centre its output (x/sqrt(var+eps)*weight), so implementing it as 'LayerNorm minus the bias' would be silently wrong. 🔴 TILING MANDATORY: full-frame 1080p costs 15.50 GB MLX / 48.02 GB phys (measured); tiled, the MLX peak is FLAT at 2.58/2.59/2.62 GB across 512²/1024²/1080p because the peak is one-tile-sized. Tile geometry is 8-ALIGNED (three pixelUnshuffle(2) stages make the grouping grid phase-sensitive to the tile origin — same class as FFTformer's 32-alignment, one third the stride). Overlap chosen on SEAM VISIBILITY not PSNR: overlap 0 leaves a faint seam (boundary gradient 1.31x interior) while every aligned overlap >=8 is clean (1.09-1.20x) — and PSNR-vs-full-frame actually PREFERS overlap 0, which is why it is the wrong metric for a tiler. Defaults tile 384 / overlap 32. Pixel-shuffle gate additionally runs the WRONG (r,r,C) channel ordering and measures it at 493,451x the observed rounding, so the loosened 1e-5 tolerance keeps ~5 orders of discrimination. ✅ FOOTPRINT MEASURED 2026-07-27 via ValidationHarness: floor 0.14 / peak 4.90 / act 4.76 / retain 0.36 GB @1080p; declared 180 MB / 5.5 GB (the --bench-derived 4.5 GB estimate was slightly UNDER). ✅ C4 MEASURED 2026-07-27 on the FULL official DPDD test split (76 scenes, 1680×1120, PSNR vs the all-in-focus reference): defocusDeblur scores 25.98 dB against a 23.89 dB untouched-input baseline, i.e. +2.09 dB — so the row's real question ("is the only SHIPPABLE defocus option good enough to ship", every rival being AGPL-3.0) is answered YES. 🔑 We reproduced the PUBLISHED 25.98 dB EXACTLY, which validates the harness rather than just the model — the same PSNR/pairing machinery as the NIND run now has a control with a known correct answer. 🔴 THE FINDING THAT MATTERS IS ROUTING, NOT THE MODEL: the two control arms are both mistakes a planner can actually make, and both are expensive. Right model / WRONG VARIANT (motionDeblur on defocus) scores +0.09 dB — indistinguishable from doing nothing, on an architecturally IDENTICAL network. Wrong model entirely (FFTformer GoPro on defocus) scores −0.86 dB, i.e. actively worse than leaving the image alone. So picking imageRestore correctly but the PackageID wrongly throws away essentially all the benefit, and defocus vs motion blur are NOT interchangeable at the surface level even though both are "deblur" — yet nothing in the pipeline measures which blur an input actually has. ⚠️ DSLR (5D IV), PSNR-only, full-frame (the shipped package tiles at 384/32, which is not what produced these numbers), and DP figures remain non-comparable. ⚠️ DPDD's DATASET terms are UNSTATED — the repo's MIT covers "the Software" and the data ships separately with no licence at all, so this is INTERNAL evidence only, not redistributable and not a publishable claim (CORPUS-NEEDS.md §C4). ⬜ Still pending: C5 was measured for realDenoise (see the SCUNet row); spatially-varying blur remains untested — a focus bracket with real depth is what would expose the global-attention weakness. -->
| imageRestore | mlx-drunet-swift | DRUNet (v0.1.0) — the strength dial | wrapper+core | image/WIP | 🧪 | ✅ | ✅ | 0.39.0 | <!-- P5 (Forge Gap Program), 2026-07-27. FOURTH package on imageRestore, and the FIRST to honour a strength dial. CONTRACT 1.30.0 (engine v0.39.0, PUSHED) was added for this row: optional ImageRestoreRequest.strength (0…1) + ImageRestoreResponse.appliedStrength, both defaulted so NAFNet/FFTformer/Restormer are untouched and simply ignore them. Why a parameter and not a new capability: unlike imageRelight the request/response shape is otherwise identical, so a separate capability would fragment a surface three packages already share; the alternative was untyped metaData where no planner would find it. appliedStrength == nil is the honest signal a backer has NO dial vs a dial that did nothing; descriptor(supportsStrength:) keeps the knob off surfaces that would ignore it. LICENCE/AVAILABILITY — the queue was WRONG in our favour: it said DRUNet had only a third-party deepinv mirror needing a check; in fact the same author publishes every weight first-party in the cszn/KAIR v1.0 release under an MIT LICENSE (which also unblocks P9/SCUNet). The deepinv mirrors are NOT used — deepinv/drunet's bsd-3-clause tag is the DeepInverse LIBRARY's licence blanketed across their org and says nothing about upstream MIT; deepinv/scunet has none at all. A mirror's licence tag is not evidence about the weights it mirrors. PARITY green: S0 64 tensors / 32,640,960 params strict-clean; primitives 5/5 with the TRANSPOSED CONV BIT-IDENTICAL (0.00e+00); sigma sweep 4/4 at cosine 1.00000000 with monotonicity ASSERTED. CONVERSION TRAP: ConvTranspose2d is (I,O,k,k) in PyTorch vs conv (O,I,k,k) — different transposes, and INDISTINGUISHABLE BY SHAPE (m_down3.4 and m_up3.0 are both (512,256,2,2), opposite meanings); the converter keys on names and asserts exactly 3. Conformance 12/12 incl. four tests pinning the dial. FOOTPRINT MEASURED via ValidationHarness: floor 0.16 / peak 6.71 / act 6.56 / retain 2.49 GB @1080p; declared 200 MB / 7.0 GB. ⚠️ NO TILING — activation is LINEAR in resolution unlike the tiled siblings whose peak is flat; viable at 1080p, a 4K frame costs ~4x, so tile before claiming 4K. Weights mlx-community/DRUNet-color-fp32, round-trip verified. ⚠️ POSITIONING per the queue: a UX play, not a quality play — DRUNet is Gaussian-only in its paper, does not report SIDD, and is NOT a NAFNet replacement on quality. It is the slider. Validation needs corpus C5, which now also asks for a DARK FRAME PER ISO because of this row: something must choose sigma for a real photograph, and a dark frame's standard deviation IS sigma for that ISO, measured rather than guessed. -->
| imageRestore | mlx-gfpgan-swift | GFPGAN v1.4 — blind FACE restoration (v0.1.0) | wrapper+core | image/PROD | ✅ | 🟡 | ✅ | 0.38.0 | <!-- Porting evaluation + port 2026-07-27, PUBLISHED same day: github.com/xocialize/mlx-gfpgan-swift tagged v0.1.0; weights mlx-community/GFPGANv1.4-fp32 (fresh-download re-verified: S0-S3 all green + real-engine validate against the published bytes). SIXTH package on imageRestore (after NAFNet, FFTformer, Restormer, DRUNet, SCUNet) and the SECOND to honour the 1.30.0 strength dial (blends restored vs original aligned crop pre-paste; upstream GFPGANer(weight:) intent — the clean arch swallows it). FACE restorer, not a frame restorer: Vision detect→FFHQ 5-pt align→512² StyleGAN2-prior restore per face→feathered paste-back; no face = typed passthrough (appliedStrength nil). LICENSE: Apache-2.0 both layers, carve-outs verified clean — clean arch has no NVIDIA-derived code; v1.4 decoder prior is BasicSR from-scratch StyleGAN2 (…_scratch_800k.pth), NOT NVIDIA FFHQ weights; DFDNet clause outside inference path; residual FFHQ dataset CC-BY-NC-SA flag. Weights local converted/GFPGANv1.4 (348.6 MB fp32, params_ema, 285 tensors, NHWC; stored noise buffers included) — PUSHED to mlx-community/GFPGANv1.4-fp32 (verified present 2026-07-27, 348.6 MB / 3 files, matching this row's own byte count; the earlier 'NOT yet pushed' clause was written pre-push and never updated, contradicting this row's own opening line). PARITY green fp32 CPU: S0 285/285 strict both ways; S1 7/7 (bilinear, modconv ±demod, StyleConv, ToRGB) ≤3e-6 rel; S2 2/2 ResBlock; S3 94/94 per-stage taps (512² rand + real face), final image rel 6.6e-6; parity mode = stored noise (randomize_noise=False), package default too. DTYPE fp32 BY MEASUREMENT: fp16 COLLAPSES e2e (cos −0.3, 8 dB — high-magnitude class); bf16 clean 48 dB; ship fp32 per restoration precedent. ALIGN SEAM (#43b gate): Vision vs facexlib RetinaFace ground truth (gen_align_fixtures.py, baked offline) — 7/7 faces matched, crop IoU 0.86–0.97, landmark dist ≤0.11×interocular; crops eyeball-identical. FOOTPRINT via gfpgan-validate (real engine, ValidationHarness): floor 0.36 GB, peak 1.81 GB, act 1.45 GB, retain 0.63 GB, 1.7 s @1297×1920×2 faces; declared 400 MB/2.0 GB. Per-decoder-level eval+clearCache seams are LOAD-BEARING: without them peak 3.97 GB + ~2 GB dirty driver pages retained. Conformance 12/12 offline (MAT, CAN cadence=face, manifest, strength surface, align math). Val 🟡 = engine-driven CLI lane; in-app + corpus validation pending (same C4-corpus block as the Forge restoration batch). Gotcha for future ports: ModuleParameters.unflattened reads numeric key-path components as ARRAY indices — upstream nn.Sequential bare-index keys (style_mlp.1, condition_*.N.{0,2}) need named Swift keys + a load-time remap (GFPGANv1Clean.remapUpstreamKeys). --> |
| imageRestore | mlx-restoreformer-swift | RestoreFormer++ — blind FACE restoration (v0.1.0) | wrapper+core | image/PROD | ✅ | 🟡 | ✅ | 0.38.0 | <!-- Porting evaluation + port 2026-07-27, PUBLISHED same day: github.com/xocialize/mlx-restoreformer-swift tagged v0.1.0; weights mlx-community/RestoreFormerPlusPlus-fp32 (fresh-download re-verified: S0/S1/S3 green + real-engine validate against the published bytes). SEVENTH package on imageRestore, SECOND FACE restorer — deliberate PackageID redundancy vs GFPGAN: codebook-reconstruction (VQ-GAN + 1024×256 ROHQD dictionary + multi-scale cross-attn, conservative/fidelity-leaning) vs GFPGAN StyleGAN2 generative prior (detail-hallucinating). Honours the 1.30.0 strength dial (same package-level crop blend). LICENSE: plain Apache-2.0 both layers, NO carve-outs (cleaner than GFPGAN); weights = author official v1.0.0 GitHub release (the README Google-Drive link is stale — availability probe found the release asset); residual FFHQ dataset flag. PARITY green fp32 CPU: S0 441/441 strict; S1 7/7 components ≤3e-6 rel; S3 40/40 per-stage taps; CODEBOOK INDICES 256/256 EXACT on both fixtures (the VQ argmin is gated on exact equality, not tolerance). Fully deterministic — no noise anywhere. DTYPE fp32 BY MEASUREMENT with an INVERSION vs GFPGAN: fp16 50.1 dB VIABLE and bf16 38.6 dB (mantissa-bound, the FFTformer pattern; GFPGAN was the reverse — fp16 collapse). Late-decoder activations ±14k. FOOTPRINT via restoreformer-validate (real engine): floor 0.31 GB, peak 1.79 GB, act 1.47 GB, retain 0.67 GB, 1.8 s @1297×1920×2 faces; declared 350 MB/2.0 GB. Per-level eval+clearCache seams in BOTH encoder AND decoder are LOAD-BEARING: encoder seams alone cut act 2.98→1.47 GB. FaceAlign seam VERBATIM from mlx-gfpgan-swift (same FFHQ template + face_size=512; the Vision-vs-facexlib agreement gate lives there and covers both consumers). Conformance 12/12 offline. Val 🟡 = engine-driven CLI lane; in-app + corpus validation pending (C4-corpus block, with the rest of the restoration family). --> |
| imageRestore | mlx-scunet-swift | SCUNet (v0.1.0) — the blind one | wrapper+core | image/WIP | 🧪 | ✅ | ✅ | 0.39.0 | <!-- P9 (Forge Gap Program), 2026-07-27. FIFTH package on imageRestore, and the only genuinely BLIND one: no baked-in degradation (NAFNet/FFTformer/Restormer) and no noise-level input (DRUNet). supportsStrength FALSE and appliedStrength nil, which is the honest signal for "this backer has no dial" as distinct from "the dial did nothing" — the conformance suite asserts the descriptor keeps saying no. TWO VARIANTS, same architecture and same key set, differing only in objective: real-psnr (MSE, conservative, leaves residual noise) and real-gan (adversarial, crisper, HALLUCINATES texture — wrong pick for anything scored against a reference). Weights mlx-community/SCUNet-color-real-{psnr,gan}-fp32 (71.8 MB each), round-trip verified. LICENCE: upstream cszn/SCUNet is Apache-2.0 with zero NC text; weights first-party MIT from the cszn/KAIR v1.0 release (the same release that unblocked P5/DRUNet). Manifest declares portCodeLicense .mit to match this repo's own LICENSE; upstream's Apache-2.0 notices are retained in LICENSE-upstream + NOTICE per §4. 🔑 FIRST SHIFTED-WINDOW ATTENTION IN THE FLEET — WMSA with cyclic roll, a generated attention mask and relative-position-bias indexing; this unlocks the Swin family generally. Both of its porting errors are SHAPE-SAFE, producing a plausible image rather than an error: (1) the QKV head split — rearrange('b nw np (threeh c) -> threeh b nw np c').chunk(3, dim=0) puts ALL q heads, then all k, then all v, NOT per-head triples; (2) the SW mask masks only the LAST window row/column, the wrap-around the roll creates, and omitting it lets opposite image edges attend to each other. So the gate ladder compares the TABLES at tol 0 before anything consumes them: S1 stored relparams + gathered (heads,64,64) bias + SW mask, all five at EXACTLY 0.00e+00. PARITY green on the CPU stream: S0 540 tensors / 17,946,072 params strict-clean for BOTH checkpoints (param count asserted — upstream's constructor DEFAULTS to config=[2]*7 which no released checkpoint uses and gives 9,662,892 params); S2 WMSA W 2.36e-07 / SW 1.93e-07; S3 blocks 4/4; S4 resamplers with the transposed conv BIT-IDENTICAL (0.00e+00); S5 full model 64/128/100, worst 3.42e-06. Conformance 12/12. TWO SWIFT-SIDE TRAPS worth carrying forward: (a) Module reflection collects EVERY stored MLXArray property as a parameter — the constant relative-position gather table appeared as 28 phantom tensors and failed S0 with 28 missing keys; it is now boxed in a non-Module class and shared across all 28 blocks; (b) padToMultiple here is REPLICATION, not the reflection the sibling DRUNet port uses — shape-identical, differing only at the borders, so only the 100² golden discriminates. Also: mlx-swift's roll takes ONE shift, so the cyclic shift is two sequential rolls on independent axes. FOOTPRINT MEASURED via ValidationHarness: floor 0.10 / peak 4.48 / act 4.38 / retain 0.56 GB @1080p; declared 180 MB / 5.0 GB. ⚠️ The gate's --bench read 1.33 GB for the same tile — 3.3× UNDER, same direction and cause as every prior port; --bench is for comparing tile sizes, the harness is the admission basis. TILING MANDATORY (untiled 10.04 GB phys at 1024², LINEAR in pixels, so ~20 GB at 1080p); tile 384 / overlap 64. 🔑 BUT WINDOW ATTENTION TILES ALMOST FOR FREE: at 512² tiled at 256, overlap 64 scores 71.60 dB vs full-frame with a seam/interior gradient ratio of 1.00× (overlap 0: 58.32 dB, 1.08×) — statistically indistinguishable from ordinary image content, where Restormer's spatially GLOBAL channel attention made the same comparison a real trade. Geometry is 64-ALIGNED for CORRECTNESS not tuning: forward pads to a multiple of 64 and lays the window grid out from the tile's own origin, so an unaligned origin shifts the window phase between neighbours and leaves a seam feathering cannot remove (16/32/96 all visibly round down). ✅ V4 MEASURED: no primary source reports SCUNet's SIDD or DND — the authors skipped both deliberately — so it was measured on NIND (CC0, 5 scenes × 4 ISOs, 768² centre crops, PSNR vs the ISO100 reference; pairs verified pixel-aligned and brightness-matched first). SCUNet real-psnr is the STRONGEST BLIND DENOISER we hold: 36.39 / 34.95 / 32.28 dB at ISO 1600 / 6400 / 25600 vs an untouched baseline of 34.87 / 29.90 / 23.91, winning at 6400 and 25600 and tying Restormer at 1600 — and its margin over Restormer GROWS with noise (+0.01 → +0.17 → +0.70 dB), which is exactly the randomized-degradation claim the row was bought on. A correctly-tuned DRUNet wins at 1600 (37.36 at σ=10) but scores −2.73 dB at a wrong σ, i.e. worse than not denoising. real-gan costs 0.88–1.50 dB and is a NO-OP at ISO1600 (+0.02 dB) — a perceptual mode, never the default, and it should be gated off low-noise input. ✅ AND IT REPLACES NAFNET: the NAFNet arm was added same-day — NAFNet-SIDD-width64 is LAST among the blind denoisers at every ISO (33.39 / 31.99 / 29.38 dB, i.e. 3.00 / 2.96 / 2.90 dB behind SCUNet) despite being 6.5× LARGER at 115,982,915 params, and at ISO1600 it scores −1.48 dB — WORSE THAN DOING NOTHING. That is exactly the generalization failure this row was bought on: NAFNet trains on SIDD's five smartphone sensors and NIND is DSLR-class Canon, so being off the training sensors IS the test. 🔴 PROCESS NOTE worth more than the number: the arm had been recorded as blocked on "no PyTorch NAFNet in our oracle set" — a CATEGORY ERROR. The requirement is a verified IMPLEMENTATION, not a PyTorch one, and we hold two (xocialize/nafnet-mlx MLX-Python + xocialize/mlx-nafnet-swift, weights on mlx-community); it ran in ~20 min once posed correctly and flipped the headline from "strongest of the three we hold" to "replaces the incumbent". State the REQUIREMENT, not the artefact you happened to look for. Because NAFNet runs in MLX while the other arms run in torch, measure.py ASSERTS its key contract (664 tensors / 115,982,915 params, 0 missing / 0 unused) — a partial load would have handicapped the incumbent and flattered every challenger. ⚠️ LIMITS: a GENERALIZATION result, silent on NAFNet's in-domain SIDD performance (what a phone photo exercises); it does NOT indict mlx-nafnet-swift's shipping `.signage` default, a different width-24 checkpoint that was not measured; NIND is DSLR-class; and PSNR judges the GAN variant on the axis it deliberately trades away. Full record: mlxengine-image/corpus/nind/RESULTS.md. -->
| imageUpscale | mlx-realesrgan-swift | Real-ESRGAN 4× | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-64²-tile checkpoint in the core tile driver, CancellationError exempted from the PlaybackTierError wrap (laundering fix); cadence upsample/chunk; CAN-1..3 green; engine pin 0.27.0. Tagged v0.5.0. BUNDLED-WEIGHTS posture, first-class since 2026-07-09: all 3 SRVGGNetCompact checkpoints (~2 MB each) are vendored in the core bundle — there is NO download, ever. Engine 0.24.0 (contract 1.17) grew the bundled MAT vocabulary FOR this package (the filed gap, closed same day): BundledWeightSourcing declares the selected variant's checkpoint (role "checkpoint" → Variant.bundledWeightsURL); the gate verifies it PRESENT on a fresh machine (inverse of WeightSourcing's required-missing rule) and engine.needsDownload reads FALSE via the sanctioned bundled signal (pre-0.4.0 the store-marker heuristic read TRUE forever — the ModelSheet Studio G6 fast-upscale degradation; v0.4.0 bridged with always-present prewarmPaths). WeightSourcing stays undeclared by design (no fresh-machine network source; the HF repo also lacks the anime variant); WeightPrewarming keeps its cold-start page-in job. FULL MAT gate green per variant + e2e MLXServeEngine register→needsDownload==false. Engine pin 0.23.0→0.24.0. Tagged v0.4.1. --> |
| imageUpscale | mlx-seedvr2-swift (MLXSeedVR2) | SeedVR2-3B | wrapper+core | video/PROD | ✅ | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09 (see videoUpscale row — same package, v0.6.0): CAN-1..3 green on both surfaces. Export/diffusion tier of imageUpscale (Real-ESRGAN = fast tier; multi-package via PackageID). ONE `SeedVR2UpscalePackage` exposes imageUpscale + videoUpscale (see the videoUpscale row — SAME package, loads the 3B core once). Lives in `mlx-seedvr2-swift`, which folded `SeedVR2MLX` core in: the standalone `seedvr2-mlx-swift` core repo was ARCHIVED read-only 2026-06-15, so the EdgeTAM same-repo fold couldn't ship there — consolidation went the REVERSE of BRIDGE-VID-002's note (core folded INTO the wrapper repo, wrapper NOT retired; `mlx-seedvr2-swift` is the single live home). Born sweep-clean: QuantConfigured fp16+int8 ONLY (int4 degrades 22.7 dB); BudgetAware fp16→int8 under pressure; unload()→clearCache; mmap/lazy. Split measured via seedvr2-package-smoke @256²→512²: int8 resident 4.72 GB / MLX-peak 7.07 GB (transient ~2.35 GB); fp16 est. Activation 3.5 GB conservative (single-pass, resolution-driven); in-app phys re-baseline pending (~2.5–2.9× under). License Apache/MIT permissive. Published v0.3.0 (a45b7eb), relocated to video/PROD. Val 🟡 = CLI gate green (valid 512²); MLXEngineImage in-app run pending (owner's call — it's imageUpscale, has IMAGE_AUTORUN) for Val→✅ + phys re-baseline. Resolves BRIDGE-VID-002. --> |
| imageInpaint | mlx-lama-swift | LaMa + MI-GAN | wrapper+core | image/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); LaMa/MI-GAN each one monolithic eval — cadence encode/frame + postprocess/frame, pre-forward checkpoints after both lazy tier builds; first test target added; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. LIGHT split + unload-clearCache + QuantConfigured. Split the flat fp16 4.5 GB into resident floor 0.5 GB (inpainter weights: LaMa-bf16 ~0.1 GB on disk + MI-GAN-fp16 ~15 MB; load() holds only LaMa, MI-GAN loads on first `fast` request — both nets tiny, P2 per-stage-evict skipped as negligible) + ~4.0 GB FFC inpaint transient (measured e2e erase peak 2.8 GB fp32 @880²; total envelope stays ~4.5 GB). dtype stays bf16 (LaMa fp16-FATAL; .fp16 is the quant-tier LABEL only). unload()→clearCache. 2-input surface (image+mask) → headless autorun can't read an arbitrary mask (sandbox), so activation = smoke/derived est, in-GUI phys re-baseline pending. --> |
| matting | mlx-birefnet-swift | BiRefNet | wrapper+core | image/PROD | ✅ | ✅ | ✅ P1a (P1b deferred) | 0.36.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); monolithic single-eval forward — cadence encode/frame + postprocess/frame at the real seams (pre-forward after lazy best-tier build, pre-encode); CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. **C14 2026-07-25:** **INFERENCE-MODE DEFECT FOUND + FIXED 2026-07-25 (v0.3.0) — the motivating case for engine C14.** MLXNN.Module.training defaults to TRUE, and in that state BatchNorm normalizes by the CURRENT batch's statistics and OVERWRITES the checkpoint's running_mean/running_var every forward (MLXNN/Normalization.swift). Every BatchNorm2d in squeeze_module / ASPPDeformable / the decoder ran on per-image stats; the trained stats were never read and each forward mutated them further, so repeated calls on one instance drifted. The Swin encoder is LayerNorm-only, which is part of why it stayed invisible. INVISIBLE TO EYEBALL VALIDATION: the matte still looked like a matte while the PROD fast tier over-segmented by 68% (foreground fraction 0.42 vs the PyTorch oracle's 0.25) and e2e logits cosine vs the oracle was 0.264. Fix = one line, model.train(false) in BiRefNetPipeline.init — the single construction choke point every load path funnels through (fromPretrained, BiRefNet.segment, direct init, the package wrapper, the parity CLI) — taking cosine to 0.99999. NEW birefnet-parity executable (injects the oracle's exact normalized input, compares encoder stages + e2e logits/sigmoid; doubles as the publish-dtype selector) is what produced those numbers. C14 INF gate wired: posture .moduleGraph, and testPipelineInitIsTheInferenceModeChokePoint asserts a fresh BiRefNet FAILS INF-1 and passes after the choke point, so the regression cannot return silently. 9/9 green. Engine pin 0.27.0->0.36.0. **WeightSourcing/MAT RETROFIT 2026-07-25 (rode the Lucida sibling change):** this was the last non-MAT image package — it still downloaded via its own HubApi call inside load(), so the engine's materialization executor never ran for it (the MSS-7 fresh-install silent-degradation class). BiRefNetConfiguration now conforms to WeightSourcing (per-variant sources, quant-aware globs, missingWeightSources honoring the explicit *WeightsURL escape hatches before the store probe); the package-local HubApi download + the swift-transformers dependency are DELETED, and weightsURL() keeps only a resolve+throw offline backstop (accepting both the hub-snapshot and the engine's flat store layout). MAT-1..5 green for BOTH families. **BEHAVIOUR CHANGE worth knowing:** the .birefnet family now DECLARES both tiers, so a fresh install materializes the 2048 best checkpoint up front (~+440 MB) instead of lazily on first best request. That is the honest declaration once the package no longer downloads for itself — an undeclared tier would just fail on a fresh store. Restoring laziness is exactly what P1b (mode -> PackageID) buys, since best's weights would then only be declared when that package is registered. Tagged v0.4.0. --> |
| matting | mlx-birefnet-swift (MLXBiRefNet) | Lucida (BiRefNet_HR fine-tune) | wrapper (checkpoint swap) | image/PROD | ✅ (v0.4.0) | ✅ | ✅ | 0.36.0 | <!-- NEW 2026-07-25 (v0.4.0). SIBLING CHECKPOINT, ZERO PORT: egeorcun/lucida is a fine-tune of ZhengPeng7/BiRefNet_HR with no architecture change — converted key set byte-identical to BOTH upstream bases (754 tensors in -> 687 out, 0 keys/shapes differing), and the resolved arch config matches BiRefNetConfig.swinLargeDefault exactly (swin_v1_l, lateral [1536,768,384,192], mul_scl_ipt=cat, cxt_num=3, ASPPDeformable, BasicDecBlk_x1, BN present at batch_size=4, refine='', size=1024). So no port, no new core: BiRefNetPackage runs it via BiRefNetConfiguration.variant == .lucida. OWN PackageID (surface lucida-matting) rather than a third mode on the birefnet surface, because the manifest is per-registration — license, provenance (sourceRepo is the engine's marker key) and footprint stay separable, so a future license posture on Lucida cannot gate BiRefNet's PROD consumers (EngineMatteProvider, the TRELLIS.2 multi-view front door). Same shape as FireRed-on-QwenImageEdit and Klein base-vs-distilled. Cross-family guard: (variant, mode) -> checkpoint resolves at ONE choke point (BiRefNetConfiguration.resolved), Lucida advertises no modes and IGNORES any mode sent, so a .best request on lucida-matting cannot reach BiRefNet's HR weights; Lucida.registration also refuses a .birefnet configuration. CONVERSION: existing birefnet-convert, 687 written / 67 dropped, fp32 output BIT-EXACT vs source (NHWC transposes, conv_out1 remap, 0 all-zero tensors). Source carries 1 inf in decoder.gdt_convs_2.1.running_var (upstream training artifact, zeroes that BN channel identically in torch and MLX — matched, not 'fixed'; note patching it changed NOTHING pre-C14-fix, which was itself evidence the running stats were never being read). PARITY (new birefnet-parity CLI, vs a PyTorch oracle at 1024 with the oracle's exact normalized input injected): encoder stages CPU-stream relMax <= 8.3e-5 cos 1.0000000; e2e GPU logits cos 0.99998, sigmoid cos 0.99977, matte fg fraction 0.0292 == oracle 0.0292. NOTE the stream split is FORCED — the ASPPDeformable DCNv2 custom Metal kernel hard-fails on CPU ('[metal_kernel] Only supports the GPU'), so the encoder runs CPU-pinned and the decoder GPU with a cosine bound (GPU fp32 accumulation makes maxAbs uninformative: the same encoder tensor measures 3.96e-3 on CPU vs 7.25 on GPU while cosine holds 0.9999). Tolerances are relative-to-range (CPU) / cosine (GPU), never absolute — the Mage-Flow miscalibration lesson. DTYPE fp16 (sigmoid cos 0.999995); bf16 REJECTED at cos 0.9958 — no bf16 variant published, asserted by testOnlyFP16Published. Eff: split QuantFootprint(.fp16, resident 0.6 GB, peakActivation 14 GB) — MLX smoke measures byte-identical to the fast tier (floor 423 MB / peak 4941 MB), and that 4941 MB is the same MLX-peak whose in-app phys was re-baselined to floor 0.54 / peak 13.70 GB, so the fast tier's in-app numbers transfer by MEASUREMENT not assumption; resident is one graph (< the birefnet package's 0.9 GB two-tier floor). QuantConfigured + unload->clearCache + engine-executed materialization + the C14 train(false) choke point all inherited. 18 new offline tests (MAT-1..5 both families, Codable back-compat for pre-variant configs, the cross-family mode guard, manifest identity, CAN-1..3 on the new PackageID); 27/27 green. **IN-APP VALIDATED 2026-07-25 (Val 🟡→✅)** via the NEW consumer **BiRefNet Demo** (mlxengine-image/PROD/Birefnet — HF-Space-parity UI vs egeorcun/lucida-demo, bundling that Space's own four hard samples, plus a Compare view across all three checkpoints that the web demo cannot offer; styled from MLXEngineUI MarqueeTokens = the MLXEngine Figma token sheet). Golden-path consumer: store-before-register, both families registered under their own PackageIDs, ModelStateView bound to engine.preparation, prepare-before-run, EngineSettingsView reused. Driven live end-to-end: folder grant → register → needsDownload true → COLD 440 MB materialization + load in ~20 s → valid matte on the glass sample (true soft alpha; glass and water stream see-through against the checkerboard); 10.81 s / phys 2.48 GB / matte mean 0.070 / fg 6.2%. The birefnet PackageID was exercised in the same session (real download phases surfaced — 'Downloading 90% · 27.2 MB/s' with a progress bar, then Ready; 4.40 s / phys 3.08 GB / mean 0.093 / fg 9.5%), and preparing it materialized BOTH declared tiers on disk (420 MB each) though only fast loaded — the eager-declaration change above, confirmed empirically. CAVEAT: those seconds are Debug -Onone; the Release CLI measures 0.38 s for the same forward (GPD-2 — quote Release numbers). Remaining: a real-corpus A/B vs birefnet on our own images before Lucida becomes any default. CONSUMER FINDING fixed in the demo (commit 9d939c1): routing first run only through the settings sheet made setup depend on that sheet's layout — at 560 pt wide EngineSettingsView clipped its own Apply button, so a user could pick a folder and stay stuck on 'Choose a models folder to begin'. A sandboxed app can't type its way to a usable store (only the panel's security-scoped grant works), so the grant is now a first-class main-window affordance. WEIGHTS PUBLISHED 2026-07-25: **mlx-community/Lucida-fp16** (fp16 440 MB, 687 tensors; card documents the conversion steps, the fp16-vs-bf16 parity result, the Lucida-vs-BiRefNet routing split with its benchmark caveat, the un-ported colour-decontamination/edge-refinement passes, and the C7 training-data provenance note). PUBLISHED-ARTIFACT VERIFIED (the LaMa lesson — a broken dtype-rounded upload builds and parity-passes locally): fresh hf download → sha256 identical to the local file → parity on the DOWNLOADED bytes sigmoid cos 0.99999505 / fg 0.0292 == oracle, and a real engine smoke run (register→run→decode, 0.38 s, valid matte, floor 423 MB / peak 4941 MB). C7 CAVEAT: upstream declares MIT for weights and code and that is what the manifest declares, but the card says training data 'mix[es] MIT-licensed and research-only datasets' (P3M-10k, COD10K, DIS5K, + CC-BY-4.0 ToonOut). A data-derivation question rather than a defect in the grant we received, but the same shape as the MobileWan C7 finding — legal read owed BEFORE republishing derived weights under the mlx-community org, and before this becomes a shipping default rather than an opt-in tier. POSITIONING (author's own 203-image MAE benchmark, directional not decisive): Lucida leads overall 0.0257 vs 0.0346 and on camouflage 0.0270/0.0752, design 0.0235/0.0544, text 0.0091/0.0207, illustration 0.0092/0.0157, transparency 0.0358/0.0687; BiRefNet-HR still wins hair 0.0048/0.0093, thin 0.0196/0.0322, complex 0.0385/0.0484 — so route photographic hair work to birefnet, illustration/text/hard-edge work here. NOT ported: the author's colour-decontamination + edge-refinement passes (GitHub, not the checkpoint) — this package emits raw sigmoid alpha. --> |
| promptSegment / trackObject | mlx-edgetam-swift | EdgeTAM (SAM 2) | wrapper+core | image/PROD | ✅ (v0.4.0) | ✅ | ✅ (activation = est, phys re-baseline pending) | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint was ALREADY first; pre-existing per-frame track checkpoint declared generate/frame (+ postprocess/frame for promptSegment) — honest cadence over the available exemption; CAN-1..3 green both surfaces; engine pin 0.27.0. Tagged v0.4.0. --> |
| imageQualityScore | mlx-siglip2-iqa-swift | SigLIP2 NR-IQA | wrapper+core | image/WIP | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); one small batched forward — subSecondRuns exemption; CAN-1..3 green; engine pin 0.27.0. Tagged v0.3.0. --> |
| opticalFlow | mlx-sea-raft-swift | SEA-RAFT | wrapper+core | image/WIP | ✅ | ✅ | ➖ | 0.36.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); RAFT refinement loop is LAZY graph construction (one eval — per-step checkpoints would be decorative) so subSecondRuns exemption with rationale, post-eval checkpoint added; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. **C14 2026-07-25:** C14 INF gate 2026-07-25 (v0.5.0): posture .moduleGraph. CHOKE-POINT REFACTOR — train(false) moved from SEARAFTOpticalFlowPackage.load() into SEARAFT.loadWeights(from:). SEARAFTMLX is a public .library product, so `SEARAFT(.s)` + `loadWeights(...)` is a fully supported path that previously left the model in TRAINING mode; the feature encoder (ResidualBlock bn1/bn2/bn3, BasicEncoder bn1) is BatchNorm-dense, so that path never read the trained statistics. Setting it in the loader makes it the one choke point. Tests need no download — they round-trip the model's own parameters through a temp safetensors, so loadWeights is exercised for real; testPublicLibraryPathGetsInferenceMode pins the regression. 5/5 green. Engine pin 0.27.0->0.36.0. --> |
| imageTo3D | mlx-trellis2-swift | TRELLIS.2 | wrapper+core | mlxengine-3d/WIP | ✅ (successor) | ✅ | ✅ | 0.31.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() (was validation-first); FlowSampler per-denoise-step bail across all 4 diffusion stages + SparseVAEDecoder per-block bails + pipeline stage-seam early-outs (seams placed BEFORE shape-sensitive consumers — partial feats would crash flexiPost / trip coords precondition), wrapper rethrows unchanged; CAN-1..3 green (denoise/step + decode/layer); engine pin 0.12.0→0.27.0 (mlx-swift exact:→from: required — engine floors 0.31.5); first-ever test target added. Tagged 0.4.0. WeightSourcing retrofit 2026-07-09 (was pre-MAT, field-issue MSS-7 — fresh installs silently degraded instead of downloading): single "snapshot" source xocialize/trellis2-mlx (~15 GB consolidated, globs safetensors+json+LICENSE/NOTICE/DINOv3_LICENSE.md); missing-probe = STRICT all-10-files (loadConsolidated tolerates absent tex/cascade files, so a lenient probe would silently degrade textures/tiers on a half-materialized snapshot); weightsRootOverride escape hatch honored first (flat or org/name layout, never networks); load() via WeightMaterializer (HubClient) w/ .downloading progress; prewarm store-resolved; upstream 3-repo + HF-cache chain stays a load()-time fallback only (NOT declared/probed — keeps MAT-4 honest; store-rooted machines with cache-only weights re-download once, fleet-accepted). Repo UNGATED 2026-07-09 (same day): re-framed as a PIPELINE artifact (converted DINOv3 conditioner integral; card redirects DINOv3-seekers to Meta's repo; terms-respect = license-with-artifact + C7 gate + "Built with DINOv3" app caption) — token-less first-run materialization verified, no HF_TOKEN needed. MAT-1..5 green + layout/partial-snapshot tests. Tagged 0.5.0. **IN-APP VALIDATED 2026-07-10 (Val 🟡→✅)** — ran live on GPU through the sandboxed MLXEngine3D app (register→prepare→run→decode), valid textured GLB (POSITION+COLOR_0) from engine_input.png. Consumer gotcha logged: Xcode Debug Metal API Validation asserts `setBytes: bytes argument cannot be nil` on MLXSparseConv3d's zero-size gather — disable Metal API Validation (or env `MTL_DEBUG_LAYER=0`); runs clean without it (CLI never had the validation layer). **SUCCESSOR RE-PORT 2026-07-14 (this row now tracks the NEW verified Swift-MLX re-port, NOT the 0.5.0 vertex-color port — that one was renamed `xocialize/mlx-trellis2-swift-old`; the successor took the name):** full oracle-parity single-image→textured GLB — DINOv3 ViT-L/16 → SS dense DiT + shape/tex SLat sparse DiTs (1.3B ea) → SS decoder + FlexiDualGrid VAE decoders → o_voxel dual-grid mesh + PBR base-color bake → **UV-textured** GLB (base-color texture atlas, NOT vertex colors). Package at `mlxengine-3d/WIP/mlx-trellis2-swift` (github.com/xocialize/mlx-trellis2-swift), URL deps, 18/18 offline gates. **Multi-view conditioning** (contract **1.21.0**, engine **v0.31.0**): `ImageTo3DRequest.additionalViews` — per-view DINOv3 tokens concatenated to `[1,K·N,D]` (oracle get_cond); one-button turnaround front door (klein + BiRefNet → 3 matted T-pose views → multi-view TRELLIS, auto-piped in-app). **'1024' tier:** SS decoder emits 64³ occupancy, so shape/tex run resolution-matched `shape_flow_1024`/`tex_flow_1024` (not the off-distribution 512 models) with dual-res DINOv3 (cond_512 for SS, cond_1024 for shape/tex — DINOv3 is patch-grid-agnostic); true `1024_cascade` (decoder.upsample coarse-to-fine) deferred (diminishing returns at 1024). Weights **xocialize/trellis2-mlx republished with ORIGINAL upstream keys** (renamed by filename only, ungated PIPELINE artifact, "Built with DINOv3"). **IN-APP VALIDATED 2026-07-14:** front door → neutral-color UV-textured mesh (Elena golden + tactical char), fixed a multi-view blue/dark regression (was: additionalViews silently ignored) + a 512-on-64³ off-distribution model mismatch; oracle A/B confirmed multi-view parity. Engine pin 0.27.0→**0.31.0**. **Eff ✅ (2026-07-14):** vendored mesh+xatlas in-tree → self-contained, consumed as a REMOTE URL dep (v0.7.1); split footprint MEASURED via in-app phys_footprint on the 1024 multi-view front door — real process peak **42.05 GB** (baseline 11.18 GB) ⇒ QuantFootprint resident 21 GB + peakActivation 18 GB (was a 16 GB estimate). Runtime note: 1024 multi-view (3 views, ~12.3k concatenated cond tokens) ~753 s/gen — heavy. --> |
| meshRig | mlx-skintokens-swift | SkinTokens | wrapper+core+rig+gltf | mlxengine-3d/WIP | 🧪 | ✅ | 🔵 | 0.30.0 | <!-- STAGE 2 COMPLETE 2026-07-10 (engine-wrap; LANDED on main + tagged v0.30.0). NET-NEW capability `meshRig` (contract 1.19.0, additive — the stt branch reserved the 1.19.0 slot; meshRig landed second on top of stt's 1.20.0/v0.29.0, as engine release v0.30.0): a 3D Mesh (GLB) → same geometry + skeleton + skin weights (JOINTS_0/WEIGHTS_0). First surface whose INPUT and OUTPUT are both Mesh (imageTo3D only outputs one); reuses CanonicalOutput.mesh. Two modes on the surface (C4): `auto` (generate skeleton+skin, any mesh e.g. a TRELLIS.2 output) · `skinOnly` (skin the input GLB's OWN skeleton — the companion-character VRM path, J-in==J-out). Contract edits: Capability.meshRig + canonicalOutput→.mesh, MeshRig.swift (Request/Response/Contract, optional skeleton input per imageInpaint 2-input precedent), ParameterSchema.Kind.mesh (first mesh-typed INPUT), meshRig∈longRunCapabilities. 4-target Swift pkg: SkinTokensCore (ported MLX blocks — Michelangelo pt-encoder, SkinVAE FSQ[8^5] chunked decoder, Qwen3-0.6B backbone + KV-cache incremental decoder, grammar-constrained batched beam(10), KDTree skin propagate, GLB inject) + SkinTokensGLTF (VENDORED dependency-free GLTFKit from xocialize-convert-to-usdz — world-space VRM parse, existing-skeleton, humanoid bone map) + SkinTokensRig (engine-free VRMLoad + SkinTokensPipeline, shared by CLI gates + conformer) + SkinTokensKit (SkinTokensPackage conformer). Stage-1 numeric parity green: S1b michelangelo cos .9994/.9996 · S1c skinvae-cond .9998 · S1d FSQ .99999 + skin_pred .9999 · S2 Qwen3 teacher-forced .9998 (inject captured inputs, NOT seed-match — CPU gen non-reproducible). S2b beam decoder: validation-by-construction (DFA mask → 0 grammar violations by construction; emitted skin decodes to rowsum-1 0-unweighted; aggregate stats ≈ P0 beam10). C7 weights MIT + C8 port MIT ⇒ default .permissiveOnly ADMITS (Qwen3-0.6B backbone Apache-2.0, inside ckpt). Specialties mesh-rigging + character-rigging. Born clean: MEASURED split (RunSkinTokens --measure, M-series GPU) resident weights floor 1.19 GB + peakActivation max-over-modes — AFTER the efficiency sweep — **4.08 GB** (skinOnly VRM J=141 worst case; auto giraffe ~2.5 GB) → QuantFootprint(.bf16, resident 1.2 GB, peak 4.2 GB) ⇒ fits an **8 GB** machine. Eff levers (2026-07-10, each gen-non-parity-safe, parity gate keeps fp32; net 8.21→4.08 GB / 50%, rig quality preserved J-in==J-out 0-viol rowsum-1): prefill B=1 then tile KV to numBeams (O(T²) prefill runs once) + point-BATCHED skin decode (SkinVAE.decodeBatched — exact, query pts don't interact) + **bf16 KV cache** (Qwen3.Config.kvDtype, compute fp32, biggest lever halving the ~2.75 GB beam cache). CAN: entry checkpoint first act of run() + per-TOKEN checkpoint in beam loop (SkinGen.generate rethrowing progress) + stage seams; CAN-3 generate/token; offline C0-C13 static + CAN-1..3 + MAT-1..5 XCTest suite GREEN (44 tests total). VALIDATED (Val 🟡 = engine-CLI, not in-app): drove giraffe THROUGH MLXServeEngine register→prepare(0.2s/charged 1.60GB)→run(32.1s)→valid rig (J54 rowsum 1.000 0-unweighted, roundtrips analyze_rig.py); both auto (giraffe→new-skeleton rig) + skinOnly (VRM 141-joint→skin, real VRoid bones preserved). Eff 🔵: split footprint measured+declared + 3 activation levers applied (prefill-B1-tile + point-batched decode + bf16-KV → 50% cut, 8 GB-capable); mmap/BudgetAware/fewer-skinOnly-beams pending. S6 int4-WEIGHT-quant deferred LOW-VALUE (1.19 GB weights, activation-bound). P1.5 voxel-gate deferred (P0-flagged low-value geodesic port; raw skin already rowsum-1/0-unweighted/mean-4). LANDED 2026-07-10: meshRig merged to main + tagged v0.30.0; SkinTokens path-dep reverted to URL from:0.30.0, dev worktree removed, 44/44 tests green against the published engine. WEIGHTS PUBLISHED 2026-07-10: **mlx-community/SkinTokens-bf16** (public — config.json + tokenrig.safetensors bf16 + skinvae.safetensors f32 + skeleton_vroid.json + README + MIT LICENSE; community naming `<Name>-<precision>`, bf16 = the loaded tokenrig dtype; WeightSourcing.consolidatedRepo now points here, the 4-file probe resolves against it). Fresh-machine proof: snapshot_download → engine drive → valid rig. REMAINING DEBT: in-app validation (MLXEngine3D target). S6 int4-weight-quant + P1.5 voxel-gate deferred LOW-VALUE (activation-bound tiny weights; P0-flagged geodesic port). plan=mlxengine-3d/DEV/SkinTokensDev/{PORTING-SPEC,HANDOFF,PLAN}.md. **IN-APP VALIDATED 2026-07-10 (Val 🟡→✅)** — first in-app run (the named remaining debt): drove both modes live on GPU through the sandboxed MLXEngine3D app. auto (giraffe) → skin[0] 54 joints + IBM + JOINTS_0/WEIGHTS_0; skinOnly (humanoid_vroid) → 141-joint VRM skeleton PRESERVED (J-in==J-out) + valid skin. Both GLBs load in Quick Look. Package still WIP (unpublished — no github repo yet); consumed by MLXEngine3D via local path + workspace member. --> |

> BiRefNet `Eff: ✅ P1a` (2026-06-30, engine 0.14.0) — the motivating same-quant multi-mode case (fast@1024 vs best@2048). Split declared on the **fast** envelope: `QuantFootprint(.fp16, resident 0.9 GB, peakActivation 14 GB)` (+ `QuantConfigured`), replacing the flat 6.5 GB → engine charge ~0.9 GB resident + a shared transient. best stays a runtime-guarded variant (`insufficientMemoryForBest`, guard 48 GB; peakActivation ~47.8 GB, documented not admitted). **RE-BASELINED 2026-06-30** to in-app process `phys_footprint` (fast 13.7 / best 47.8 GB peak) after the MLXEngineImage acceptance run — the birefnet-smoke MLX-peak (4.9/18.3) under-read the real admission basis ~2.7× (MLX cache + overhead); guard 19.5→48 GB stops best falsely admitting on 32 GB Macs. **NB the optimizer trio + other smoke-measured packages likely share the same MLX-peak-vs-phys under-read — re-measure in-app.** **P1b deferred** — promoting mode → PackageID (best first-class admitted) is a coordinated change; the PROD consumer (`EngineMatteProvider`) relies on per-request `req.mode` + the fallback.

> Qwen-Image-Edit-2511 `Eff: ✅` (2026-06-30, engine 0.15.0) — the heaviest image package, three wrappers (`MLXQwenImageEdit` base · `MLXQwenImageEditTurbo` bf16/int4 · `MLXTeleStyle`) over one shared core (`QwenImageEditGenerator`). **P2 (headline) = per-stage encoder eviction:** the ~16.6 GB Qwen2.5-VL encoder, used once per request then idle through denoise+decode, is no longer held resident — the core generator owns an async `encoderProvider`, loads it, encodes, `eval`s the embeddings, then drops the ref + `Memory.clearCache()` before the DiT denoise peak (Swift 6: the now-async `generate`/`loadEncoder` take `isolated (any Actor)? = #isolation` to stay on each wrapper's `@InferenceActor`; back-compat `keepEncoderResident` init for parity tests). All three wrappers inherit it; the DiT (with its resident LoRA swapper) + small fp32 VAE stay resident. **Split (measured M5 Max, 1024²/8-step CFG4, `QIE_MEMBENCH` in MLXQwenImageEditTests):** bf16 resident floor (DiT 40.9 GB + fp32 VAE 0.5 GB) **41.4 GB**, worst peak **59.2 GB** → activation (peak−floor, the transient encoder load) **17.9 GB → 21 GB** declared (+20%). base/TeleStyle `QuantFootprint(.bf16, resident 42 GB, peakActivation 21 GB)` replaces the flat 60 GB; Turbo adds `(.int4, resident 12 GB, peakActivation 17 GB)`. All three configs now conform to `QuantConfigured` (Turbo's `quant` is computed bf16/int4). P3 (mmap): verified — the measured floor equals on-disk DiT+VAE bytes exactly, so the per-key `asType` rebuild does NOT eager-copy. P4 (BudgetAware): deferred (quant is config-chosen). **FLAG:** the int4 split is DERIVED from the documented component measurements (pre-split resident 22 GB included the co-resident encoder); the bf16 split is freshly measured — re-measure int4 via `QIE_MEMBENCH` with `quantizedDiTPath`+`quantizedEncoderPath` once the image category app is stood up for app-autorun.

> EdgeTAM `Eff: ✅` (2026-06-30, engine 0.17.0) — light split + `unload()` clearCache (batch #6, non-image/video stack). Single fp16 checkpoint (54 MB on disk), two surfaces (`promptSegment` image + `trackObject` streaming video) over one encoder+decoder. Flat `QuantFootprint(.fp16, 1.5 GB)` → **split** `QuantFootprint(.fp16, resident 1.0 GB, peakActivation 0.5 GB)`; config now conforms to `QuantConfigured`. residentBytes = the RepViT encoder+FPN+decoder weights floor held resident through the per-frame loop; peakActivationBytes = one frame's encode+decode transient. **P2 N/A** — the encoder IS the per-frame core (not an evictable upfront stage); the video path already runs `MLX.GPU.clearCache()` per frame (EdgeTAMVideo.swift:411) so streaming stays flat (~1.0 GB + 0.37 MB/frame) — kept, not regressed. `unload()` now `MLX.Memory.clearCache()` after niling predictor/videoPredictor. mmap/lazy: noted — the core `fromPretrained` does `.mapValues{ asType }` (eager) but it's a 54 MB no-op cast (already fp16), left as-is. **Smoke green** (promptSegment, 960×540 synthetic, fp16): valid binary matte, score 0.945, MLX-peak **0.41 GB** (matches documented image-mode 0.42). **FLAG:** activation = smoke/derived est — in-app process `phys_footprint` (R-MEM-1/admission, ~2.5–2.9× a smoke MLX-peak) re-baseline pending (promptSegment via MLXEngineImage, trackObject via the video app).

> **Optimizer-family footprint measurement, 2026-07-27 (engine 0.38.0).** FFTformer, HVI-CIDNet and Restormer were measured through the REAL `MLXServeEngine` with `MLXEngineTestKit.ValidationHarness` — the same code path and process-`phys_footprint` metric the archived MLXEngineImage app used (150 ms sampling; floor read **post-load / pre-run**). Each package now ships a `*-validate` executable, so the number is reproducible rather than a one-off. All three at 1920×1080:
> ```
> [fftformer-gopro]        SPLIT floor=0.10GB peak=5.80GB act=5.70GB retain=0.33GB engine=0.12GB reserve=8.00GB load=0.0s run=34.6s
> [cidnet-generalization]  SPLIT floor=0.04GB peak=5.87GB act=5.84GB retain=2.66GB engine=0.06GB reserve=3.00GB load=0.0s run= 2.9s
> [restormer-motionDeblur] SPLIT floor=0.14GB peak=4.90GB act=4.76GB retain=0.36GB engine=0.15GB reserve=4.50GB load=0.1s run=20.7s
> [drunet-color]           SPLIT floor=0.16GB peak=6.71GB act=6.56GB retain=2.49GB engine=0.20GB reserve=6.50GB load=0.0s run= 1.6s
> [scunet-real-psnr]       SPLIT floor=0.10GB peak=4.48GB act=4.38GB retain=0.56GB engine=0.18GB reserve=2.00GB load=0.0s run= 4.9s
> ```
> ⚠️ **The residual caveat these rows share**, and the reason every one declares with margin: a CLI process carries no AppKit/Metal-view overhead, so absolute `floor` and `peak` sit a few hundred MB BELOW a GUI app's — conservative in the *wrong* direction for admission. `activation = peak − floor`, the dominant term for a full-frame restorer, is process-shape independent and is the number to trust.

> 🔴 **The finding that justified the exercise: CIDNet's estimated 3.0 GB activation was UNDER-declared by ~2× against the measured 5.84 GB** — the dangerous direction, since under-declaring falsely admits on tight Macs (the BiRefNet-best failure mode). Restormer's `--bench`-derived 4.5 GB was also slightly under the measured 4.76 GB. FFTformer's 8.0 GB was over-declared and has been tightened to 6.5 GB. All three now declare **above** measured, with margin: a CLI process carries no AppKit/Metal-view overhead so absolute floor/peak sit a few hundred MB below a GUI app's, which is conservative in the wrong direction.
> ⚠️ **CIDNet does NOT tile** — it runs the full frame, which is why a 7.9 MB model shows 5.84 GB of activation and a 2.66 GB post-run retain (vs the siblings' ~0.35 GB). Tiling it the way FFTformer and Restormer are tiled is the obvious next lever. Its measurement also deliberately feeds a DARK image: a mid-grey one trips the luma gate and would measure a footprint with no model in it.

> Optimizer image trio `Eff: ✅` (2026-06-30, engine 0.15.0) — NAFNet (`imageRestore`) · Real-ESRGAN (`imageUpscale`) · DDColor (`imageColorize`), the ForgeOptimizer chain alongside BiRefNet matting. All three are single-component (P2/P3 n/a) and activation-dominated, so the win is the **split + the shared transient reserve**: each now declares a small resident weights floor + its activation peak (measured via each package's own smoke target through `MLXServeEngine` at the documented envelope), and all conform to `QuantConfigured`. Floors/activation: NAFNet signage **64 MB / 2.0 GB** (fp16) + width64 **512 MB / 2.9 GB** (fp32) @1024² · Real-ESRGAN **32 MB / 2.2 GB** (fp32, tile-bounded — 1024² peaked below 512²) · DDColor **512 MB / 1.8 GB** (fp16, best). Finding: the old flat NAFNet-fp16 0.6 GB and Real-ESRGAN 1.0 GB **under-declared** the real activation peak; the split right-sizes the charge AND frees it into the single shared reserve, so the trio + BiRefNet co-reside on ~1.1 GB of weights (64+32+512+~900 MB) sharing ONE ~4.4 GB transient (BiRefNet fast is the max) instead of each baking activation into residency. BudgetAware deferred on all three (validated runtime dtypes, no in-variant quality/memory lever).

## 🎬 Video

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| textToVideo | **ltx-2-mlx-swift** | Lightricks LTX-2.3 | wrapper+core | video/LTX_DEV (research) | 🔬 | ✅ | ✅ **+tiers** | 0.32.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint moved to first act of run() (was notLoaded-first — even the program's proving package had the CAN-1 drift); per-denoise-step + per-VAE-chunk checkpoints pre-existing and RunProgress-evidenced (DenoiseLoop.swift:75/119, VideoVAE.swift:93), encoder seams declared; CAN-1..3 green; engine pin 0.25.0→0.27.0. Commit c1c961f, no tag (research repo). **ENGINE-EXECUTED MATERIALIZATION ADOPTED 2026-07-23 (engine 0.27.0→0.32.0 / contract 1.24.0, tagged v0.1.1):** package-local WeightMaterializer — **the ORIGINAL of the four per-package copies** — deleted along with the load()-side materialize call; load() keeps the store-root guard and cfg.resolved(storeRoot:) since directory resolution is still the package's job, only the download moved. swift-huggingface **stays**: Sources/LTX2/GemmaEncoder.swift is a second consumer (#huggingFaceLoadModel stack), so unlike the three image repos the dep was not materializer-only. The transitional ../mlx-swift-lm path-dep is untouched and unaffected — the engine bump resolves mlx-swift 0.31.6, inside the existing upToNextMinor(0.31.3) range. MAT store-layout fixtures de-staled (paths from ModelStore.directory(for:) — literals predated the 1.22.0 models--org--name layout). Build + MAT + CAN green; CLI parity gates untouched (no numerics in this change). --> |

> LTX `Eff +tiers`: memory-tier profiles landed (LOW-TIER-PLAN complete) — compact24 15.4 GB / balanced32 16.1 / standard64 37.5 / max128 92.2 measured peaks; 24 GB Macs supported, 16 GB declared ineligible.
| textToVideo | helios-mlx-swift | Helios (Wan) | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); FIXED `try? onChunk` cancellation swallow (documented per-chunk cancellation never worked) — Task.isCancelled bails per AR chunk/DMD step + per decode chunk, throwing post-generate rethrow; CAN-1..3 green; engine pin 0.27.0. Commit 3e21af1, no tag (research). --> |
| textToVideo | ti2v-5b-mlx-swift | Wan TI2V-5B | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); denoise/step onStep now on ALL paths (t2i gained pass-through onStep) + vae22 decodeStreaming22 per-chunk bail (wan-core b791b62, local-path pin); CAN-1..3 green; engine pin 0.27.0. Commit 287f711, no tag (research). --> |
| textToVideo | phantom-wan-mlx-swift | Phantom-Wan | wrapper+core | video/WAN_DEV (research) | 🔬 | ⬜ | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); S2V sampler per-step onStep + decode/chunk bail with post-core rethrow; CAN-1..3 green; engine pin 0.27.0. Commit 5287b77, no tag (research). --> |
| textToVideo / textToImage | **animegen-mlx-swift** (MLXAnimeGen) | AnimeGen-T2V (Wan2.2-A14B fine-tune) | wrapper (reuses BerniniR core) | video/WAN_DEV (research) | 🧪 | 🟡 | 🔵 | 0.27.0 | <!-- STAGE 2 (Phase 3) 2026-07-13. Anime-specialized T2V — a full fine-tune of Wan2.2-T2V-A14B → WEIGHTS-SWAP on the Bernini-R/wan-core substrate, NOT a new port. Fills the anime-VIDEO gap Anima (NC + image-only) leaves open; Apache-2.0 (AIdeaLab/GENIAC). Own MLXAnimeGen package (per "one model = one package") depends on BerniniR (pipeline/denoiseT2V reused verbatim) + MLXBerniniR (frame→PNG/MP4 encoders made public) + WanCore. Delta = the two fine-tuned experts (converted 1:1 into Bernini's 1095-key layout via scripts/convert_animegen.py — original-Wan keys → sanitize only, no diffusers premap; key-contract assert both experts) + AIdeaLab's fixed recipe (CFG-free euler · 8 steps · shift 3.0) with lightx2v 250928 Lightning LoRA merged OFFLINE [high 2.0, low 1.0] (scripts/merge_animegen_lightning.py, kohya). Core gained a general `shift` override on BerniniPipeline.t2v/t2i; RunBernini gained a dev-only `--animegen` flag. Surfaces: textToVideo + textToImage (t2i = single-frame, anime-still corpus harvest). Specialties anime 0.95 / general 0.3. Split footprint: int4 MEASURED 42.1 GB peak @480p/17f/8-step (resident 28 + peakActivation 18 declared) — fits a 64 GB Mac; bf16 estimated (resident 58 + peak 26, re-measure Phase 4). C7 weights Apache + C8 port Apache ⇒ permissiveOnly ADMITS. CAN: entry checkpoint first act of run() + per-denoise-step throwing onStep (t2v/t2i) + wan-core streaming-decode per-chunk bail + post-core rethrow; offline conformance GREEN (4 tests: CAN-1..3 pre-cancelled+cadence, license/surfaces/footprint, Codable). Builds offline against MLXToolKit (pinned to Bernini's Package.resolved — swift-jinja 2.3.6, NOT 2.4.0 which breaks swift-transformers 1.3.3). VALIDATED: the converted int4 checkpoint renders coherent high-quality anime via RunBernini --animegen (Phase 2, decoded-frame eyeball, temporal coherence across 17f). WEIGHTS PUBLISHED 2026-07-13: T2V mlx-community/AnimeGen-T2V-A14B-Lightning-{int4(28.6GB,3cbba73),bf16(69GB,d2db574)} + I2V mlx-community/AnimeGen-I2V-A14B-Lightning-{int4,bf16}. **I2V surface DONE 2026-07-13**: AnimeGenConfiguration.i2vInt4/.i2vBf16 (Wan2.2-I2V-A14B fine-tune, in_dim=36) — textToVideo with `initImage` routes to the channel-concat i2v path (encode first-frame → z_video(16) + mask(4) → 36-ch model input via BerniniPipeline.i2v; CFG-free 4-step/shift-3.0). Core edits: BerniniR `denoiseT2V(yCond:)` + BerniniI2V.swift + RunBernini --i2v; FIXED denoiseT2V precondition (vaeZDim not inDim) + FrameDecode/RunBernini y-flip (was upside-down). VALIDATED via Swift (frame0 reconstructs cond image + coherent motion, int4 peak 43GB; Python oracle unusable — watchdog). REMAINING: Val 🟡→ in-app register→prepare→run (Phase 4, DELEGATED to Xcode agent) + measured footprints. Package WIP (local path, no github repo yet). plan=DEV_ARCHIVE/bernini-r-mlx + memory animegen-t2v-port. --> |
| videoEdit | bernini-r-mlx-swift | Bernini-R | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-denoise-step throwing onStep in all t2v/t2i/r2v/v2v/rv2v paths + wan-core streaming-decode per-chunk bail with post-core rethrow; CAN-1..3 green; engine pin 0.9.1→0.27.0. Local commit c28710f (branch phase-b-wan-core, no upstream), no tag. --> |
| videoEdit | vace-mlx-swift | VACE (Wan) + VACE-Fun-A14B | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() in BOTH MLXVACEPackage + MLXVACEFunPackage; shared surface runners per-denoise-step onStep + decode/chunk bail; CAN-1..3 green per PackageID (4 tests); engine pin 0.27.0. Local commits d422afe (pre-existing Fun-A14B WIP, labeled) + 15c155a (CAN) — NOT pushed (would publish the WIP); push when Fun-A14B is ready. --> |
| videoEdit | chronoedit-mlx-swift | ChronoEdit | wrapper+core | video/WAN_DEV (research) | 🧪 | ⬜ | ⬜ | |
| characterAnimation | scail-2-mlx-swift | SCAIL-2 | wrapper+core | video/WAN_DEV (research) | 🔬 | 🟡 | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); segmented DPM++ per-step onStep + decode/chunk bail with post-core rethrow; CAN-1..3 green; engine pin 0.27.0. Commit 809cc26, no tag (research). --> |
| talkingHead | musetalk-mlx-swift | MuseTalk 1.5 | wrapper+core | video/PROD | ✅ | ✅ | ✅ (activation = est, phys re-baseline pending) | 0.36.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-generated-frame checkpoint pre-existing in the UNet+VAE loop + pre-MP4-encode seam; cadence generate/frame; NEW MLXTalkingHeadTests target (repo had none) with CAN-1..3 green; engine pin 0.27.0. Tagged v0.9.0. **C14 2026-07-25:** C14 INF gate 2026-07-25 (v0.10.0): posture .moduleGraph. CHOKE-POINT REFACTOR — train(false) moved from the TWO BiSeNet() construction sites (TalkingHeadPackage + musetalk-cli) into MuseTalkWeights.load. BiSeNet is the BatchNorm carrier and produces the blend mask pasted over EVERY generated frame, so per-frame batch stats would have segmented each frame by its own statistics and drifted across the clip — the kind of defect a plausible-looking video hides. UNet/VAE are GroupNorm-only and unaffected, but the loader sets it uniformly so a future BatchNorm-carrying component doesn't have to remember. 5/5 green. Engine pin 0.27.0->0.36.0. --> |
| videoUpscale | mlx-seedvr2-swift (MLXSeedVR2) | SeedVR2-3B | wrapper+core | video/PROD | ✅ | 🟡 | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() before two-surface dispatch; per-frame (video) + ADDED per-diffusion-tile (frame refiner) and pre-forward (single-eval image path) checkpoints; cadence upsample/frame + upsample/chunk; CAN-1..3 green on both surfaces; engine pin 0.27.0. Tagged v0.6.0. BRIDGE-VID-002 fold LANDED 2026-06-30 (a45b7eb): videoUpscale is now a 2nd SURFACE on the SAME `SeedVR2UpscalePackage` as the imageUpscale row above (one model, two surfaces; loads the 3B core once). Streamed tile-refined per-frame path on frame-stream-native/NativeFrameStream. Correction to the original decision: the consolidation folded the archived `seedvr2-mlx-swift` core INTO `mlx-seedvr2-swift` (which stays as the single live home) — the WIP wrapper repo was NOT retired; it IS the home. Born-clean levers + footprint shared with the imageUpscale row. Val 🟡 = CLI NativeFrameStream round-trip green; live GPU video run + MLXEngineVideo pending. --> |
| frameInterpolate | mlx-rife-swift (MLXRIFE) | RIFE 4.25 | wrapper+core | video/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run() (was validation-first); per-source-frame checkpoint pre-existing in the NativeFrameStream transform; cadence generate/frame; CAN-1..3 green; engine pin 0.27.0. Tagged v0.8.0. Consolidated repo (RIFEMLX core folded in 2026-06-18; standalone `rife-mlx-swift` core ARCHIVED read-only, local clone quarantined to `mlxengine-video/_ARCHIVED-cores-quarantine/`). The 2026-06-18 pass moved to frame-stream-native but left engine pinned 0.8.0 → bumped to 0.17.0 (tag v0.4.0). Born-clean hygiene applied though Eff stays ➖ (fp32-only single-component low-peak; BudgetAware/per-stage-evict genuinely N/A): unload()→MLX.Memory.clearCache() (was ref-drop only); honest split footprint resident 150 MB (weights ~21 MB) + peakActivation 1.5 GB pyramid (est) — the old flat fp32 1.5 GB mislabeled activation as resident. License MIT/MIT. Validated: builds @0.17.0 + offline conformance 5/5; inference path unchanged. --> |
| contentClassify | mlx-vjepa2-swift (MLXVJEPA2) | V-JEPA2 ViT-L | wrapper+core | video/PROD | ✅ | ✅ | ➖ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); laundering fix — frame-sampling catch (async AVAssetImageGenerator call) now rethrows CancellationError unchanged; per-sampled-frame checkpoint added; CAN-3 subSecondRuns (two monolithic ViT-L forwards, 1.7 GB < 2 GB); CAN-1..3 green; engine pin 0.27.0. Tagged v0.5.0. Consolidated repo (VJEPA2MLX core folded in 2026-06-18; standalone `vjepa2-mlx-swift` core ARCHIVED, local clone quarantined to `mlxengine-video/_ARCHIVED-cores-quarantine/`). Same as RIFE: the 2026-06-18 pass left engine pinned 0.8.0 → bumped to 0.17.0 (tag v0.4.0). Born-clean hygiene, Eff stays ➖ (fp32-only single-component; BudgetAware/evict N/A): unload()→clearCache; honest split footprint resident 1.3 GB (ViT-L ~1.2 GB) + peakActivation 1.7 GB attention (est) — flat fp32 3.0 GB baked activation into residency. License MIT/MIT. Validated: builds @0.17.0 + offline conformance 6/6; inference path unchanged. --> |

> MuseTalk `Eff: ✅` (2026-06-30, engine 0.17.0) — split + `unload()` clearCache (batch #6, non-image/video stack). Wrapper `MLXTalkingHead` over core `MuseTalk` (VAE+UNet pipeline + shared whisper-tiny + bisenet). Flat residentBytes (fp16 8 / int8 7 / int4 6.5 GB) BAKED the bs=8 per-frame activation into residency → **split** per quant: residentBytes = weights floor (VAE fp32-loaded ~0.67 GB + UNet@quant + whisper-tiny ~75 MB + bisenet ~50 MB) = **fp16 2.5 / int8 1.7 / int4 1.3 GB** (only the UNet shrinks with quant); peakActivationBytes = the bs=8 UNet+VAE per-frame transient, dtype-INDEPENDENT (co-residency premise) = **~4.5 GB across all three**. Config now conforms to `QuantConfigured`. **P2 whisper-evict skipped** (noted N/A-low-value): whisper-tiny encodes once upfront then idles, but at ~75 MB the win is marginal and evicting mid-`runTalkingHead` would complicate the per-frame loop — the headline is the split; bisenet runs per-frame so stays. `unload()` now `MLX.Memory.clearCache()`. mmap/lazy: noted — `MuseTalkWeights.load` casts per-tensor lazily (not a full eager dict copy), `eval(module)` realizes; left as-is. **No full-pipeline smoke** (video GPU-watchdog risk on this beta OS — the LTX lesson; build green, talkingHead surface stable). **FLAG:** residentBytes derived from on-disk weight sizes + peakActivationBytes from the ~7 GB Python-port bs=8 peak — BOTH smoke/derived est; in-app `phys_footprint` (R-MEM-1/admission, ~2.5–2.9× a smoke MLX-peak) re-baseline pending (MLXEngineVideo/LTXVideoTesting).

> Wan family `Eff: ⬜` deferred behind a dedicated deep-dive (more complex). **LTX is the sweep's first target + process-validation.**

## 🧠 Language & Vision-Language

| Capability | Package | Model | Role | Home | Avail | Val | Eff | Eng |
|---|---|---|---|---|---|---|---|---|
| llm | mlx-qwen-llm-swift | Qwen3.5 | wrapper | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-token cadence on both paths (mlx-swift-lm 3.31.4 tokenLoop Task.isCancelled + wrapper post-respond checkpoint — ChatSession.respond surfaces a cancel as a SILENT PARTIAL RETURN, and the miss also re-held a truncated turn into the KV-reuse session: the post-call checkpoint now guarantees a cancelled run drops the held session; structured TokenIterator checks per token); CAN-1..3 green; engine pin 0.27.0. Tagged v0.5.0. --> |
| llm | mlx-gemma-llm-swift | Gemma-3-12B-it + Gemma-4-12B-it | wrapper | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); per-token cadence on both paths (upstream tokenLoop + post-respond checkpoint fixing silent-partial-return absorption; structured TokenIterator per token); static manifest under the 2 GB threshold but gemma-4 int4 (2.16 GB measured) crosses it → cadence declared, not exemption; CAN-1..3 green; engine pin 0.27.0. Tagged v0.4.0. PUBLISHED xocialize/mlx-gemma-llm-swift @v0.2.0, engine URL from:0.19.0 (v0.1.0 first publish @0.18.0 = gemmaTerms allowlisted). MLXLLM-only (never MLXVLM — gemma3 registry-shadowing guard; guard's FIRST LIVE CATCH: the pe-e2e single-binary mix with MLXQwenLLM, 2026-07-02 → harness split into per-backbone executables). Born sweep-clean: split MEASURED int4 floor 10 GB + act ~1 GB @2k envelope (dense Gemma has NO Qwen GDN prefill-scratch blowup); QuantConfigured+FootprintConfigured+WeightPrewarming; modelDirectory reuses LTX's gemmaDirectory (zero extra download). Val ✅ 2026-07-02: RunGemmaLLM smoke/mem-bench + prompt-enhance-e2e-gemma GATE PASS 4/4 + IN-APP (LTXVideoTesting, BRIDGE-LTX-006): compact24 enhance→generate 15.02 GB (zero regression vs the ungoverned 15.04) AND enhance-with-LTX-resident via governor LRU-evict-to-admit (exceedsMemoryBudget = alone-too-big only; no app-side evict-retry needed). Purpose: govern the LTX enhancer's previously out-of-engine Gemma load. FAMILY AXIS (2026-07-04, in v0.2.0): GemmaFamily gemma3|gemma4 — gemma-4-12b-it (model_type=gemma4_unified, mlx-swift-lm ≥3.31.4 text factory Gemma4Model; sanitize strips vision_embedder). Gemma-4 weights Apache-2.0 (manifest keeps stricter gemmaTerms = conservative-correct); catalog += gemma-4 12B {4bit,8bit,bf16}; identical 12B hybrid KV geometry (verified both configs). Val ✅ 2026-07-04: RunGemmaLLM --smoke/--mem-bench --gemma4 — int4 floor MEASURED 7.11 GB (declared 8), act MEASURED 2.16 GB @2k envelope (≈4.6× analytic KV — gemma4_unified prefill scratch; declared 2.7, family-specific NOT analytic). Gemma-3 regression smoke PASS same day. Codable back-compat: legacy {size,quant} payloads decode as gemma3. MAT (2026-07-04, v0.2.0): born materialization-clean — WeightSourcing single "checkpoint" source (quant tiered by REPO ⇒ whole-snapshot matching, nothing to exclude; unpublished quants declare nothing = explicit-dir-only); missingWeightSources honors explicit modelDirectory first then store <root>/<org>/<name> (config.json probe); load() auto-materializes dir-less configs via HubClient.downloadSnapshot → WeightDownloadProgress (replaces the v0.1.0 HubClient(cache:) hub-cache path — weights materialized under the OLD hub-cache layout re-download ONCE into the store layout); prewarm resolves the store (second-cold-launch payoff). Offline MAT-1..5 green per published checkpoint (22 tests, Xcode-beta xcrun). --> |
| llm | mlx-lfm-llm-swift | LFM2.5-8B-A1B | wrapper | think/PROD | ✅ | ✅ | ✅ | 0.34.0 | <!-- NEW 2026-07-24 (v0.1.0): the agentic/companion-assistant EVALUATION target for the AI-Avatar pairing (MLXCompanion) — sparse MoE 8.3B total / 1.5B ACTIVE (32 experts / 4 active, 24 layers = 18 double-gated short-conv + 6 GQA), so decode compute is ~1.5B-class next to the avatar renderer + TTS; IFEval 91.8 / BFCLv3 64.4 / Tau²-Telecom 88.1. PATH-A REUSE, zero layer translation: model_type=lfm2_moe → stock MLXLLM LFM2MoEModel (mlx-swift-lm 3.31.4, the existing fleet floor — no runtime bump); load() verifies the resolved type (registry-shadowing guard). LICENSE (C7) = CONDITIONAL PASS: weights lfmOpen1 (LicenseRef-LFM-Open-1.0, engine 0.34.0) — Apache-2.0-derived with a $10M annual-revenue gate on commercial use, allowlisted per the ltx2Community precedent (same shape minus the non-compete); record the gate wherever LFM weights ship, revisit at crossing. Catalog: first-party LiquidAI/LFM2.5-8B-A1B-MLX-{4bit 4.83,5bit 5.86,6bit 6.89,8bit 9.0,bf16 16.94 GB} (quant tiered by REPO); default int4. Born sweep-clean: split MEASURED int4 floor **4.89 GB** post-LOAD (declared 5.5) + act **1.31 GB** @2k envelope (declared 1.6), retain 0.16 GB — floor lands within 0.1 GB of the 4.83 GB on-disk weights (near-perfect mmap load); activation is ~52× the 25 MB analytic KV (only 6 attention layers grow, 12,288 B/token; conv cache fixed ~221 KB) but still modest next to Qwen3.5's GDN blowup. **v0.2.1 CORRECTION** — v0.1-0.2 declared floor 6.5 / act 0.5 from a --mem-bench that read the floor AFTER a warmup run: a post-run floor conflates resident weights with retained intermediates, over-reading the floor AND collapsing peak−floor toward zero (the ValidationRun / NAFNet-DDColor trap). Caught by cross-checking the CLI against the in-app ValidationHarness (agreed on floor to 0.13 GB); the CLI bench is now fixed to ValidationRun semantics (floor post-load, retention reported separately); QuantConfigured+FootprintConfigured+WeightPrewarming; unload clearCache. Born materialization-clean at the MS-1/MS-2 contract (FIRST package authored against it — the gemma-era nested <root>/<org>/<name> pattern is STALE): missingWeightSources = explicit modelDirectory probe then defaultMissingWeightSources (accepts hub-snapshot AND flat layouts); resolution snapshot-first then flat; MAT-1..5 green ×5 quants. Born cancel-clean: CAN-1..3 green, per-token cadence both paths (upstream tokenLoop + post-stream checkpoint; structured TokenIterator per token); static manifest under 2 GB threshold but reasoning-first runs are long → cadence declared, not exemption. REASONING: LFM2.5 ALWAYS emits <think>…</think> (no template toggle; preserve_thinking only affects history) → modes [direct(default)/companion = think-STRIPPED canonical text (TTS-safe, split-on-last-</think> mirroring the template), thinking = raw]; truncated-mid-think → empty text (never leak reasoning to TTS). TOOL CALLING (the two hard-won lessons): (1) metaData["tools"] (C5) rendered by the PACKAGE as the trained "List of tools: […]" system line in EXACT Python-json.dumps format (", "/": " separators, name→description→parameters key order) — swift-jinja tojson (compact, alphabetical) PROVABLY breaks tool grounding: Python-mlx-lm oracle fed the swift-jinja rendering reproduces the temp-0 refusal byte-for-byte and calls the tool on the trained format (numerics parity proven in the same experiment — Swift greedy output == Python greedy output); byte-pinned unit test. (2) mlx-swift-lm auto-detects ToolCallFormat .lfm2 from the model_type prefix and its ToolCallProcessor EXTRACTS <|tool_call_start|>…<|tool_call_end|> from the text stream as .toolCall events — ChatSession.respond (string plane) silently DROPS them; run() drives streamDetails and re-serializes calls back into the canonical marker text. Val ✅ 2026-07-24: RunLFMLLM --smoke (load 0.6s warm, ~2s/512-tok runs, think-strip verified), --tool-smoke PASS (get_weather(city="Reykjavík") + no spurious call on the negative), --structured 15/15 strict-parse · 0.73 ms/step masking on the 128k BPE vocab (proves the shared kit on a third tokenizer family), --mem-bench (split above). Offline: 32 tests green. PUBLISHED xocialize/mlx-lfm-llm-swift, latest **@v0.3.0** (first publish 2026-07-24, public; port code MIT matching the shipped LICENSE — manifest aligned .mit, was briefly .apache2 pre-publish). v0.2.0 ADDS metaData SAMPLING EXTRAS (C5): topK / repetitionPenalty / repetitionContextSize / minP / seed → GenerateParameters on BOTH the freeform and structured paths — the canonical LLMParameters carries only temperature/top-p/maxTokens, but LFM2.5's own HF Space exposes top_k + repetition_penalty as first-class chat controls, so a parity consumer needs them (4 unit tests; tool-smoke regression green). IN-APP VAL ✅ 2026-07-24 (Liquid LFM 2.5 Demo, think/PROD/LiquidLFM — the HF-Space-parity harness app): full golden path exercised on a real machine — folder grant → store-resolved dir-less register → prepare → multi-turn chat, int4, ~5–6 s per short answer INCLUDING the always-on reasoning pass, reasoning disclosure + strip verified in the UI, storage panel reports 6.4 GB. Three CONSUMER-side bugs the app surfaced (all app-side, package unchanged): (1) a `.disabled(!isReady)` composer is uninteractable — gate SEND, not typing, and a `.plain` TextField only accepts clicks inside its glyph bounds so the padded box needs contentShape+tap-to-focus; (2) EngineSettingsView declares its own 720pt natural width (200 sidebar + FIXED 520 storage panel) — imposing a smaller frame clips the Choose… button and every storage value; (3) a re-register path (quant picker) that skips the store-root guard silently flips status to "Not loaded" while the real blocker is a missing folder grant — guard in register(), not just bootstrap(). NOT YET: KV-session reuse (qwen v0.3.0 pattern — adopt if the eval picks LFM for the companion loop), tool-role turns in history (template tool_response formatting), per-quant re-measure beyond int4, int5/int6/bf16 footprints (still estimates). METRICS ✅ v0.2.2 — the demo app carries the fleet ValidationHarness (Metrics panel + headless `LFM_AUTORUN=1 LFM_SCENARIO=<name>`); measured ONE PROCESS per scenario with `engine.trimCaches()` as the clearCache hook, reproducible to two decimals: int4 floor 4.89 GB stable / act envelope-2k **1.06** · tool-call 0.56 · structured 0.26 · short-turn 0.20 GB, retain 0.15-0.25 (no retention leak; pool active 4.83 GB == on-disk weights). int8 floor MEASURED **9.07 GB** (9.00 on disk — same ~0.07 overhead, mmap load holds across quants) / act 0.89 GB. Activation does NOT scale with quant (int8 < int4) — Qwen25VL lesson holds. Declaration 5.5/9.7 + 1.6 shared confirmed correct. THREE MEASUREMENT TRAPS worth reusing: (1) read the resident floor POST-LOAD, never post-warmup — the v0.1-0.2 mis-declaration; (2) `isolate: true` on EVERY scenario or the previous one's phys_footprint inflates the next floor (observed drift 4.89→6.85 GB with act collapsing to 0.04); (3) one PROCESS per declarable number — an in-process suite is a smoke sweep only (a 1.75 GB tool-call 'peak' was pure contamination). Consumers should use `engine.trimCaches()`, NOT link MLX — the engine exposes it precisely so an app doesn't have to. **MEASURED USAGE (contract 1.26.0, FIRST ADOPTER) 2026-07-24** — closes the throughput gap this row's METRICS section left open: memory was measured properly, throughput was not (the demo app estimated `chars ÷ 4 ÷ runSeconds` and printed a caveat saying so). The counts were already arriving and being discarded — `streamDetails` delivers mlx-swift-lm's `GenerateCompletionInfo` as a `.info` event, and this package's switch swallowed it under `default: break`. Both paths now populate `LLMResponse.usage`: freeform captures `.info` (promptTokenCount / generationTokenCount / promptTime / generateTime); the structured `TokenIterator` drive — which has no `.info` — counts and times itself (`lmInput.text.tokens.size`, prefill split at the first sampled token) so the two are comparable. Ride-along fix: `GenerateCompletionInfo.stopReason` maps 1:1 onto `FinishReason`, retiring the hardcoded `finishReason: .stop` on the freeform path (a maxTokens truncation previously reported as a natural end). TWO SEMANTICS WORTH REUSING: (1) usage counts the always-on `<think>` block even when non-`.thinking` modes strip it from canonical text — decode throughput measures work done, not characters surfaced, and dividing only the visible text by run time under-reports a reasoning model badly (the exact false comparison the estimate produced); (2) `promptTokens` is NOT comparable against a package that reuses a KV session across turns (qwen v0.3.0 prompt caching prefills only the new suffix; LFM builds a fresh session per turn and re-counts) — `generationTokens`/`generateSeconds` is the comparable axis and the number the planned LFM2.5-vs-Qwen3.5 bake-off should quote. 35/35 offline tests green (3 new). SHIPPED v0.3.0 against engine **0.35.0** (contract 1.26.0); `RunLFMLLM --smoke` now prints usage, proving the `.info` capture fires — an offline test cannot. LIVE int4, ~1.2k-token prompt: prompt 1356 tok / 0.19 s · generated 418 tok / 1.51 s = **277.2 tok/s decode** — the ~1.5B-active MoE claim borne out. The `chars÷4` estimate this replaces read ~72 tok/s on the same run: **off by ~3.8×**, purely because it could not see the stripped reasoning tokens. --> |
| llm (prompt enhance) | ernie-pe-swift | ERNIE-PE (Ministral-3B) | wrapper+core | image/PROD | ✅ | ✅ | ⬜ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); pre-existing per-token isCancelled seam in the vendored Mistral3 core declared generate/token; CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. --> |
| embed | mlx-qwen-embedding-swift | Qwen3-Embedding-0.6B | wrapper | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); pre-existing per-text checkpoint in the batch embed loop declared as encode/chunk cadence (not long-run-implied, ~1 GB); CAN-1..3 green; engine pin 0.27.0. Tagged v0.2.0. PUBLISHED xocialize/mlx-qwen-embedding-swift @v0.1.0, engine from:0.20.0 (contract 1.15.0 — `embed` introduced FOR this package; unblocks MLXCompanion semantic memory, ENGINE-NEEDS N4; vectors ride the response per the imageQualityScore non-Artifact precedent, no CanonicalOutput.vector). PATH-A REUSE, zero layer translation: checkpoint model_type=qwen3 → stock MLXLLM Qwen3Model (quantized Linears included); Qwen3ModelInner.callAsFunction already returns norm(h) = last_hidden_state — the package adds ONLY the head semantics: last-token pooling at the trailing <|endoftext|> (151643; tokenizer post-processor appends it, encode paths that don't are repaired defensively — the #1 silent-failure risk), query-side "Instruct: {task}\nQuery: {text}" (documents verbatim — asymmetric retrieval), Matryoshka truncate-BEFORE-normalize (dims 1..1024, native 1024 = hidden_size), always L2-normalized. MLXLLM-only (registry-shadowing guard: load() verifies Qwen3Model). Texts run as SEQUENTIAL single-seq forwards, cache:nil (no left-pad/mask bookkeeping; pooling exact by construction; batched-GEMM win is noise at 0.6B/consumer scale). Born sweep-clean: split MEASURED int8 floor 0.87 GB (declared 1.0) + act 1.02 GB @4k-token envelope (declared 1.3; no KV cache — full-seq forward scratch, shared across quants); QuantConfigured+FootprintConfigured+WeightPrewarming. Born materialization-clean: WeightSourcing single "checkpoint" source, quant tiered by REPO (8bit default 633 MB / 4bit-DWQ 335 MB; -mxfp8 out-of-grammar, uncataloged; no published bf16 ⇒ explicit-dir-only); offline MAT-1..5 green per checkpoint (15 tests). Val ✅ 2026-07-04: PARITY vs sentence-transformers fp32 reference (embed-oracle goldens) — token ids EXACT on every role, per-vector cosine ≈0.9996 (gate ≥0.99, int8-vs-fp32), Matryoshka-256 ≈0.9996, retrieval ranking match (max |Δscore| 0.0055), instruct-vs-plain asymmetry matches oracle ±0.003; RunQwenEmbedding --smoke/--dump/--mem-bench + --engine-e2e through the REAL MLXServeEngine (register→prewarm 0.6 GB→prepare 0.3s store-resolved dir-less→run→evict): order-preserving batch cos 1.000000 vs single-text, asym cos 0.7876, m256 via engine, modelID provenance stamped. int4-DWQ published but not yet live-validated (floor est 0.7 GB — re-measure at first consumer). Consumer contract reminder: persist modelID+dimension beside stored vectors; an index must never mix models/dims. -->
| imageAnalysis | qwen25vl-mlx-swift | Qwen2.5-VL-3B | wrapper+core | think/PROD | ✅ | ✅ | ✅ | 0.27.0 | <!-- CAN retrofit 2026-07-09: entry checkpoint first act of run(); NEW per-token checkpoint in the greedy decode loop + post-encode/pre-decode stage seam in Qwen25VLPipeline.generate; CAN-1..3 green (bf16 3.0 GB implies long runs); engine pin 0.27.0. Tagged v0.3.0. WeightSourcing retrofit sweep 2026-07-09 (pre-MAT fleet): single self-contained "main" snapshot per quant tier (defaultRepo(for:) → mlx-community/Qwen2.5-VL-3B-Instruct-{bf16,4bit}; explicit repo wins); missingWeightSources honors explicit snapshotDirectory first, then the store layout (probe = config/preprocessor/tokenizer/index + ≥1 safetensors shard — bare dir is NOT enough, the materializer pre-creates it); load() materializes via WeightMaterializer (HubClient snapshot) w/ .downloading progress; prewarm store-resolved (index + dir). BONUS FIX: the tokenizer now loads from the snapshot's own files (AutoTokenizer modelFolder) instead of fetching the stock Qwen repo into HubApi's PRIVATE cache at load() — a materialized snapshot is now offline-sufficient; gated TokenizerLocalLoadTests pins the vision special-token ids (151655/151652/151645/151643) on the local path. snapshotDirectory stays the dev escape hatch (never touches the network). MAT-1..5 green per quant tier (MLXQwen25VLTests). Engine pin 0.15.0→0.23.0, + swift-huggingface. Tagged v0.2.0. --> |
| imageAnalysis | qwen3vl-mlx-swift | Qwen3-VL | core (wrapper pending) | think/WIP | 🧪 | ⬜ | ⬜ | |
| imageAnalysis / videoAnalysis | mage-vl-swift (MLXMageVL) | Mage-VL 4B (Mage-ViT + Qwen3-4B) | wrapper+core | think/WIP | 🧪 | ✅ | ✅ | 0.36.0 | <!-- NEW 2026-07-27. FIRST PROVIDER of `videoAnalysis` — the capability already existed in Capability.swift with VideoAnalysisRequest/Response and a C11 descriptor, and had no backer, so no core change was needed. One model, two surfaces, ONE package; run() dispatches on request.capability with @unknown default (C12). C7/C8 both Apache-2.0, verified by raw-file fetch. PARITY three-way on examples/dog.jpg: PyTorch -> MLX-Python (mlx-vlm fork, mage_vl model dir) -> MLX-Swift, greedy **48/48 tokens identical**; vision tower rel <=4.0e-5 / cos 1.0 vs PyTorch on the CPU stream across 4 grids incl. t=0-63; weight key set 696<->696 0-missing/0-unused; vision seam 2048 replaced == 2048 image-token positions exactly. Preprocessing vs AutoProcessor: max|d| 2.4e-7 on identical decoded pixels — the file-path residual (cos 0.9999318) is the JPEG DECODER (CoreGraphics vs libjpeg, mean 0.56/max 10 levels), quantified not assumed; PILResize lifted verbatim (3rd lift: qwen25vl->qwen3vl->here) and temporal_patch_size=1 (not 2) makes patch vectors 768-wide. SWIFT IS PATH B FOR BOTH HALVES: MLXLLM.Qwen3's Attention/MLP/TransformerBlock are internal and Qwen3ModelInner.layers fileprivate with a token-embedding callAsFunction, MLXVLM.Qwen3VL's language half is mRoPE (wrong model), and our qwen3vl-mlx-swift exposes Qwen3VL monolithically — so the decoder is translated here (~200 lines). Depends on MLXLMCommon ONLY (KVCache/createAttentionMask/THW), never MLXLLM/MLXVLM, to keep ModelFactoryRegistry out of the host process. FOOTPRINT MEASURED (M5 Max, Release, one process per number, floor POST-LOAD): image 8192 patches floor 8.85 GB / peak 11.70 / act 2.85 @54.9 tok/s · video 16f 32,640 patches floor 8.85 / peak 23.02 / act 14.16 @49.2 · video 32f floor 8.85 / peak 52.25 / act 43.40 @40.4. Floor identical across all three = the measurement is sound. Activation is NOT constant: vision attention is linear in frames (4-frame windows) but LLM prefill is quadratic in visual tokens, so 16f->32f doubles patches and triples activation — the TOKEN BUDGET is the memory lever. That measurement CHANGED the default frameCount 32->16 (32 frames = 4x the model's trained 4096-token budget and a 52 GB peak, inadmissible on most machines); video resolution is now derived from visualTokenBudget, and the derivation reproduces upstream's own recipe (4096 over 64 frames = 256x256). Gates: 17 offline green (positions 5 mirroring the Python test_mage_vl_positions.py, manifest 5, CAN 3 cadence .generate/.token, INF 1, MAT 3) + declared globs PROVEN sufficient AND minimal by loading and answering from a 13-file/8.8 GB copy containing only them (repo has 26 entries; neural_codec CUDA runtime and streammind_gate excluded = ~1.3 GB never fetched). Val 🟡 = CLI gates only, in-app pending. NOT implemented: Tier-3 streaming (needs a Mamba/selective-scan primitive MLX-Swift lacks) and codec-native frame selection (a codec-free SEA-RAFT+AVFoundation substitute measured +0.037 selection IoU over persistence on a 10-clip corpus — agreement with the codec, NOT an effect on answers, which only became measurable now this package exists). **v0.2.0 2026-07-27 — int8 published + Swift quantized-load.** mlx_vlm affine int8 g64 (9.023 bpw, LM only, ViT stays bf16) PUBLISHED to **mlx-community/Mage-VL-8bit** and validated per doctrine: fresh download of exactly the declared globs (11 files, 5.0 GB) -> identical 48/48 greedy tokens at 86.1 tok/s. Quant gate: int8 vs torch final-position cos 0.9982 (bf16 baseline 0.9996 — deficit 4.1x the Mage-Flow <=2x rule, resolved on the OBJECTIVE instead: tokens byte-identical, and token identity is what a cosine gate proxies for on a generative LM). Swift core loads it (mlx-vlm namespace + single-file layout in sanitize; nn.quantize BEFORE update(parameters:) — structure first or verify:[.all] fails; scales-presence predicate reproduces the LM-int8/ViT-bf16 split without hard-coding). Configuration quant-keyed (.bf16/.int8 -> repo + shard globs); manifest int8 footprint MEASURED floor 5.00 GB / act 14.2 GB unchanged (activations do not quantize) / 88.3 tok/s image. Engine chip: PR #13 adds videoAnalysis to CAN longRunCapabilities (verified against this provider via package edit). plan=mlxengine-think/Docs/MageVL-Swift-Port-Plan.md **DEMO APP 2026-07-28 (`mlxengine-think/PROD/MageVL/MLX MageVL Demo`, v0.1.0, local-only like the LiquidLFM reference app).** HF-Space-parity UI vs hugging-apps/mage-vl-demo (Image · Video · Proactive-streaming-explaining-why-not) plus a Metrics tab the hosted Space cannot have. Golden-path consumer (store-before-register, ModelStorageModel app-scope bookmark, EngineSettingsView, detached-task runs, MLXEngineTestKit.ValidationHarness for measurement), styled from MLXEngineUI.MarqueeTokens (Figma MqQ9CA930ZqaZ3ZFQD7KI6 verified hex-for-hex against the existing tokens, so consumed not duplicated). Headless `MAGEVL_AUTORUN=1` via HeadlessAutorun; each scenario also logs a `[say]` answer excerpt, because a suite reporting only megabytes proves the pipeline ran, not that it said anything. **IN-APP MEASURED (M5 Max, Release, engine path register→prepare→run→evict, ValidationHarness isolate:true, floor POST-LOAD, background peak sampler; ONE in-process suite so treat as a sweep):** int8 image floor 5.45 / act 3.14 · video 16f 5.47 / 4.01 · video 32f 5.50 / 4.37 · video 16f@8192tok 5.54 / 6.05 GB. bf16 image floor 9.59 / act 2.72 · video 16f 9.60 / 4.03 · video 32f 9.64 / 4.39 · video 16f@8192tok 9.67 / 6.02 GB. Answers coherent and specific on both quants (Border Collie + patterned rug; England-v-Argentina BBC Sport broadcast with the scoreline read off the graphic). ⚠️ **DECLARED peakActivationBytes 14.5 GB IS STALE — over-declared ~3.3× at the shipped defaults.** The 14.16 GB figure came from a video-16f run at 32,640 patches, i.e. BEFORE video resolution was derived from `visualTokenBudget`; at the shipped default (budget 4096, frameCount 16) video 16f is 4,032 visual tokens / 16,128 patches and activation is 4.0 GB — measured identically on bf16 and int8, confirming 'activations do not quantize'. Worst case across the default-config suite is 4.4 GB, and even DOUBLING the budget to 8192 only reaches 6.05 GB. Consequence: the governor reserves 14.5 GB (visible as reserve=14.50GB in every log line), so bf16 declares 9.5+14.5 = 24 GB and is refused on machines that would actually run it in ~14 GB. Fix is a package version bump re-declaring peakActivationBytes (~6-7 GB covers the default suite with headroom for a raised budget) — NOT done here, since this session's scope was the demo app and the package is published at v0.2.0 on GitHub + HF. Also measured, and the reason the demo's 4th scenario exists: **the budget is the memory lever, not the frame count** — doubling frames at a fixed budget barely moves activation (4.01→4.37 GB) because per-frame resolution drops to compensate (32f yields FEWER visual tokens than 16f: 3,840 vs 4,032), while doubling the budget at fixed frames takes it to 6.05 GB and run time 5.5→12.3 s. Two demo-side bugs this surfaced and fixed, both the same shape: `frameCount`/`visualTokenBudget` are package CONFIGURATION, so a UI stepper or a harness scenario that only fed a local preview left every run measuring the defaults while the screen claimed otherwise — both now re-register. **Val stays 🟡**, narrowed: the engine path, real answers and footprints are now in-app validated on both quants, but via an UNSANDBOXED dev build (`codesign` re-sign + `MAGEVL_MODELS` override, which the app refuses under the sandbox via a readability probe). The remaining gap is exactly one human action — launch the sandboxed GUI once, pick a models folder, confirm the app-scope bookmark persists across relaunch — the same bar the nemotron-stt row cleared before going ✅. **IN-APP VALIDATED 2026-07-28 (Val 🟡→✅)** — driven live in the SANDBOXED demo app on the real engine path, both surfaces. Image: dog.jpg 1024×2048 → correct detailed answer (Border Collie, patterned rug), 10.33 s incl. cold prepare. Video: soccer-broadcast.mp4 → 16 frames, grid t=16 h=24 w=42, 16,128 patches, **4,032 visual tokens**, 9.63 s, answer reads the scoreline and broadcaster off the graphic — token counts and frame grid match the headless suite EXACTLY, which is the cross-check that the in-app and CLI harnesses agree. Models folder granted via NSOpenPanel; weights loaded from the shared store. TWO CONSUMER BUGS FOUND AND FIXED, neither in this package: (1) SwiftUI's `VideoPlayer` ABORTS on macOS 27 — `_AVKit_SwiftUI` fails to instantiate its generic class metadata (`getSuperclassMetadata`→`swift::fatalError`) while SwiftUI builds the representable's responder chain, because the binary links the `_AVKit_SwiftUI` shim WITHOUT AVKit proper, so the shim's superclass is unresolvable; an `NSViewRepresentable` over AppKit's `AVPlayerView` is plain ObjC metadata and has no such failure mode. (2) the demo built **TWO `MageVLEngine` instances** — `RootView` made its own via `@StateObject` while `DemoWindow` owned and bootstrapped a different one — so the engine the UI displayed was never bootstrapped and read `.needsFolder` on every launch no matter how the models-folder bookmark restored, waking only when the settings sheet's Done called `modelStoreChanged()`. Two engines also meant two `ModelStorageModel`s, so the storage panel and the header pill read different objects and could disagree about whether a folder was granted. **This was misdiagnosed first as a security-scope failure**, and a defensive `hasPendingChange` guard was added to MLXEngineUI and released as v0.40.1 on that wrong premise; the guard is kept (a path-only comparison would strand a real scope failure) but its comment now records that it is unreproduced — see the correction commit. The app-scope bookmark PERSISTS and its scope RESTORES correctly: with one engine the app comes up 'Not loaded' on a cold launch with zero folder interaction and answers an image question in 5.98 s. Lesson: two objects of the same type, one shown and one driven, mimics a permissions failure closely enough to send a diagnosis into the OS layer — check instance identity before entitlements. **UPSTREAM 2026-07-29 — mlx-vlm PR #1745 MERGED (Blaizzy/mlx-vlm, 18:23Z).** The Python port went up as 7 commits; @lucasnewman approved after one CHANGES_REQUESTED round. Three things came back from the community/review that are worth keeping, because two of them are defect CLASSES rather than one-off slips. (1) **@ismaelvega found the torch-free processor was not torch-free** on a clean Apple-Silicon install: `AutoImageProcessor.from_pretrained` needs torchvision, the ImportError was swallowed by the AutoProcessor patch dispatcher's `except Exception: pass`, and dispatch fell through to the checkpoint's torch remote-code processor — throwing the exact `ones_like(): argument 'input' must be Tensor, not mlx.core.array` the class exists to prevent. Fixed by constructing the in-repo numpy/PIL `Qwen3VLImageProcessor` directly (no `Auto*` resolution anywhere); old-vs-new `pixel_values` agree to max|Δ| 4.8e-7. **Root cause of the ESCAPE, not the bug: the dev venv had torch installed FOR THE PYTORCH PARITY ORACLE, so the verification environment differed from the user environment in exactly the dimension under test.** Any port validated against a torch oracle has this hole by construction — check the torch-free path in a venv that has never seen torch. (2) **Unevaluated init-time caches strand on a stream.** The Python `VisionRotaryEmbedding` built `_inv_freq_{t,h,w}` as three lazy graphs in `__init__` and never `mx.eval`'d them, so they carry whatever stream was current at construction and crash when the model is later used on another. Invisible to single-stream local runs, which is every local run. **CROSS-CHECKED AGAINST THE SWIFT PORT — NOT AFFECTED, verified not assumed:** `MageVLMLX.VisionRotaryEmbedding` holds the tables as host-side `[Float]` and lifts them to `MLXArray` per call, so no MLX graph is retained at all; and `MageVL.load` ends with `eval(self)` after `update(parameters:)`, so nothing lazy survives load. A grep confirms the core has **zero** `MLXArray` stored properties. The `[Float]` choice was made for an unrelated reason — an `MLXArray` stored property on a `Module` is reflected as a PARAMETER, so `update(parameters:verify:[.all])` would demand `invFreqT/H/W` from the checkpoint and fail the load, since upstream registers them as non-persistent buffers — which is a useful coincidence to know rather than rely on: **a Swift port that DOES cache an `MLXArray` in `init` inherits the Python bug, and the engine's `eval(self)`-after-load convention is what covers it.** (3) numpy round-trips: `masked_scatter` used `np.where` on the mask, forcing a device→host→device trip per image; replaced with a boolean-mask assignment proven bit-identical across fp32/bf16 at (1,64,32)/(1,2048,2560)/(2,128,64) incl. the zero-image case. The Python port is now numpy-free end to end. Swift never had this path. Review also asked for porting-narrative comments to be cut — a house-style difference worth noting when contributing outward: rationale-dense comments are right for this fleet's repos and read as notes-to-self in someone else's. PROCESS: fork-PR workflow runs land at `action_required` and CANNOT self-clear — every push needs a maintainer approval, so a red ✗ on a fork PR is often a stale run, not a live failure. Merge required signed commits; the 7 commits were rebased onto current `main` and SSH-signed (all 7 GitHub-VERIFIED, `BLOCKED`→`MERGEABLE`). The two `Merge branch 'main'` commits by @Blaizzy/@Lazarus-931 were DROPPED rather than re-signed, because signing them would have re-attributed their work to our key; rebasing onto `main` reproduces their effect. mage_vl tree byte-identical to the pre-signing commit CI had already passed on. **v0.3.0 2026-07-29 — ACTIVATION RE-DECLARED 14.5 GB → 6.5 GB (both quants), and the bench that hid the drift is fixed.** Re-measured properly (ONE PROCESS PER NUMBER, floor post-load, shipped defaults budget 4096 / frameCount 16): bf16 image 8,192 patches floor 8.85 / act 2.85 · bf16 video 16f 16,128 / floor 8.85 / act 5.77 · int8 image floor 5.00 / act 3.00 · int8 video 16f floor 5.00 / act 5.79 · int8 video 32f 15,360 / floor 5.00 / act 5.57. Floors stable per quant across every mode; activation matches across quants (5.77 vs 5.79) confirming activations do not quantize; 32f costs LESS than 16f at a fixed budget because per-frame resolution drops — the BUDGET is the lever. **ROOT CAUSE of the stale declaration, now understood rather than inferred: `magevl-gate --mem-bench` had DRIFTED FROM THE PACKAGE** — it called `preprocessFrames` directly while `run()` calls `.budgeted(tokenBudget:frames:)` first, so the bench measured an unbudgeted path production never takes and kept reproducing the pre-budget-derivation number. Proven by reproducing it exactly: the fixed bench at `--budget 8192` gives 32,640 patches and **14.19 GB**, matching the 14.16 GB on record — so the old figure was never wrong, it described a configuration that stopped being the default. The bench now walks the production path and takes `--budget`. ⚠️ **THIS ALSO CORRECTS TWO NUMBERS THIS ROW CARRIED FROM THE DEMO-APP SWEEP:** that in-process suite reported video-16f activation 4.01 GB (actual 5.79) and budget-8192 activation 6.05 GB (actual 14.19 — under by 2.3×). A warm MLX buffer pool absorbs the growth, so an in-process suite UNDER-reads activation even when the floor looks sane. The bucket rule 'one process per declarable number' is usually cited for the FLOOR; this shows it bites ACTIVATION harder and in the dangerous direction — under-declaring gets a package admitted onto hardware that then OOMs. Treat an in-process sweep as a lower bound on activation, never as evidence for a declaration. REMAINING HAZARD, documented in the manifest rather than glossed: `visualTokenBudget` is caller-settable and NOT bounded by the declaration, so a caller raising it to 8192 is admitted against 6.5 GB and then needs 14.19 GB. Clamping it to the declared envelope (or deriving the declaration from it) is open follow-up. README and the HF card (mlx-community/Mage-VL-8bit, commit f1cebca) corrected in the same change. **FORK RETIRED 2026-07-29.** `mage_vl` is on upstream `main`, review fixes included (`mx.eval` present, zero numpy in `mage_vl.py`), so `xocialize/mlx-vlm@mage-vl` has no remaining purpose — it is the parity oracle's source no longer, and anything still installed from it will now silently diverge. **Timing gap worth knowing: PyPI 0.6.8 shipped 2026-07-27, TWO DAYS BEFORE the merge, so `pip install mlx-vlm` does NOT yet get mage_vl.** 0.6.9 is the first release that will. Recent cadence is 2–4 days (0.6.5→0.6.6→0.6.7→0.6.8 at 3/2/4 days), so the gap is short but real. HF card now says `pip install "mlx-vlm>=0.6.9"` with a git-from-main fallback and an explicit migrate-off-the-fork note, so it needs no further edit when 0.6.9 lands (commit 40dc28d). The card's Swift floor also moved to ≥ v0.3.0, since the corrected activation declaration is admissibility-affecting. Earlier references to the 'mlx-vlm fork' in this row describe how PARITY was done and remain accurate as history. -->

> mlx-qwen-llm-swift `Eff: ✅` (2026-06-30, engine 0.15.0) — first **autoregressive** sweep target: the transient is the **context-scaled** activation, not a fixed peak. P1 split: flat `residentBytes = onDisk + 600 MB` (KV-cache-blind) → `FootprintConfigured` split `residentBytesHint = onDiskBytes` (weights floor) + `peakActivationBytesHint = peakActivationBytes` declared at a **documented 2048-token context envelope**. **Measurement surprise (the interesting bit):** Qwen3.5 is a HYBRID linear/full-attention model — only 1-in-4 layers is softmax attention with a context-growing `KVCacheSimple`; the rest are GatedDeltaNet (linear) with a fixed `MambaCache`. So the analytic KV-cache (`QwenModel.kvCacheBytes`, verified **bit-exact** at 12 288 B/token for 0.8B) is TINY (96 MB @8192). But the measured transient is **prefill-scratch-dominated** (the GatedDeltaNet chunked-scan over the prompt), ~20× the KV-cache and ~linear in sequence length: 0.8B-8bit measured 427 MB @~340 tok · 2.1 GB @~2k · 4.2 GB @~4k · 7.0 GB @~8k. Declared peak is the **measured** prefill-scratch at the 2048 envelope (~2.2 GB for 0.8B), width-scaled by `hidden_size` for 4B/9B (only 0.8B measured). Classic "flat footprints UNDER-declare." P2 (per-stage evict) N/A (single model). P3 (mmap) verified — floor ≈ on-disk weight bytes (loads via mlx-swift-lm safetensors). P4 (BudgetAware) the LLM-specific lever = cap context to fit KV-cache into budget; **documented + deferred** (KV-cache is small; silently shrinking context is a correctness surprise). Engine pin `from: 0.3.0 → 0.15.0`, no API drift.

> mlx-qwen-llm-swift **KV-cache reuse** (2026-07-05, **v0.3.0** — the package-level tail of MLXCompanion ENGINE-NEEDS N5; engine 0.21.0 shipped the pool-policy side separately). `run()` now holds ONE `ChatSession` across calls: exact-transcript fingerprint (`SessionReuse.decision`, pure + unit-tested) — hit ⇔ incoming messages == held transcript + exactly one new user turn AND same system prompt; on hit only the new turn is prefilled (`TokenIterator` does no prefix dedupe), on ANY mismatch it rebuilds fresh (byte-identical pre-reuse behavior). System prompt seeds `history[0]` (NOT `instructions:`, which re-templates every respond); `generateParameters`/`additionalContext` refresh per call so sampling/mode changes don't rebuild; held session drops on `unload()`, mismatch, or mid-generation throw. **Triage note:** the retained KV/GDN state is *intentional* active-memory retention (+143 MB measured after a 4-turn chat) — "active climbing across turns" while chatting is not a leak. Observability: `kvReuseHits/kvReuseMisses` + `Logger(subsystem: MLXQwenLLM, category: kv-reuse)`; `kvCacheReuseEnabled` escape hatch. Gates (`RunQwenLLM --kv-reuse/--mem-bench`, 0.8B-8bit): counters exact, held-leg recall through the reused cache ✅, post-first-turn latency **0.29s → 0.06s (5.3×, flat)** under a ~5k-token persona, mismatch fallback byte-matches a fresh run. **Known + accepted:** held-vs-fresh temp-0 byte equality is NOT guaranteed *by upstream design* (the cached generation prompt carries the empty `<think>` block a re-template omits) — near-tie argmax flips are possible (1-of-4 turns observed, both outputs valid); the consumer must echo the package's replies **verbatim** into subsequent requests or every turn is a (correct, slower) rebuild. Floor/envelope declarations unchanged (re-measured on the ~1.03 MB/token line).

> mlx-qwen-llm-swift + mlx-gemma-llm-swift **structured output** (2026-07-05, qwen **v0.4.0** / gemma **v0.3.0** — MLXCompanion ENGINE-NEEDS **N6**; contract **1.16.0** `LLMRequest.responseFormat`, engine v0.22.0). Grammar-constrained decoding: `.json(container: .any/.object/.array)` masks every token that can't extend a valid JSON prefix and force-stops after one complete top-level value; `.jsonSchema` = V1 best-effort (`.json` syntax + container inferred from the schema root). Runtime = shared **`mlx-constrained-decoding-swift` v0.1.0** (think/PROD): byte-level JSON FSM + once-per-residency vocab classification (Qwen byte-level-BPE ~151k / Gemma SentencePiece ~262k — string-safe bitset fast path + first-byte buckets; per-step full-vocab scans don't scale) + MLXLMCommon `LogitProcessor`. **Wiring:** structured requests BYPASS `ChatSession` (mlx-swift-lm 3.31.x has no processor seam through `GenerateParameters`) — template via `UserInput`/`prepare`, drive `TokenIterator` (custom-processor init) with a FRESH cache; the v0.3.0 held-session KV-reuse path is untouched (companion structured calls are one-shot side calls). Qwen structured calls default `enable_thinking:false` (`<think>` is masked anyway; explicit `.thinking` still wins). Honest `finishReason`: `.stop` only when the grammar completed; maxTokens-truncation → `.length`. Dead ends force EOS (never silently un-constrain). Manifests advertise `supportsStructuredOutput` (C11); packages without the runtime must throw `PackageError.unsupportedRequestFeature` (contract). **Gates:** `RunQwenLLM --structured` (0.8B-8bit, 3 companion shapes × 20 @ temp 0.7): constrained strict-parse **60/60** vs baseline scrape 57/60 (parseFacts 17/20 — the motivating silent-drop), masking **1.04 ms/step** /1928 steps (decode ≈5–15 ms → negligible); `RunGemmaLLM --structured` (gemma-3-12B-4bit): **15/15**, 1.07 ms/step (proves the SentencePiece path). Vocab classification ≈0.3 s once per residency, dropped on `unload()`. Kit offline tests: 14 (FSM malformed-prefix rejection, container hints, stop detection, dead-end forcing — no MLX kernel in the test product).

> qwen25vl-mlx-swift `Eff: ✅` (2026-06-30, engine 0.15.0) — the **VLM** sweep target, which combines two levers: a vision tower + an autoregressive LM. **P2 (vision-tower evict):** the ViT encodes the image once then is idle through the LM decode loop, so the core pipeline now holds `vision` as a rebuildable `private(set) var` — `load()` captures a `visionBuilder` closure over the (mmap-backed) ViT weights; `generate()` runs `ensureVision()` → encode → `eval(imageFeatures)` → `evictVision()` (`vision = nil` + `Memory.clearCache()`) before the LM prefill+decode, rebuilding lazily on the next call. Stays synchronous (the builder closure isn't async), so no `#isolation` hop was needed. Modest (the 3B LM ≫ the ViT) but real. **P1 split (MEASURED via gated `MemoryReportTests`, M5 Max, envelope: image 800×557 ≈0.45 MP → ~580 vision tokens × maxTokens 256, post-evict):** bf16 resident floor **7.51 GB**, worst peak **9.98 GB** → transient **2.47 GB**; int4 floor **3.07 GB**, peak **4.04 GB** → transient **0.97 GB**. Declared `QuantFootprint(.bf16, resident 7.7 GB, peakActivation 3.0 GB)` and `(.int4, resident 3.2 GB, peakActivation 1.2 GB)` (+`QuantConfigured`; single size so no `FootprintConfigured`), replacing the **flat** 9.6 / 4.5 GB — the old flat number was the *peak*, baking the activation into residency. Per the Qwen-LLM lesson the transient is **measured per quant** (it does NOT width-scale across quants: int4's 0.97 GB ≠ a scale of bf16's 2.47 GB, since the quantized LM's prefill scratch is smaller); it's the image-token-inflated LM prefill/KV-cache scratch and scales with the (image-res × maxTokens) envelope. P3 (mmap): loads via `MLX.loadArrays` (lazy safetensors) — note only. P4 (BudgetAware) deferred (quant is config-chosen). Engine pin `from: 0.3.0 → 0.15.0`, no API drift (the `imageAnalysis` surface + `ModelPackage` are unchanged). Smoke (`OracleSmokeTests`, chart-read "29") still green after the evict refactor.

## 🧱 Shared foundation (not capability providers)

| Package | Provides | Role | Home | Avail |
|---|---|---|---|---|
| mlx-engine-swift | the engine (MLXToolKit/MLXServeCore/UI/retrieval) + **MLXEngineTestKit** (opt-in category testing harness) | engine | MLXEngine/ | ✅ (0.39.0, contract 1.30.0) |
| mlx-swift-lm | LLM/VLM building blocks | shared | video/LTX_DEV | ✅ |
| mlx-constrained-decoding-swift | JSON grammar-constrained decoding (`responseFormat` runtime: byte-level JSON FSM + vocab classification + `LogitProcessor`) | shared | think/PROD | ✅ (v0.1.0) |
| flux2-vae-mlx-swift | FLUX.2 VAE decoder | shared | image/PROD | ✅ |
| wan-core-mlx-swift | shared Wan-family video core | shared | video/WAN_DEV | 🔬 | <!-- CAN sweep 2026-07-09: per-chunk Task.isCancelled bails in decodeStreaming / decodeStreaming22 / encodeStreaming (bail AFTER first append — empty concat would crash); per-tile bail in the vae22 halo-tiler deliberately NOT added (mid-tile break mis-shapes the strip concat); commit b791b62 pushed; consumers inherit via local-path pin. --> |

---

## Efficiency sweep status (the 1.14 program)

The **Eff** column above IS the sweep tracker (one source of truth — don't duplicate it elsewhere).
Order: **LTX first** → most-consumed (image caps, Qwen LLM/TTS) → optimizer family (BiRefNet/upscale/
NAFNet/SigLIP2) → Wan last (dedicated deep-dive). Per-package work orders are the
`EFFICIENCY-ADOPTION.md` briefs in each package repo (template: `ltx-2-mlx-swift/EFFICIENCY-ADOPTION.md`).
Plan + rationale: `EngineeringDocs/MLXEngineDocs/ENGINE-ROADMAP.md` → "Library efficiency program".

## Cross-references
- Capability schemas: `Sources/MLXToolKit/Capability.swift` + `capability-contract.md`.
- App-side consumption of these models: the `mlxengine-implementation` skill.
- Bringing a new model in (and adding its row here): the `mlx-swift-integration` skill.
